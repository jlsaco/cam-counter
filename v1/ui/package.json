{
  "name": "cam-counter-ui",
  "version": "0.0.0",
  "private": true,
  "description": "SPA local + suite Playwright E2E. NOTA: la SPA React/Vite/Tailwind completa llega en PR09; en esta base v1/ui/public/index.html es un harness mínimo vanilla para el E2E.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install --with-deps chromium",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3"
  }
}
