# chore: mover el sistema edge a v1/ (git mv, sigue ejecutable vía unit renderizada) + esqueleto de repo + contracts/ + scripts/version.py (sin-tag seguro)

# PR01 — Mover el sistema edge a `v1/` + esqueleto de monorepo + `contracts/` + `scripts/version.py` (sin-tag seguro)

Eres un agente Claude CLI que debe completar UN Pull Request, de forma autónoma y sin más contexto que este documento. Trabajas en el repositorio ya clonado `cam-counter` (raíz del repo es tu directorio de trabajo). Lee este prompt entero antes de actuar. Al final DEBES abrir un PR con `gh` y devolver su URL.

---

## 1) Contexto

`cam-counter` (GitHub: `github.com/jlsaco/cam-counter`, rama por defecto `main`) es un proyecto que hoy contiene un detector de personas en el borde: una **Raspberry Pi 5 + acelerador Hailo-8 + YOLOv8s** que toma vídeo de una cámara EZVIZ/Hikvision por RTSP y publica un stream MJPEG en `:8080`. Corre como servicio `systemd` (`hailo-personas`).

El producto está evolucionando hacia un **contador de personas multi-cámara/multi-sitio, edge-first, con UI local en la LAN y actualización OTA de una flota de Pis**, todo en este monorepo. Ese trabajo se ejecuta como una **pila de PRs apilados** (cada PR ramifica sobre el anterior). Tu PR, **PR01**, es la **base transversal** sobre la que se apilan TODOS los PRs posteriores: hace el `git mv` del sistema edge actual a `v1/`, crea el esqueleto de carpetas del monorepo, deja los contratos canónicos en `contracts/`, y añade `scripts/version.py`. Este PR **NO cambia lógica de código edge**; solo mueve, renombra rutas derivadas, crea esqueleto y contratos.

Estado real del repo en este punto (verificado; pueden existir cambios menores hechos por el PR previo PR00b sobre credenciales — ver más abajo):
- `detection/` — código Python del detector: `yolo_personas_mt.py` (producción, multi-hilo), `yolo_personas.py` (un hilo), `test_hailo_person.py`, `profile_pipeline.py`.
- `rtsp-enable/` — utilidad (Java + box64) que reactiva el RTSP de la cámara; incluye `start_detection.sh`, `rtsp_enable_final.sh`, `enable_rtsp_now.sh`, `build.gradle.kts`, `gradlew`, `src/`, `gradle/`, `_scripts_exploracion/`. (Las carpetas pesadas `lib/` y `x64root/` y los `*.jar` están en `.gitignore`, no están versionadas.)
- `systemd/hailo-personas.service` — unit del servicio.
- `docs/` — `HALLAZGOS.md` (bitácora técnica del edge) e `images/` (`deteccion_resultado.jpg`, `frame_en_vivo.jpg`).
- `README.md` (raíz) — describe la estructura actual y las instrucciones de instalación/operación.
- `.gitignore` (raíz).
- **No existen aún**: `terraform/`, `v1/`, `ota/`, `scripts/`, `contracts/`, ni tags git (`git tag -l` está vacío; `git describe --tags` falla con "No names found").

### Hechos VERIFICADOS importantes para este PR (no asumas otra cosa; reléelos en los archivos):

1. **`systemd/hailo-personas.service` usa rutas ABSOLUTAS hardcodeadas**:
   ```
   WorkingDirectory=/home/pi/Documents/hailo-ezviz-personas/rtsp-enable
   ExecStart=/home/pi/Documents/hailo-ezviz-personas/rtsp-enable/start_detection.sh
   ```
   Esas rutas **no corresponden a la estructura del repo** (`hailo-ezviz-personas` ≠ `cam-counter`). El `README.md` (en la sección de restauración en otra Pi) las reescribe en tiempo de instalación con un `sed`:
   ```
   sudo sed -i "s|/home/pi/Documents/hailo-ezviz-personas|$(cd .. && pwd)|g" /etc/systemd/system/hailo-personas.service
   ```
   Es decir, hoy el path correcto se obtiene por sustitución del token `/home/pi/Documents/hailo-ezviz-personas` por la ruta real del clon.

2. **`rtsp-enable/start_detection.sh` YA deriva sus rutas de `dirname`** (no usa rutas absolutas): hace `BASE="$(cd "$(dirname "$0")" && pwd)"`, `REPO="$(dirname "$BASE")"`, `DETECTION="$REPO/detection/yolo_personas_mt.py"`. Por tanto, al moverlo a `v1/rtsp-enable/`, `REPO` pasará a ser `.../v1` y `DETECTION` resolverá a `.../v1/detection/yolo_personas_mt.py` automáticamente — **siempre que `detection/` también se mueva a `v1/detection/`**. Verifica esto tras el move.

3. **No hay tags git**. `scripts/version.py` debe degradar limpio (nunca lanzar excepción) cuando no hay tags.

4. **Credenciales de cámara**: el PR previo (PR00b) ya debió rotar/parametrizar el secreto de la cámara (la contraseña `RWCHBY` debe estar tratada como QUEMADA y sustituida por una variable/fuente externa como `CAM_PASS` desde fichero en `shared/` o variable de entorno). **En tu PR NO debes reintroducir ningún secreto.** Cuando muevas archivos, NO añadas credenciales en claro. Si al mover encuentras todavía un secreto en claro en algún script vivo, NO es tu trabajo cambiar su lógica, pero **no debes empeorarlo**: parametrízalo solo si es necesario para no romper, y nunca escribas una credencial real nueva. Si dudas, deja la variable/placeholder que dejó PR00b.

### Contratos canónicos resueltos (deben quedar reflejados en `contracts/` como JSON Schema)

Estas son las definiciones acordadas del producto. Crea un JSON Schema (draft 2020-12) por cada uno, con **exactamente** estos nombres de campo (snake_case). Estos schemas son solo documentación-contrato en este PR (los modelos Pydantic ejecutables llegan en PRs posteriores); deben ser JSON válido y coherentes con esta lista.

**Convenciones transversales que los schemas deben respetar:**
- Identificadores `site_id`, `device_id`, `camera_id`: slugs ASCII en minúscula que cumplen el patrón regex `^[a-z0-9][a-z0-9-]{1,62}$` (SIN `#`, SIN `/`, porque `#` delimita claves compuestas de DynamoDB y `/` delimita rutas de S3). `camera_id` es globalmente único, con forma `'{device_id}-cam{N}'`. Aplica este `pattern` en los schemas donde aparezcan estos campos.
- Coordenadas/geometría: floats normalizados `0..1` relativos al frame original, origen arriba-izquierda. Nunca píxeles.
- `event_id` es DETERMINISTA = sha1 en hex minúscula de `site_id|device_id|camera_id|track_id|crossing_seq` (sha1 NO criptográfico, solo para deduplicación idempotente del sync).
- `crossing_seq` es un **contador MONÓTONO PERSISTIDO POR CÁMARA** (no por track, no reiniciable) — documenta esto explícitamente en la descripción del campo dentro del schema de crossing_event.
- Versionado: SemVer canónico (`vX.Y.Z`, prereleases `-rc.N`), un solo string que fluye por bundle-manifest, channel-manifest, device-registry y `/api/device`.

Los cinco schemas a crear en `contracts/`:

1. **`contracts/crossing_event.schema.json`** — Evento de cruce de línea. Campos:
   `event_id` (string, determinista; descripción: sha1 hex minúscula de `site_id|device_id|camera_id|track_id|crossing_seq`, NO criptográfico, solo dedupe), `site_id`, `device_id`, `camera_id` (los tres con el `pattern` de slug), `track_id` (string), `crossing_seq` (integer ≥ 0; descripción: **contador monótono persistido por cámara, no por track, no reiniciable**), `direction` (enum `["in","out"]` — único valor de cable/almacenado), `positive_label` (string, p.ej. `"subieron"`), `negative_label` (string, p.ej. `"bajaron"`), `label` (string, etiqueta humana resuelta en el momento del evento), `line_version` (integer, config de línea en vigor), `ts_event_ms` (integer, epoch ms UTC, autoritativo), `ts_event_iso` (string, formato `date-time`, UTC), `confidence` (number), `clip_key` (string o null, clave S3 del media), `clip_status` (enum `["pending","uploading","uploaded","failed"]`), `schema_version` (integer, const 1), `synced` (integer enum `[0,1]`, solo-local), `created_at` (string `date-time`). `required` razonable (al menos los identificadores, `direction`, `ts_event_ms`, `ts_event_iso`, `schema_version`, `event_id`, `crossing_seq`).

2. **`contracts/line_config.schema.json`** — Config de la línea-umbral por cámara. Campos sugeridos coherentes con el producto: `site_id`, `device_id`, `camera_id` (slug pattern), `config_version` (integer, monótono, hot-reload), `line` (objeto con `a` y `b`, cada uno `{x:number 0..1, y:number 0..1}` — endpoints normalizados), `positive_side` (integer enum `[-1,1]`, qué flip de signo cuenta como `in`), `positive_label` (string, p.ej. `"subieron"`), `negative_label` (string, p.ej. `"bajaron"`), `updated_at` (string `date-time`), `schema_version` (integer, const 1).

3. **`contracts/device_registry_item.schema.json`** — Item del registro de dispositivos (DynamoDB `cam-counter-devices`). Campos: `device_id` (slug pattern), `site_id` (slug pattern), `camera_ids` (array de strings con slug pattern), `release_channel` (enum `["canary","stable"]`), `desired_version` (string SemVer; descripción: lo escribe la nube = versión del manifiesto del canal; ESPEJO de observabilidad, NUNCA leído por el agente para decidir actualizar), `reported_version` (string SemVer, lo escribe la Pi), `last_good_version` (string SemVer), `last_update_status` (enum `["idle","downloading","verifying","activating","healthy","rolled_back","failed"]`), `last_update_error` (string o null), `last_seen_at` (string `date-time` UTC), `agent_version` (string), `status` (enum `["online","offline","updating","degraded"]`), `hardware` (objeto `{model:string, hailo_fw:string}`), `schema_version` (integer, const 1).

4. **`contracts/channel_manifest.schema.json`** — Manifiesto por canal en el bucket de releases (`channels/<channel>/manifest.json`). Campos: `schema_version` (integer, const 1), `channel` (enum `["canary","stable"]`), `version` (string SemVer), `sequence` (integer, monótono), `artifact` (objeto `{key:string, sha256:string, size_bytes:integer, sig_key:string}`), `native_blob` (objeto `{key:string, sha256:string}`), `min_agent_version` (string SemVer), `released_at` (string `date-time`), `released_by` (string), `git_sha` (string), `previous_version` (string SemVer o null, para rollback de un clic). Documenta en una `description` de nivel raíz que este manifiesto es la ÚNICA fuente de la versión deseada que lee el agente OTA (vía SigV4, nunca presigned URLs).

5. **`contracts/bundle_manifest.schema.json`** — Manifiesto embebido en el artefacto OTA. Campos: `schema_version` (integer, const 1), `version` (string SemVer; = el mismo string que el resto del sistema), `git_sha` (string), `built_at` (string `date-time`), `min_agent_version` (string SemVer), `entrypoint` (string, p.ej. ruta relativa al ejecutable dentro del bundle), `python` (string, versión objetivo, opcional). `required` razonable.

Cada schema debe incluir `"$schema": "https://json-schema.org/draft/2020-12/schema"`, `"$id"` único (p.ej. `https://cam-counter/contracts/crossing_event.schema.json`), `"title"`, `"type": "object"`, y `"additionalProperties": false` salvo donde explícitamente convenga lo contrario.

---

## 2) Objetivo

Convertir el repo de "detector edge suelto en la raíz" a "monorepo con `v1/` versionado, esqueleto de subsistemas, contratos canónicos y derivación de versión robusta", **preservando la historia git** del código edge y **manteniéndolo ejecutable** en la Pi mediante una unit `systemd` **renderizada por un instalador idempotente** (no rutas absolutas hardcodeadas ni `v1/` hardcodeado). Concretamente:

1. `git mv` de `detection/`, `rtsp-enable/`, `systemd/` bajo `v1/` con historia preservada.
2. Mover los **docs históricos del edge** (`docs/HALLAZGOS.md`, `docs/images/`) a `v1/docs/`, dejando un `docs/` nuevo en la raíz SOLO para docs de producto nuevos (no muevas docs de producto inexistentes; crea los placeholders indicados abajo).
3. Sustituir las rutas absolutas `/home/pi/...` de la unit por un **PLACEHOLDER** + un **script instalador idempotente** que renderiza `WorkingDirectory`/`ExecStart` desde la ruta real del repo clonado. Actualizar las instrucciones de instalación del README al nuevo subárbol `v1/`. La Pi sigue clonando en su ruta actual; la unit se **renderiza**, NO se hardcodea a `v1/`.
4. Actualizar TODOS los enlaces markdown relativos del README raíz a las rutas movidas y reescribir su sección de estructura.
5. Crear el esqueleto vacío-con-README: `terraform/{modules,environments/prod}`, `v1/api/`, `v1/ui/`, `v1/edge/`, `ota/`, `scripts/`.
6. Crear `contracts/` con los cinco JSON Schemas canónicos.
7. Crear `scripts/version.py` que deriva SemVer vía `git describe --tags --always` y degrada a `0.0.0-dev.N+g<sha>` sin tags (nunca lanza); `--json` imprime `{version, git_sha, is_dirty, is_release}`. Añadir un test que cubre el camino sin-tag.
8. Añadir reglas `.gitignore` para terraform y node.
9. **Sin cambios de lógica** en el código edge.

---

## 3) Alcance y NO-alcance

### En alcance
- Mover archivos con `git mv` (preservar historia). Ajustar rutas DERIVADAS y referencias de docs.
- Crear esqueleto, contratos, `scripts/version.py` + su test, instalador de la unit, y placeholders de docs de producto.

### NO-alcance (NO TOCAR)
- **NO cambies la lógica** de ningún `.py`, `.sh`, `.gradle`, ni del activador RTSP (`rtsp-enable/`). Solo cambia rutas/referencias estrictamente necesarias por el move (y, dentro de eso, NO toques la lógica de activación de RTSP).
- **NO** crees recursos en AWS, ni terraform real con providers (solo carpetas + README placeholder). No ejecutes `terraform apply`/`plan` contra la nube.
- **NO** crees tags git.
- **NO** muevas ni renombres `lib/`, `x64root/`, `.jar` (siguen ignorados).
- **NO** reintroduzcas el secreto de la cámara ni ninguna credencial en claro. **Cero secretos en el repo.**
- **NO** crees `CLAUDE.md` ni `.claude/` (es de PR00; no lo dupliques).
- **NO** implementes los modelos Pydantic, la API, la UI, el counter, ni el agente OTA (esos son PRs posteriores). Solo el esqueleto y los contratos.
- **NO** uses squash al mergear (no es tu decisión, pero NO reescribas la historia con `filter-repo` ni `rebase` que rompa SHAs).

---

## 4) Restricciones

- **Stack**: edge en Python; UI será React + TypeScript + Vite + Tailwind (solo esqueleto aquí); infra en Terraform (solo esqueleto aquí). Monorepo: TODO vive en este repositorio.
- **Edge-first / offline-tolerante**: el diseño asume que el Pi cuenta y persiste en local; la nube solo sincroniza. (Relevante para los contratos, no para el código de este PR.)
- **Multi-cámara/multi-sitio desde v1**: todo indexado por `site_id`/`device_id`/`camera_id` (slugs `^[a-z0-9][a-z0-9-]{1,62}$`). Refleja esto en los schemas.
- **Cero secretos en el repo**: nada de claves de larga vida en git, ni contraseñas de cámara en claro. Credenciales por env/SSM/Secrets Manager. `gitleaks` correrá en CI en PRs posteriores; no dejes nada que lo dispare.
- **Prefijos/identidad AWS** (solo para documentación/contratos, NO crear nada): cuenta `950639281773`, región `us-east-1`, prefijo `cam-counter-`. Tres buckets distintos, nunca conflados:
  1. `cam-counter-rpi-artifacts-950639281773` = backup de binarios EXISTENTE, RESERVADO/no-tocar.
  2. `cam-counter-media-950639281773` = media de producto NUEVO (clips/gifs), clave `media/{site_id}/{device_id}/{camera_id}/{yyyy}/{mm}/{dd}/{event_id}.{ext}`.
  3. `cam-counter-fleet-releases-950639281773` = artefactos OTA + manifiestos de canal.
  Tabla DynamoDB de devices: `cam-counter-devices`. (Estos nombres aparecen en descripciones de los schemas y en READMEs de esqueleto; no se crea nada en AWS.)

---

## 5) Rama base y rama nueva

- **Rama base** (de la que partes y contra la que abrirás el PR): `feat/00b-secret-remediation` (la rama del PR previo PR00b). Si esa rama no existe en remoto, parte de `origin/main` y deja constancia en el cuerpo del PR de que la base esperada era `feat/00b-secret-remediation`.
- **Rama nueva a crear**: `feat/01-monorepo-v1-skeleton`.

Comandos de partida:
```bash
git fetch origin
# base preferida: feat/00b-secret-remediation
git checkout feat/00b-secret-remediation 2>/dev/null || git checkout -b feat/00b-secret-remediation origin/feat/00b-secret-remediation 2>/dev/null || git checkout main
git checkout -b feat/01-monorepo-v1-skeleton
```

---

## 6) Pasos sugeridos

> Usa rutas absolutas o relativas a la raíz del repo de forma consistente. Verifica cada paso.

### A) Crear la rama
Como en la sección 5.

### B) Mover el sistema edge a `v1/` con `git mv` (preservar historia)
```bash
mkdir -p v1
git mv detection v1/detection
git mv rtsp-enable v1/rtsp-enable
git mv systemd v1/systemd
mkdir -p v1/docs
git mv docs/HALLAZGOS.md v1/docs/HALLAZGOS.md
git mv docs/images v1/docs/images
```
Tras esto, `docs/` raíz queda vacío de contenido histórico. **No borres `docs/`**: lo reutilizarás para docs de producto nuevos (paso F). Verifica que `git mv` preservó la historia con `git log --follow`.

### C) Verificar que `start_detection.sh` sigue resolviendo
`v1/rtsp-enable/start_detection.sh` deriva `REPO` por `dirname` del `dirname` de su ubicación, así que ahora `REPO=.../v1` y `DETECTION=.../v1/detection/yolo_personas_mt.py`. **Confírmalo leyendo el archivo y con `bash -n`** (chequeo de sintaxis). NO cambies su lógica. Si — y solo si — el move dejara una referencia rota a `detection/`, ajústala mínimamente para apuntar a `v1/detection/...` vía la derivación existente (sin reescribir la lógica). Asegúrate de que un `grep` encuentra `v1/detection/yolo_personas_mt.py` o la cadena derivada `$REPO/detection` en ese script (la verificación lo exige).

### D) Unit `systemd` con PLACEHOLDER + instalador idempotente
1. Edita `v1/systemd/hailo-personas.service` para que NO tenga rutas absolutas ni `v1/` hardcodeado: usa un **placeholder claro** sustituible, p.ej.:
   ```
   WorkingDirectory=__CAM_COUNTER_REPO__/v1/rtsp-enable
   ExecStart=__CAM_COUNTER_REPO__/v1/rtsp-enable/start_detection.sh
   ```
   (El placeholder `__CAM_COUNTER_REPO__` se sustituye por la ruta REAL del clon, p.ej. `/home/pi/Documents/cam-counter`.)
2. Crea un instalador idempotente, p.ej. `scripts/install_hailo_service.sh`, que:
   - Determine la ruta real del repo (raíz del clon) de forma robusta (`git rev-parse --show-toplevel` o derivando de `dirname` del propio script).
   - Renderice la unit sustituyendo `__CAM_COUNTER_REPO__` por esa ruta real, escribiéndola en `/etc/systemd/system/hailo-personas.service`.
   - Sea idempotente (volver a correrlo deja el mismo resultado; no duplica nada).
   - Haga `systemctl daemon-reload` y `systemctl enable --now hailo-personas` (protege con guardas si `systemctl` no existe, para que el script pueda al menos `bash -n` y correr en dry-run en CI/x86; p.ej. detectar ausencia de systemd y solo renderizar a un fichero temporal imprimiendo el resultado).
   - Acepte una variable de entorno opcional para un destino alternativo de la unit (para testear el render sin permisos de root).
   - El script debe pasar `bash -n` y, en x86 sin systemd, ser capaz de **renderizar** la unit a stdout o a un fichero de prueba sin fallar.
3. Hazlo ejecutable: `chmod +x scripts/install_hailo_service.sh` y márcalo con `git update-index --chmod=+x` si hace falta tras `git add`.

### E) Actualizar el README raíz
- Reemplaza la sección "📁 Estructura" para reflejar la nueva disposición: `v1/` (con `detection/`, `rtsp-enable/`, `systemd/`, `docs/`), `terraform/`, `ota/`, `scripts/`, `contracts/`, y `docs/` de producto en la raíz. Explica brevemente que el sistema edge histórico vive bajo `v1/` y que los PRs siguientes construyen el producto sobre esta base.
- Corrige TODOS los enlaces markdown relativos: `docs/HALLAZGOS.md` → `v1/docs/HALLAZGOS.md`; cualquier referencia a `detection/...`, `rtsp-enable/...`, `systemd/...` en comandos/instrucciones → su nueva ruta `v1/...` cuando aplique (p.ej. `bash v1/rtsp-enable/rtsp_enable_final.sh`, `python3 v1/detection/test_hailo_person.py`, `sudo cp v1/systemd/hailo-personas.service ...`).
- Actualiza la sección de instalación del servicio para usar el **instalador idempotente** en vez del `sed` manual sobre `/home/pi/Documents/hailo-ezviz-personas`. Deja claro que la Pi sigue clonando en su ruta actual y que la unit se renderiza desde el placeholder. Si mencionas el `git clone`, el path de clon puede seguir siendo el actual del operador; el instalador resuelve la ruta real automáticamente.
- **No reintroduzcas el secreto**: si el README tenía la contraseña `RWCHBY` en comandos de ejemplo, debe estar ya parametrizada por PR00b (p.ej. `rtsp://admin:$CAM_PASS@...` o `<codigo>`). NO escribas una contraseña real. Si encuentras una credencial en claro residual, sustitúyela por un placeholder/variable (`$CAM_PASS` o `<codigo-de-verificacion>`), sin alterar el resto.
- No debe quedar ningún enlace markdown roto (la verificación incluye un link-check).

### F) Esqueleto del monorepo
Crea estas carpetas, cada una con un `README.md` breve que explique su propósito y que dice "esqueleto; se implementa en PRs posteriores":
- `terraform/modules/.gitkeep` + `terraform/modules/README.md`
- `terraform/environments/prod/README.md`
- `v1/api/README.md`
- `v1/ui/README.md`
- `v1/edge/README.md`
- `ota/README.md`
- `scripts/README.md` (describe `version.py` e `install_hailo_service.sh`)
- `docs/ARCHITECTURE.md` (placeholder con un resumen de 1 párrafo de la arquitectura del producto: edge-first, multi-cámara, UI local, OTA de flota; remite a `v1/docs/HALLAZGOS.md` para la historia del edge)
- `docs/CONTRIBUTING.md` (placeholder con la política de PRs apilados: cada PR ramifica sobre el anterior; merge bottom-up; **NO squash** para preservar la base de las ramas apiladas, o procedimiento de rebase explícito tras cada merge)
- `contracts/README.md` (explica que aquí viven los JSON Schemas canónicos espejo de los modelos Pydantic futuros, y que cualquier rename de campo es BREAKING → requiere bump de `schema_version`)

Asegúrate de que `docs/` raíz NO contiene los docs históricos del edge (esos están en `v1/docs/`).

### G) `contracts/` — los cinco JSON Schemas
Crea los cinco archivos descritos en la sección 1, draft 2020-12, JSON válido, con los nombres de campo exactos. Asegúrate de que `crossing_event.schema.json` contiene literalmente la cadena `crossing_seq` y documenta que es contador monótono persistido por cámara.

### H) `scripts/version.py`
Implementa un script Python 3 (sin dependencias externas; usa `subprocess` + `git`) que:
- Derive la versión con `git describe --tags --always` (usa `--dirty` para detectar árbol sucio).
- Si HAY tags: produce el SemVer del tag (p.ej. `v1.2.3` → `1.2.3`, y para describe con commits extra una forma `1.2.3-dev.N+g<sha>` o similar PEP440/SemVer-prerelease coherente). Si el tag tiene prefijo `v`, quítalo en el campo `version`.
- Si NO hay tags: degrada a `0.0.0-dev.<N>+g<sha>` donde `<N>` = nº de commits (de `git rev-list --count HEAD`, con fallback a 0) y `<sha>` = `git rev-parse --short=7 HEAD`. **Nunca lanza excepción** por ausencia de tags; captura `CalledProcessError`/stderr "No names found" y degrada.
- Si tampoco hay commits/git: degrada a un default seguro (p.ej. `0.0.0-dev.0+gunknown`) sin fallar.
- `git_sha` siempre tiene longitud ≥ 7 (usa `--short=7`; si no hay git, usa `"unknown"` pero garantiza que `--json` emita un sha de ≥7 chars, p.ej. rellena `"unknown"` que ya tiene 7).
- Soporta:
  - Sin args o `--version`: imprime solo el string de versión.
  - `--json`: imprime un objeto JSON con EXACTAMENTE las claves `version`, `git_sha`, `is_dirty` (bool), `is_release` (bool — true solo si HEAD está exactamente en un tag SemVer limpio sin commits extra ni dirty).
- Tiene `if __name__ == "__main__":` y `argparse`.
- Funciona ejecutado como `python3 scripts/version.py` desde la raíz del repo.

> IMPORTANTE: en CI, los jobs de release usan `actions/checkout` con `fetch-depth: 0`; no es tu trabajo configurar CI aquí, pero el script no debe asumir profundidad. Que sea robusto a shallow clones (no falle; si no encuentra tags, degrada).

Añade un **test** que cubra el camino sin-tag, p.ej. `scripts/test_version.py` (o `tests/test_version.py`):
- Cree un repo git temporal (en `tmp`), haga 1 commit SIN tag, ejecute `version.py --json` con ese repo como cwd, y asegure: no excepción, `version` empieza por `0.0.0-dev.`, `git_sha` longitud ≥ 7, claves correctas presentes. Usa `subprocess`/`unittest` o `pytest` sin dependencias raras. El test debe pasar en x86 sin tags.

### I) `.gitignore`
Añade (sin borrar las reglas existentes) reglas para Terraform y Node:
```
# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform.lock.hcl
crash.log

# Node / Vite
node_modules/
dist/
.vite/
*.local
```
(Ajusta para no romper reglas previas; mantén `*.tfvars.example` permitido si lo usas.)

### J) Verificación local
Corre la sección 8 completa antes de abrir el PR. Corrige hasta que todo pase.

---

## 7) Definition of Done (objetivo y verificable)

1. `detection/`, `rtsp-enable/`, `systemd/` y los docs del edge (`HALLAZGOS.md`, `images/`) viven ahora bajo `v1/` con historia preservada (`git log --follow` muestra commits pre-move).
2. La unit `systemd` usa un **placeholder** renderizado por un script instalador idempotente (NO ruta absoluta fija ni `v1/` hardcodeado); las instrucciones de instalación del README están actualizadas para que la Pi, clonando en su ruta actual, obtenga una unit funcional.
3. Los enlaces relativos y la sección de estructura del README raíz están actualizados; **no hay enlaces markdown rotos** (link-check pasa).
4. `contracts/` contiene cinco JSON Schemas válidos (draft 2020-12) con los nombres de campo resueltos; `crossing_event` documenta `crossing_seq` como contador monótono por cámara.
5. `scripts/version.py` imprime un string SemVer **SIN tags presentes** y `--json` imprime las cuatro claves (`version`, `git_sha`, `is_dirty`, `is_release`); un test cubre el camino sin-tag.
6. Existe el esqueleto `terraform/{modules,environments/prod}`, `v1/api/`, `v1/ui/`, `v1/edge/`, `ota/`, `scripts/`, cada uno con README.
7. No se reintrodujo ningún secreto; `grep -ri 'RWCHBY' .` no encuentra una contraseña real nueva añadida por ti (si la historia/PR00b la dejó parametrizada, respétalo; NO añadas nuevas).
8. No hay cambios de lógica en el código edge (`detection/*.py`, scripts de `rtsp-enable/` salvo derivación de rutas estrictamente necesaria).

---

## 8) Verificación (comandos concretos a ejecutar)

Ejecuta TODOS y asegúrate de que pasan (exit 0 donde aplique). No abras el PR hasta que estén verdes.

```bash
# 1) Historia preservada del archivo de producción tras el move
git log --follow --oneline v1/detection/yolo_personas_mt.py | head -3
# (debe mostrar commits previos al move, no solo el de este PR)

# 2) start_detection.sh existe, sintaxis OK, y referencia la ruta v1/detection (o $REPO/detection derivado)
test -f v1/rtsp-enable/start_detection.sh \
  && bash -n v1/rtsp-enable/start_detection.sh \
  && grep -qE 'v1/detection/yolo_personas_mt\.py|REPO/detection' v1/rtsp-enable/start_detection.sh \
  && echo "start_detection OK"

# 3) Los cinco JSON Schemas son JSON válido
for f in contracts/*.schema.json; do python3 -c "import json,sys; json.load(open('$f')); print('ok', '$f')"; done
# (deben ser exactamente 5 ficheros *.schema.json)
ls contracts/*.schema.json | wc -l   # -> 5

# 4) version.py funciona SIN tags y emite las cuatro claves; version + git_sha>=7
python3 scripts/version.py --json | jq -e '.version and (.git_sha|length>=7) and (has("is_dirty")) and (has("is_release"))'
python3 scripts/version.py        # imprime un string de versión no vacío

# 5) crossing_event documenta crossing_seq
grep -q 'crossing_seq' contracts/crossing_event.schema.json && echo "crossing_seq OK"

# 6) El instalador de la unit pasa chequeo de sintaxis y NO contiene rutas absolutas /home/pi en la unit
bash -n scripts/install_hailo_service.sh && echo "installer syntax OK"
! grep -q '/home/pi/Documents/hailo-ezviz-personas' v1/systemd/hailo-personas.service && echo "no hardcoded path OK"
grep -q '__CAM_COUNTER_REPO__' v1/systemd/hailo-personas.service && echo "placeholder present OK"

# 7) El test de version pasa (ajusta la ruta al fichero de test que creaste)
python3 -m pytest scripts/test_version.py -q || python3 scripts/test_version.py

# 8) No hay secreto real reintroducido por este PR
! grep -rIn 'RWCHBY' . --include='*.sh' --include='*.py' --include='*.md' | grep -v 'CAM_PASS\|<codigo' || echo "revisa: RWCHBY presente"
# (Idealmente RWCHBY ya no aparece en working tree tras PR00b. NO añadas nuevas ocurrencias.)

# 9) Link-check del README raíz: no debe haber enlaces relativos rotos
python3 - <<'PY'
import re, os, sys
root = "."
md = open("README.md", encoding="utf-8").read()
broken = []
for m in re.finditer(r'\]\(([^)]+)\)', md):
    link = m.group(1).strip()
    if link.startswith(("http://","https://","#","mailto:")):
        continue
    path = link.split("#")[0]
    if not path:
        continue
    if not os.path.exists(os.path.join(root, path)):
        broken.append(link)
if broken:
    print("ENLACES ROTOS:", broken); sys.exit(1)
print("link-check OK")
PY

# 10) El esqueleto existe
for d in terraform/modules terraform/environments/prod v1/api v1/ui v1/edge ota scripts contracts docs; do test -d "$d" && echo "dir ok: $d" || { echo "FALTA: $d"; exit 1; }; done

# 11) git status limpio salvo lo añadido/movido por este PR
git status --porcelain
```

Si `jq` no está disponible, sustituye la verificación 4 por un equivalente en Python que parsee el JSON e imprima las claves.

---

## 9) Entrega: abrir el Pull Request

1. Asegúrate de que todo en la sección 8 pasa.
2. Haz commit con mensaje claro en la rama `feat/01-monorepo-v1-skeleton`. Termina el mensaje del commit con:
   ```
   Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
   ```
3. Push de la rama: `git push -u origin feat/01-monorepo-v1-skeleton`.
4. Abre el PR con `gh`, **base = `feat/00b-secret-remediation`** (la rama del PR previo; PR apilado). Si esa rama base no existe en remoto, usa `--base main` y anótalo en el cuerpo.
   ```bash
   gh pr create \
     --base feat/00b-secret-remediation \
     --head feat/01-monorepo-v1-skeleton \
     --title "chore: mover edge a v1/ + esqueleto monorepo + contracts + version.py (sin-tag seguro)" \
     --body "$(cat <<'EOF'
   ## Qué
   - Mueve `detection/`, `rtsp-enable/`, `systemd/` y los docs del edge (`HALLAZGOS.md`, `images/`) bajo `v1/` con `git mv` (historia preservada).
   - Reemplaza las rutas absolutas de la unit `systemd` por un placeholder `__CAM_COUNTER_REPO__` renderizado por `scripts/install_hailo_service.sh` (idempotente). La Pi sigue clonando en su ruta actual; la unit se renderiza, no se hardcodea a `v1/`.
   - Crea el esqueleto del monorepo: `terraform/{modules,environments/prod}`, `v1/{api,ui,edge}`, `ota/`, `scripts/`, `contracts/`, `docs/` (ARCHITECTURE, CONTRIBUTING).
   - Añade `contracts/` con los cinco JSON Schemas canónicos (crossing_event, line_config, device_registry_item, channel_manifest, bundle_manifest); `crossing_event` documenta `crossing_seq` como contador monótono por cámara.
   - Añade `scripts/version.py` (deriva SemVer vía `git describe --tags --always`, degrada a `0.0.0-dev.N+g<sha>` sin tags, nunca lanza) + test del camino sin-tag.
   - Actualiza enlaces y sección de estructura del README raíz; añade reglas `.gitignore` para Terraform/Node.

   ## Por qué
   PR01 es la base transversal de la pila de PRs del producto cam-counter (edge-first, multi-cámara, UI local, OTA de flota). Conserva la historia del edge y lo mantiene ejecutable mientras se construye el producto encima. Se fija el mecanismo de render de la unit (no rutas absolutas) y la derivación de versión robusta sin tags, requisitos de PRs posteriores.

   ## Sin cambios de lógica
   No se modifica la lógica del código edge ni del activador RTSP; solo move + derivación de rutas + esqueleto + contratos. Cero secretos en el repo.

   ## Cómo verificar
   Ver sección "Verificación" del prompt del PR. Resumen:
   - `git log --follow --oneline v1/detection/yolo_personas_mt.py | head -3` muestra commits pre-move.
   - `bash -n v1/rtsp-enable/start_detection.sh` y `grep` de `v1/detection|REPO/detection`.
   - `for f in contracts/*.schema.json; do python3 -c "import json;json.load(open('$f'))"; done` (5 ficheros).
   - `python3 scripts/version.py --json | jq -e '.version and (.git_sha|length>=7)'` SIN tags.
   - `grep -q crossing_seq contracts/crossing_event.schema.json`.
   - link-check del README sin enlaces rotos; instalador idempotente con placeholder; test de version pasa.

   🤖 Generated with [Claude Code](https://claude.com/claude-code)
   EOF
   )"
   ```
5. **Devuelve la URL del PR** como salida final (imprímela explícitamente).

---

## Recordatorios finales (obligatorios)
- **Cero secretos en el repo.** Está terminantemente PROHIBIDO añadir o reintroducir cualquier credencial, contraseña, código de verificación de cámara (como `RWCHBY`) o clave AWS de larga vida en cualquier archivo. Usa placeholders/variables (`$CAM_PASS`, `<codigo>`) y fuentes externas (env/SSM/Secrets Manager).
- **No cambies lógica del código edge.** Solo move, derivación de rutas, esqueleto y contratos.
- **Preserva la historia** (usa `git mv`; NO `filter-repo` ni rebase que rompa SHAs).
- **No crees recursos en AWS ni tags git.**
- Si algún paso de verificación falla, corrige antes de abrir el PR.
