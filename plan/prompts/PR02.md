# infra: backend de estado remoto Terraform (S3 + lock DynamoDB) + layout módulo/entorno + bootstrap AUTÓNOMO (apply por el runner) + CI terraform-validate (plan-only) + gitleaks

# infra: backend de estado remoto Terraform (S3 + lock DynamoDB) + layout módulo/entorno + bootstrap AUTÓNOMO + CI terraform-validate (plan-only) + gitleaks

# PR02 — infra: backend de estado remoto de Terraform (S3 + lock DynamoDB), bootstrap AUTÓNOMO por el runner (apply real) + layout módulo/entorno + CI terraform-validate (plan-only) + gitleaks

Eres un agente Claude CLI (modelo opus) ejecutándote en un runner autónomo, en el workspace ya clonado del repositorio **github.com/jlsaco/cam-counter**. Vas a producir UN solo Pull Request, pequeño-mediano, apilado sobre la rama del PR anterior. Trabaja de forma autónoma y termina abriendo el PR con `gh` y devolviendo su URL.

**IMPORTANTE — dos actores distintos, no los confundas:**
- **RUNNER MAD (TÚ):** dispones de credenciales AWS amplias en tu ENTORNO (jamás commiteadas). ERES QUIEN EJECUTA `terraform apply` de forma AUTÓNOMA dentro de este PR de infra, contra la cuenta REAL `950639281773` / `us-east-1`. En ESTE PR creas de verdad el backend de estado (bucket S3 + tabla DynamoDB de lock) mediante un bootstrap en dos fases (backend local → `terraform init -migrate-state` al backend S3), de forma idempotente y sin intervención humana.
- **GITHUB ACTIONS CI:** permanece SOLO-PLAN (`fmt`/`validate`/`tflint` y, en PRs posteriores, `plan` read-only vía OIDC). NUNCA hace `apply` (evita escalada de privilegios desde PRs). En ESTE PR el job de CI de terraform NO usa credenciales AWS y NO hace `plan` ni `apply`.

---

## 1) Contexto

**Producto.** `cam-counter` evoluciona de un detector de personas (Raspberry Pi 5 + acelerador Hailo-8 + cámara EZVIZ/Hikvision por RTSP) hacia un PRODUCTO de conteo de personas que cruzan una línea-umbral configurable, edge-first y multi-cámara/multi-sitio, con UI local en LAN y actualización OTA de una flota de Pis. Todo vive en ESTE monorepo.

**Cuenta y región AWS.** Cuenta `950639281773`, región `us-east-1`. Prefijo de nombres de recursos: `cam-counter-`.

**Tres buckets S3 distintos (NUNCA confundir):**
1. `cam-counter-rpi-artifacts-950639281773` — EXISTENTE, backup de binarios de operaciones. RESERVADO, NO TOCAR.
2. `cam-counter-media-950639281773` — NUEVO, media de producto (clips). Se crea en un PR POSTERIOR (PR04), no en este.
3. `cam-counter-fleet-releases-950639281773` — NUEVO, artefactos OTA + manifiestos. Se crea en un PR POSTERIOR (PR11), no en este.

Este PR (PR02) introduce un CUARTO bucket dedicado EXCLUSIVAMENTE al estado remoto de Terraform: **`cam-counter-tfstate-950639281773`**, más una tabla DynamoDB de lock **`cam-counter-tfstate-lock`**. Estos NO almacenan media ni artefactos; sólo el `.tfstate` y el lock de concurrencia de Terraform. **En este PR el runner los CREA de verdad** (apply autónomo), no sólo los declara.

**Modelo de despliegue (NUEVO, prioritario).** La pila de infra (PR02 state backend → PR03 OIDC + roles → PR04 media/eventos/devices/IAM per-Pi → … → PR11 releases) usa UN solo state de producción compartido (`environments/prod`) con lock en DynamoDB. **El runner MAD aplica de forma autónoma** dentro de cada PR de infra; **GitHub Actions CI permanece plan-only**. Este PR02 es el bootstrap del backend: resuelve el huevo-y-la-gallina del state remoto creando el bucket+tabla con backend LOCAL y migrando luego el state al backend S3, todo de forma idempotente y sin humanos.

**INVARIANTES TRANSVERSALES DE LA PILA (obligatorias; aplican a TODO PR de infra PR02/PR03/PR04/PR11 y están documentadas en CLAUDE.md/.claude/README.md por PR00):**

- **F1 — STATE ADITIVO Y MONÓTONO.** El state `environments/prod` es compartido por toda la pila apilada (PR02→PR03→PR04→…→PR11). El runner SOLO debe ejecutar `terraform apply` desde la rama apilada MÁS ALTA que contiene TODO el HCL acumulado hasta ese punto; **NUNCA reaplicar una rama inferior después de que una superior ya aplicó** (p.ej. nunca reaplicar la rama de PR02 contra un state ya poblado por PR03/PR04, porque la rama de PR02 no conoce esos módulos y `terraform plan` mostraría DESTRUIR sus recursos). ANTES de cualquier `apply`, ejecuta `terraform plan` e INSPECCIÓNALO: si aparece CUALQUIER `destroy` o `replace` de un recurso ya existente, ABORTA el apply. El plan debe ser ESTRICTAMENTE ADITIVO (sólo "to add"). En PR02, como es la rama MÁS BAJA de la pila y la primera en aplicar, el plan de bootstrap sólo debe crear el bucket de tfstate y la tabla de lock (o converger a "sin cambios" si ya existen): cualquier `destroy`/`replace` inesperado es señal de abortar.
- **F2 — APPLY AUTÓNOMO ACOTADO Y JUSTIFICADO.** El runner aplica HCL ANTES del merge humano. Esto se justifica EXCLUSIVAMENTE porque el HCL proviene de esta pila controlada y curada y porque el plan se inspecciona para ser estrictamente aditivo (F1). El runner NO aplica HCL arbitrario de terceros; el apply autónomo se restringe a los módulos enumerados de ESTE PR (en PR02: sólo el módulo `state-backend`). GitHub Actions CI permanece plan-only para cerrar la escalada de privilegios desde PRs no confiables. Este trade-off (apply autónomo pre-merge a cambio de plan-aditivo inspeccionado + HCL curado) se acepta y se declara explícitamente.
- **F3 — TAGS UNIFICADOS.** `default_tags` en el provider con claves CAPITALIZADAS `{ Project = "cam-counter", ManagedBy = "terraform", Env = "prod" }` MÁS tags lógicos en MINÚSCULA `project = "cam-counter"` y `managed_by = "mad-runner"` en TODOS los recursos. La verificación de `managed_by=mad-runner` busca la clave en MINÚSCULA. **NUNCA uses `ManagedBy = "mad-runner"` (capitalizado):** la clave capitalizada `ManagedBy` vale SIEMPRE `terraform`; el valor `mad-runner` vive SÓLO en la clave minúscula `managed_by`.

**Estado del repo cuando empiezas (asunciones de la pila apilada).** Este PR depende de PR01. Cuando arranques, la rama base de tu trabajo (la rama de PR01) ya debe contener:
- El sistema edge movido bajo `v1/` (mediante `git mv`, historia preservada).
- Un esqueleto de repo y el directorio `contracts/`.
- `scripts/version.py` (derivación de versión vía `git describe`, tolerante a "sin tags").
- Posiblemente `CLAUDE.md`, `.claude/` (de PR00, que ya documenta F1/F2/F3 como convención maestra) y remediación de secretos (de PR00b: el código de cámara `RWCHBY` está QUEMADO y parametrizado; nunca lo reintroduzcas).

Si al inspeccionar la rama base no existe `terraform/` ni `.github/workflows/ci.yml`, es lo esperado: este PR los crea por primera vez. Si `.github/workflows/ci.yml` YA existe (creado por un PR anterior), AÑADE los jobs nuevos a ese fichero en lugar de sobrescribirlo, respetando los jobs existentes.

**Convenciones transversales de Terraform que debes respetar (vienen del diseño global):**
- Módulos bajo `terraform/modules/<servicio>`. Una sola raíz "live" en `terraform/environments/prod` (directorio-por-entorno, NO workspaces).
- Estado remoto en S3 + lock en DynamoDB con clave primaria EXACTAMENTE `LockID`.
- Provider AWS fijado con `~> 5.x`; `required_version` de Terraform fijado (`>= 1.5.0`).
- `default_tags` en el provider: claves capitalizadas `{ Project = "cam-counter", ManagedBy = "terraform", Env = "prod" }` (ver F3).
- **Etiquetado adicional para trazabilidad y limpieza (F3):** TODOS los recursos llevan, además, los tags `project = "cam-counter"` y `managed_by = "mad-runner"` (en minúsculas), bien vía `default_tags` ampliados, bien vía `tags` por recurso. Esto permite identificar y limpiar lo que el runner creó autónomamente. (Convive con los `default_tags` capitalizados `{Project, ManagedBy, Env}`; no los elimines, AÑADE los de minúsculas. NUNCA pongas `ManagedBy = "mad-runner"`.)
- Tablas DynamoDB en modo `PAY_PER_REQUEST` con PITR (point-in-time recovery) habilitado.
- Todos los buckets NUEVOS: privados, `BlockPublicAccess` con las cuatro flags en `true`, cifrado `SSE-S3` (AES256), object-ownership `BucketOwnerEnforced`, y una bucket policy que DENIEGA tráfico no-TLS (`aws:SecureTransport = false`).
- **`apply` lo ejecuta el RUNNER de forma autónoma** dentro de los PRs de infra (PR02, PR03, PR04, PR11) contra AWS REAL, SIEMPRE tras inspeccionar el `plan` para garantizar que es estrictamente aditivo (F1), y debe ser IDEMPOTENTE (un segundo apply = "0 to add, 0 to change, 0 to destroy"). **CI sólo hace `fmt`/`validate`/`tflint`** y, en PRs posteriores, `plan` read-only vía OIDC. CI NUNCA hace `apply`. En ESTE PR el job de CI de terraform NO usa credenciales AWS y NO hace `plan` ni `apply`.

**Cero secretos en el repo.** Prohibido cualquier secreto en git (claves AWS de larga vida, passwords de cámara, tokens). El apply autónomo del runner usa las credenciales de SU ENTORNO (jamás commiteadas); NO introduzcas ninguna credencial en el repo. Las credenciales de CI vendrán por OIDC en PRs posteriores; aquí NO se configuran credenciales para CI. El código de verificación de cámara `RWCHBY` está quemado y NO debe aparecer en ningún archivo nuevo.

---

## 2) Objetivo

Crear la base de infraestructura como código Y dejarla DESPLEGADA DE VERDAD por el runner:
1. Un **módulo Terraform `state-backend`** que declare el bucket de estado (`cam-counter-tfstate-950639281773`) y la tabla de lock (`cam-counter-tfstate-lock`).
2. La **raíz live `terraform/environments/prod`** con providers fijados, `default_tags` (capitalizados + tags de runner en minúsculas, ver F3), una plantilla `backend.tf` y un procedimiento de bootstrap EN DOS FASES (apply con estado local → activar `backend.tf` → `terraform init -migrate-state`).
3. **Bootstrap AUTÓNOMO ejecutado por el runner:** TÚ ejecutas el bootstrap en dos fases contra AWS REAL para CREAR el bucket de estado y la tabla de lock, y migras el state local al backend S3. ANTES del apply inspeccionas el `plan` (F1: estrictamente aditivo, abortar si hay destroy/replace). Idempotente: si los recursos ya existen, no falla y un segundo plan da "sin cambios".
4. Un **Makefile y/o README** para `fmt`/`validate`/`plan`/`apply`/`bootstrap`/`destroy`.
5. Un **job de CI de Terraform** (`fmt -check`, `init -backend=false`, `validate` por módulo y por raíz, `tflint`) que corre en PRs que toquen `terraform/`, SIN credenciales AWS, SIN `apply` y SIN `plan`.
6. Un **job de CI de gitleaks** que escanea SÓLO el working-tree/diff del PR (no toda la historia), según la política definida en PR00b.

Todo debe pasar `terraform fmt -check` y `terraform validate` para la raíz `prod` y para el módulo, en x86 CI, sin hardware ni credenciales. ADEMÁS, el runner debe dejar el backend de estado creado y verificado contra AWS REAL.

---

## 3) Alcance y NO-alcance

**Dentro de alcance:**
- `terraform/modules/state-backend/` (bucket tfstate + tabla lock DynamoDB).
- `terraform/environments/prod/` (providers, `default_tags`, plantilla `backend.tf`, `versions.tf`, README de bootstrap en dos fases AUTÓNOMO).
- `terraform/Makefile` y/o `terraform/README.md` con targets/instrucciones `fmt`/`validate`/`plan`/`apply`/`bootstrap`/`destroy`.
- **Ejecución real del bootstrap por el runner contra AWS** (inspección del `plan` aditivo F1 → `apply` → `init -migrate-state`), con verificación de que los recursos EXISTEN de verdad.
- Documentación del **teardown** (`terraform destroy` o script equivalente) para desmontar el backend cuando proceda; recursos de bajo costo (S3 + DynamoDB on-demand).
- `.github/workflows/ci.yml`: añade (o crea) un job `terraform` y un job `gitleaks` con filtros de ruta apropiados.
- `.tflint.hcl` (config mínima del plugin AWS de tflint).
- Configuración de gitleaks (`.gitleaks.toml`) si hace falta para escanear sólo el diff/working-tree.

**NO-alcance (NO TOCAR / NO crear aquí):**
- NO crear el bucket de media (`cam-counter-media-...`) ni el de releases (`cam-counter-fleet-releases-...`) ni las tablas de eventos/registry. Eso es PR04 y posteriores.
- NO crear el provider OIDC de GitHub ni los roles IAM `plan`/`deploy`. Eso es PR03.
- NO añadir credenciales AWS de ningún tipo al CI (CI sigue siendo plan-only vía OIDC en PRs posteriores). NO ejecutar `terraform plan` ni `apply` en CI.
- NO tocar `rtsp-enable/` (lógica de reactivación RTSP). NO tocar el bucket existente `cam-counter-rpi-artifacts-950639281773`.
- NO mover archivos del edge ni rehacer lo de PR01.
- NO reintroducir el secreto `RWCHBY` ni ningún otro secreto. NO commitear ninguna credencial AWS del runner.
- NO hacer `git push --force` ni reescribir historia. NO crear tags git.
- NO commitear el fichero de state local (`terraform.tfstate*`) ni `.terraform/`: ignóralos (asegúrate de que están en `.gitignore`).
- NO aplicar HCL arbitrario fuera del módulo `state-backend` enumerado en este PR (F2). NO reaplicar esta rama (la más baja de la pila) una vez que un PR superior haya aplicado contra el mismo state (F1).

---

## 4) Restricciones

- **Stack infra:** Terraform (HCL). Provider `hashicorp/aws ~> 5.x`. `required_version` Terraform `>= 1.5.0`. Estado remoto S3 + lock DynamoDB (`LockID`). Directorio-por-entorno.
- **State remoto compartido (F1):** hay UN solo state de producción (`environments/prod/terraform.tfstate` en el bucket de tfstate) con lock en DynamoDB; todos los applies de la pila (PR02..PR11) lo comparten. El state es ADITIVO Y MONÓTONO: aplica SÓLO desde la rama apilada más alta con todo el HCL acumulado; NUNCA reaplicar una rama inferior tras una superior. ANTES de cualquier `apply`, inspecciona el `plan` y ABORTA si aparece cualquier `destroy`/`replace` de un recurso ya existente.
- **Apply autónomo acotado y justificado (F2):** el runner aplica HCL pre-merge SÓLO porque proviene de esta pila curada y el plan se inspecciona para ser estrictamente aditivo; el runner NO aplica HCL de terceros y se limita al módulo `state-backend` de este PR. CI permanece plan-only para cerrar la escalada desde PRs no confiables. Declara este trade-off en el cuerpo del PR.
- **Despliegue autónomo idempotente:** el runner ejecuta `apply` real; un segundo `apply` debe dar "0 to add, 0 to change, 0 to destroy". El bootstrap tolera que el bucket/tabla ya existan (idempotente, no falla): si existen en AWS pero no en el state, impórtalos (`terraform import`) en lugar de fallar.
- **Edge-first / multi-cámara / multi-sitio:** no afecta directamente a este PR de infra, pero todo recurso futuro se indexará por `site_id`/`device_id`/`camera_id` (slugs `^[a-z0-9][a-z0-9-]{1,62}$`). Aquí sólo el state-backend.
- **Prefijos AWS:** todo nombre de recurso empieza por `cam-counter-`. Cuenta `950639281773`, región `us-east-1`.
- **Etiquetado de trazabilidad (F3):** TODOS los recursos llevan `project=cam-counter` y `managed_by=mad-runner` (minúsculas), además de los `default_tags` capitalizados `{Project, ManagedBy=terraform, Env}`. NUNCA `ManagedBy="mad-runner"`.
- **Cero secretos en el repo:** prohibido cualquier secreto en git. Nada de claves de larga vida. Las credenciales del runner viven SOLO en su entorno. gitleaks corre en CI sobre el diff. Si el job de gitleaks encontrara un hallazgo en el diff de ESTE PR, corrígelo antes de abrir el PR.
- **CI plan-only en este PR:** el job de terraform usa `terraform init -backend=false`, NO asume ningún rol AWS y NO hace `plan`/`apply`.
- **Monorepo:** todo vive en este repositorio.

---

## 5) Rama base y rama nueva

- **Rama base (de la que ramificas):** `feat/01-monorepo-v1-skeleton` (la rama de PR01, de la que este PR depende). Asegúrate de partir de su HEAD actualizado (`git fetch origin` y basa la rama nueva en `origin/feat/01-monorepo-v1-skeleton` si existe en remoto; si sólo existe localmente, úsala localmente).
- **Rama nueva a crear:** `feat/02-tf-state-backend`.
- El Pull Request se abre con **base = `feat/01-monorepo-v1-skeleton`** (PR apilado), NO contra `main`.

Comando de arranque sugerido:
```
git fetch origin
git checkout feat/01-monorepo-v1-skeleton 2>/dev/null || git checkout -b feat/01-monorepo-v1-skeleton origin/feat/01-monorepo-v1-skeleton
git checkout -b feat/02-tf-state-backend
```
Si la rama base no existe ni local ni remota, DETENTE y reporta el problema en la salida en lugar de ramificar sobre `main`.

---

## 6) Pasos sugeridos

1. **Verifica la pila.** `git fetch origin`, confirma que existe `feat/01-monorepo-v1-skeleton`, ramifica `feat/02-tf-state-backend` desde ella.

2. **Crea el módulo `terraform/modules/state-backend/`** con estos archivos:
   - `versions.tf`: bloque `terraform { required_version = ">= 1.5.0"; required_providers { aws = { source = "hashicorp/aws", version = "~> 5.0" } } }`. (El módulo NO declara `provider` ni `backend`; los hereda de la raíz.)
   - `variables.tf`: variables con valores por defecto sensatos pero parametrizables: `state_bucket_name` (default `cam-counter-tfstate-950639281773`), `lock_table_name` (default `cam-counter-tfstate-lock`), `noncurrent_version_expiration_days` (default p.ej. 90).
   - `main.tf`:
     - `aws_s3_bucket` `tfstate` con `bucket = var.state_bucket_name`.
     - `aws_s3_bucket_versioning` → `status = "Enabled"`.
     - `aws_s3_bucket_server_side_encryption_configuration` → `sse_algorithm = "AES256"` (SSE-S3).
     - `aws_s3_bucket_public_access_block` → las 4 flags (`block_public_acls`, `block_public_policy`, `ignore_public_acls`, `restrict_public_buckets`) en `true`.
     - `aws_s3_bucket_ownership_controls` → `object_ownership = "BucketOwnerEnforced"`.
     - `aws_s3_bucket_lifecycle_configuration` → regla que EXPIRA versiones no-actuales tras `var.noncurrent_version_expiration_days` días (`noncurrent_version_expiration { noncurrent_days = ... }`), y opcionalmente `abort_incomplete_multipart_upload`.
     - `aws_s3_bucket_policy` que DENIEGA acceso cuando `aws:SecureTransport = false` (deny TLS-only) sobre el bucket y sus objetos.
     - `aws_dynamodb_table` `lock` con `name = var.lock_table_name`, `billing_mode = "PAY_PER_REQUEST"`, `hash_key = "LockID"`, `attribute { name = "LockID", type = "S" }`, y `point_in_time_recovery { enabled = true }`.
     - Tags de trazabilidad `project = "cam-counter"` y `managed_by = "mad-runner"` (MINÚSCULA, F3) en los recursos (o heredados vía `default_tags` de la raíz; si los pones en la raíz, asegúrate de que se aplican a todos los recursos del módulo). NUNCA uses la clave capitalizada `ManagedBy` con valor `mad-runner`.
   - `outputs.tf`: outputs `state_bucket_name`, `state_bucket_arn`, `lock_table_name`, `lock_table_arn`.

3. **Crea la raíz live `terraform/environments/prod/`** con:
   - `versions.tf`: `required_version` + `required_providers` (aws `~> 5.0`).
   - `providers.tf`: `provider "aws" { region = "us-east-1"; default_tags { tags = { Project = "cam-counter", ManagedBy = "terraform", Env = "prod", project = "cam-counter", managed_by = "mad-runner" } } }`. Opcional pero recomendado: `allowed_account_ids = ["950639281773"]`. (F3: los tags capitalizados `{Project, ManagedBy=terraform, Env}` y los de minúsculas `project`/`managed_by=mad-runner` conviven; ambos deben acabar en TODOS los recursos. La clave capitalizada `ManagedBy` vale `terraform`, NUNCA `mad-runner`.)
   - `main.tf`: `module "state_backend" { source = "../../modules/state-backend" }` (sin overrides, usando los defaults), más outputs que reexporten los del módulo.
   - `backend.tf.example` (plantilla, NO activa de inicio): contenido del bloque `terraform { backend "s3" { bucket = "cam-counter-tfstate-950639281773"; key = "environments/prod/terraform.tfstate"; region = "us-east-1"; dynamodb_table = "cam-counter-tfstate-lock"; encrypt = true } }`. Comenta claramente que `backend.tf.example` se ACTIVA (se copia a `backend.tf`) SÓLO en la fase 2 del bootstrap. (Si tras el bootstrap decides commitear `backend.tf` activo, está PERMITIDO y de hecho es deseable para que PR03+ usen el backend remoto sin pasos manuales; ver paso 4.)
   - Un `outputs.tf` si separas outputs del `main.tf`.

4. **Ejecuta el bootstrap AUTÓNOMO en dos fases (TÚ, el runner, contra AWS REAL)** y documenta el procedimiento en `terraform/environments/prod/README.md` (enlazado desde `terraform/README.md`):
   - **Fase 1 (estado local → crear backend):** SIN `backend.tf` activo presente, corre `terraform -chdir=terraform/environments/prod init` (estado local). ANTES de aplicar, ejecuta `terraform -chdir=terraform/environments/prod plan` e INSPECCIÓNALO (F1): el plan debe ser estrictamente aditivo (sólo CREAR el bucket de tfstate y la tabla de lock, o "sin cambios" si ya existen); si aparece CUALQUIER `destroy`/`replace` de un recurso ya existente, ABORTA y reporta. Si el plan es aditivo, corre `terraform -chdir=terraform/environments/prod apply -auto-approve` para CREAR de verdad el bucket de estado y la tabla de lock en AWS. (Resuelve el huevo-y-la-gallina: el backend no puede existir antes de crearse.) Es IDEMPOTENTE: si el bucket/tabla ya existen (p.ej. de una ejecución previa), no debe fallar; si Terraform no los conoce en su state, impórtalos (`terraform import`) en lugar de fallar, de modo que el apply converja a "sin cambios".
   - **Fase 2 (migración a remoto):** activa el backend copiando `backend.tf.example` → `backend.tf` y corre `terraform -chdir=terraform/environments/prod init -migrate-state -force-copy` para mover el `.tfstate` local al bucket S3 con lock DynamoDB. Tras migrar, un `terraform plan` debe dar SIN CAMBIOS.
   - **Idempotencia y verificación:** corre un SEGUNDO `plan`/`apply` y confirma "0 to add, 0 to change, 0 to destroy". Verifica contra AWS REAL que el bucket y la tabla EXISTEN (`aws s3api head-bucket --bucket cam-counter-tfstate-950639281773` y `aws dynamodb describe-table --table-name cam-counter-tfstate-lock`).
   - Deja MUY claro en el README que **este bootstrap lo ejecuta el RUNNER de forma AUTÓNOMA con las credenciales de su entorno** (no un operador humano, no el CI), que el apply va SIEMPRE precedido de la inspección del `plan` aditivo (F1), y que el apply autónomo se justifica porque el HCL proviene de la pila curada y el plan es estrictamente aditivo (F2). El CI NUNCA hace `apply` ni `plan`. Documenta también la regla de monotonía (F1): no reaplicar esta rama una vez que PR03/PR04/PR11 hayan aplicado contra el mismo state. Documenta el **teardown**: `terraform destroy` (o un script `make teardown`/`make destroy`) para desmontar el backend; nota que destruir el state-backend exige primero mover/extraer el state o usar backend local, y advierte que sólo debe hacerse al desmontar toda la pila. Recursos de bajo costo (S3 + DynamoDB on-demand).
   - NO commitees el `terraform.tfstate` local ni `.terraform/`. Si dejas `backend.tf` activo commiteado (recomendado), asegúrate de que NO arrastra ningún secreto.

5. **Crea `terraform/Makefile`** (y/o instrucciones en `terraform/README.md`) con targets:
   - `fmt` → `terraform fmt -recursive .`
   - `fmt-check` → `terraform fmt -check -recursive .`
   - `validate` → `terraform -chdir=modules/state-backend init -backend=false && terraform -chdir=modules/state-backend validate && terraform -chdir=environments/prod init -backend=false && terraform -chdir=environments/prod validate`
   - `bootstrap` → secuencia de fase 1 + fase 2 del paso 4 (plan-aditivo inspeccionado → apply local → migrate-state), idempotente; documentada como acción del RUNNER con credenciales de entorno.
   - `plan` → `terraform -chdir=environments/prod plan` (requiere backend remoto y credenciales; lo usa el runner / PRs posteriores; NO corre en CI). Documenta que su salida DEBE inspeccionarse para ser estrictamente aditiva (F1) antes de cualquier apply.
   - `apply` → `terraform -chdir=environments/prod apply -auto-approve` (lo ejecuta el RUNNER de forma autónoma tras inspeccionar el plan; NO corre en CI).
   - `destroy`/`teardown` → `terraform -chdir=environments/prod destroy` con la advertencia del paso 4 sobre el state-backend.
   - `tflint` → `tflint --chdir=modules/state-backend && tflint --chdir=environments/prod` (o recursivo).

6. **Crea `.tflint.hcl`** en la raíz `terraform/` (o donde lo invoques) con el plugin `aws` de tflint habilitado mínimamente.

7. **CI — `.github/workflows/ci.yml`.** Si el fichero ya existe, AÑADE jobs; si no, créalo. Define:
   - Disparadores: `pull_request` (y opcionalmente `push`), con `paths` que incluyan `terraform/**` y `.github/workflows/ci.yml` para el job terraform.
   - **Job `terraform`** (runner `ubuntu-latest`, SIN credenciales AWS, sin bloque de OIDC `permissions: id-token`):
     - `actions/checkout@v4`.
     - `hashicorp/setup-terraform@v3` con una versión fijada (p.ej. `1.13.x` o `~> 1.5`).
     - Paso `terraform fmt -check -recursive terraform/`.
     - Pasos `terraform -chdir=terraform/modules/state-backend init -backend=false && validate`.
     - Pasos `terraform -chdir=terraform/environments/prod init -backend=false && validate`. (Usa `-backend=false` para que el CI valide sin tocar el backend S3 real ni necesitar credenciales, aunque `backend.tf` esté commiteado.)
     - Instalar tflint (`terraform-linters/setup-tflint@v4`), `tflint --init`, y correr tflint sobre módulo y raíz.
     - NINGÚN paso de `plan` ni `apply`; NINGÚN `aws-actions/configure-aws-credentials`. (El `apply` lo hace el RUNNER MAD, no el CI.)
   - **Job `gitleaks`** (según política PR00b — escaneo del WORKING-TREE/diff del PR, NO toda la historia):
     - Usa `gitleaks` en modo `detect --no-git` (sobre el working tree) o `protect`/diff del PR, de modo que NO escanee commits históricos pre-rotación (donde el secreto ya invalidado `RWCHBY` puede seguir presente y dejaría el job rojo permanentemente).
     - Incluye `.gitleaks.toml` si necesitas allowlist documentada de la historia pre-rotación. El job debe terminar VERDE sobre el working-tree/diff de este PR.
     - NO uses la action por defecto de gitleaks que escanea toda la historia (`git log -p`); configúrala explícitamente para diff/working-tree.

8. **Verifica localmente** todo lo de la sección 8 antes de commitear, INCLUYENDO la verificación contra AWS REAL del backend ya desplegado y la confirmación de plan aditivo (F1). Corrige hasta que pase.

9. **Commitea** con mensajes claros en español, y termina el mensaje de commit con la línea de co-autoría requerida (ver sección 9). NO commitees `terraform.tfstate*` ni `.terraform/` ni credenciales.

10. **Abre el PR** con `gh` (sección 9) y devuelve la URL.

---

## 7) Definition of Done (objetivo y verificable)

1. `terraform/modules/state-backend/` declara el bucket de tfstate con versioning + SSE-S3 (AES256) + BlockPublicAccess (4 flags true) + lifecycle que expira versiones no-actuales + bucket policy TLS-only-deny, y la tabla de lock DynamoDB con `hash_key` EXACTAMENTE `LockID`, `PAY_PER_REQUEST` y PITR habilitado. Todos los recursos llevan tags `project=cam-counter` y `managed_by=mad-runner` (MINÚSCULA, F3), además de `{Project, ManagedBy=terraform, Env}` (la clave capitalizada `ManagedBy` vale `terraform`, NUNCA `mad-runner`).
2. `terraform/environments/prod/` tiene providers fijados (`required_version` + aws `~> 5.0`), `default_tags` con los tags capitalizados `{Project, ManagedBy=terraform, Env}` Y los de runner en minúsculas `project`/`managed_by=mad-runner` (F3), una plantilla de backend (`backend.tf.example`) y un README documentando el bootstrap en dos fases AUTÓNOMO (inspección de plan aditivo → apply local → activar backend → `init -migrate-state`) ejecutado por el RUNNER, la regla de state aditivo/monótono (F1), la justificación del apply autónomo (F2) y el procedimiento de teardown.
3. **PLAN ESTRICTAMENTE ADITIVO VERIFICADO (F1):** antes del apply de bootstrap, el `terraform plan` se inspeccionó y NO contenía ningún `destroy`/`replace` de un recurso ya existente (sólo creación del bucket de tfstate y la tabla de lock, o "sin cambios"). Si hubiera aparecido cualquier `destroy`/`replace`, el apply se habría ABORTADO. Esta verificación es un paso explícito y un item del DoD.
4. **EL BACKEND EXISTE DE VERDAD EN AWS (apply autónomo del runner):** `aws s3api head-bucket --bucket cam-counter-tfstate-950639281773` devuelve exit 0, y `aws dynamodb describe-table --table-name cam-counter-tfstate-lock` muestra la tabla con `KeySchema` `LockID` (HASH), `BillingMode=PAY_PER_REQUEST` y PITR `ENABLED`. El state de `environments/prod` está migrado al backend S3 (la `key environments/prod/terraform.tfstate` existe en el bucket).
5. **Idempotencia demostrada:** un SEGUNDO `terraform -chdir=terraform/environments/prod plan` (con backend remoto) da "0 to add, 0 to change, 0 to destroy" (sin cambios).
6. `.github/workflows/ci.yml` contiene un job `terraform` que corre `fmt -check` + `validate` (módulo y raíz) + `tflint` SIN credenciales AWS y SIN `apply`/`plan`; y un job `gitleaks` que corre VERDE sobre el working-tree/diff (no historia).
7. `terraform fmt -check -recursive terraform/` pasa, y `terraform validate` (con `-backend=false`) pasa tanto para la raíz `prod` como para el módulo `state-backend`.
8. No hay secretos en el diff (gitleaks verde). NO se ha commiteado `terraform.tfstate*`, `.terraform/` ni credenciales AWS. No se han tocado `rtsp-enable/`, el bucket `cam-counter-rpi-artifacts-...`, ni archivos del edge movidos en PR01. No se han creado tags git. CI NO ejecuta ningún `apply`/`plan`. El apply autónomo se restringió al módulo `state-backend` de este PR (F2).
9. El PR está abierto con base = `feat/01-monorepo-v1-skeleton` y su URL se devuelve como salida final.

---

## 8) Verificación (comandos concretos a ejecutar)

Ejecuta y confirma que TODOS pasan (exit 0). Usa rutas relativas a la raíz del repo o absolutas; estás en la raíz del workspace.

```
# --- Validación estática SIN credenciales (lo que también corre el CI) ---
# Formato y validación del módulo
terraform -chdir=terraform/modules/state-backend init -backend=false
terraform -chdir=terraform/modules/state-backend validate

# Formato recursivo de todo terraform/
terraform fmt -check -recursive terraform/

# Validación de la raíz prod (sin tocar backend remoto)
terraform -chdir=terraform/environments/prod init -backend=false
terraform -chdir=terraform/environments/prod validate

# La clave de lock es exactamente LockID
grep -q 'LockID' terraform/modules/state-backend/*.tf

# El bucket de tfstate y la tabla de lock con los nombres prefijados
grep -Rq 'cam-counter-tfstate-950639281773' terraform/
grep -Rq 'cam-counter-tfstate-lock' terraform/

# Tags de trazabilidad del runner presentes en MINÚSCULA (F3)
grep -Rq 'managed_by' terraform/
grep -Rq 'mad-runner' terraform/
# Y NUNCA la clave capitalizada ManagedBy con valor mad-runner (F3)
! grep -REq 'ManagedBy[[:space:]]*=[[:space:]]*"mad-runner"' terraform/

# --- Despliegue AUTÓNOMO del runner contra AWS REAL (este PR SÍ aplica) ---
# F1: ANTES del apply, inspecciona el plan y confirma que es ESTRICTAMENTE ADITIVO.
# Guarda el plan y verifica que NO hay destroy/replace; aborta si los hay.
terraform -chdir=terraform/environments/prod plan -no-color | tee /tmp/pr02-bootstrap.plan
! grep -Eq 'will be destroyed|must be replaced' /tmp/pr02-bootstrap.plan   # 0 destroy/replace = aditivo
# (Bootstrap idempotente: fase 1 apply local + fase 2 migrate-state. Reejecutable.
#  Ver target `make -C terraform bootstrap` o ejecuta los init/plan/apply/migrate del paso 4.)

# El bucket de estado EXISTE de verdad en AWS
aws s3api head-bucket --bucket cam-counter-tfstate-950639281773

# La tabla de lock EXISTE de verdad, con LockID HASH, PAY_PER_REQUEST y PITR ON
aws dynamodb describe-table --table-name cam-counter-tfstate-lock \
  --query 'Table.{Keys:KeySchema,Billing:BillingModeSummary.BillingMode}' --output json
aws dynamodb describe-continuous-backups --table-name cam-counter-tfstate-lock \
  --query 'ContinuousBackupsDescription.PointInTimeRecoveryDescription.PointInTimeRecoveryStatus' --output text  # ENABLED

# El state ya está en el backend S3 remoto (objeto presente)
aws s3api head-object --bucket cam-counter-tfstate-950639281773 --key environments/prod/terraform.tfstate

# IDEMPOTENCIA: un segundo plan (con backend remoto) NO tiene cambios
terraform -chdir=terraform/environments/prod init -input=false
terraform -chdir=terraform/environments/prod plan -detailed-exitcode   # exit 0 = sin cambios

# --- Higiene CI / secretos ---
# NO se ha añadido configuración de credenciales AWS al CI
! grep -Rq 'configure-aws-credentials' .github/workflows/ci.yml
# El job de terraform en CI NO hace apply ni plan
! grep -Eq 'terraform[[:space:]]+(apply|plan)' .github/workflows/ci.yml

# El secreto quemado no aparece en ningún archivo nuevo
! grep -Rq 'RWCHBY' terraform/ .github/

# No se ha commiteado state local ni .terraform/
! git ls-files | grep -E 'terraform\.tfstate|\.terraform/'

# (Si están disponibles en el entorno) tflint y gitleaks sobre el working tree
tflint --version >/dev/null 2>&1 && tflint --chdir=terraform/modules/state-backend || echo "tflint no instalado localmente (se ejecuta en CI)"
gitleaks version >/dev/null 2>&1 && gitleaks detect --no-git --source . --verbose || echo "gitleaks no instalado localmente (se ejecuta en CI)"
```

Notas:
- `terraform init -backend=false` NO requiere credenciales AWS ni acceso a red más allá de descargar el provider; es lo correcto para el job de CI plan-only.
- Las comprobaciones `aws ...`, el `plan` aditivo (F1) y el `plan -detailed-exitcode` requieren las credenciales del ENTORNO del runner (no commiteadas). Es el RUNNER quien las corre, NUNCA el CI.
- **F1 (abortar si el plan no es aditivo):** si `grep -Eq 'will be destroyed|must be replaced'` encuentra coincidencias en el plan, ABORTA el apply, NO continúes, y reporta el bloqueo. En PR02 el plan de bootstrap sólo debe crear el bucket de tfstate y la tabla de lock (o no cambiar nada si ya existen).
- `plan -detailed-exitcode` devuelve 0 si no hay cambios, 2 si hay cambios pendientes (eso sería un fallo de idempotencia: corrige el HCL/imports hasta que dé 0), y 1 ante error.
- Si `tflint`/`gitleaks` no están instalados en el entorno local del agente, basta con que el workflow de CI esté bien definido; documenta que se ejecutan en CI. No instales binarios de sistema si no es trivial.
- Si algún comando falla, corrige el HCL/workflow/bootstrap y repite hasta verde. El apply debe converger a idempotente.

---

## 9) Entrega: abrir el Pull Request

1. Asegúrate de estar en `feat/02-tf-state-backend` con los cambios commiteados. Mensajes de commit en español, claros (qué/por qué). NO incluyas `terraform.tfstate*`, `.terraform/` ni credenciales. Termina CADA mensaje de commit con exactamente:
   ```
   Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
   ```
2. Empuja la rama: `git push -u origin feat/02-tf-state-backend`.
3. Abre el PR APILADO con base en la rama del PR anterior:
   ```
   gh pr create \
     --base feat/01-monorepo-v1-skeleton \
     --head feat/02-tf-state-backend \
     --title "infra: backend de estado remoto Terraform (S3 + lock DynamoDB) + bootstrap autónomo (apply real) + CI plan-only" \
     --body "<cuerpo>"
   ```
   El `<cuerpo>` debe incluir, en español y de forma clara:
   - **Qué:** módulo `state-backend` (bucket `cam-counter-tfstate-950639281773` con versioning/SSE-S3/BPA/lifecycle/TLS-only; tabla `cam-counter-tfstate-lock` con `LockID`/PAY_PER_REQUEST/PITR), raíz `terraform/environments/prod` con providers fijados y `default_tags` (capitalizados `{Project, ManagedBy=terraform, Env}` + minúsculas `project=cam-counter`/`managed_by=mad-runner`, F3), plantilla `backend.tf.example`, bootstrap en dos fases AUTÓNOMO documentado, Makefile/README con `bootstrap`/`apply`/`destroy`, y jobs CI `terraform` (fmt/validate/tflint, sin credenciales, sin apply/plan) y `gitleaks` (diff/working-tree).
   - **Desplegado de verdad (apply autónomo del runner):** el backend de estado se ha CREADO contra AWS REAL (cuenta `950639281773`, `us-east-1`) por el runner MAD mediante bootstrap en dos fases (inspección de plan aditivo → apply local → `init -migrate-state`), idempotente. Incluye evidencia: el plan inspeccionado fue ESTRICTAMENTE ADITIVO (0 destroy / 0 replace, F1), y las salidas de `aws s3api head-bucket`, `aws dynamodb describe-table` (LockID/PAY_PER_REQUEST/PITR), `head-object` del state migrado, y `terraform plan -detailed-exitcode` = sin cambios.
   - **Modelo de despliegue (F1/F2):** declara que hay UN solo state `environments/prod` compartido, ADITIVO Y MONÓTONO (sólo se aplica desde la rama apilada más alta con todo el HCL acumulado; nunca reaplicar una rama inferior tras una superior; abortar el apply si el plan muestra cualquier destroy/replace, F1); y que el apply autónomo pre-merge se justifica porque el HCL proviene de esta pila curada y el plan se inspecciona para ser estrictamente aditivo, mientras CI permanece plan-only para cerrar la escalada desde PRs no confiables (F2).
   - **Por qué:** establecer y DEJAR OPERATIVO el backend de estado remoto compartido y el gate de CI de infra sobre el que se apilan PR03 (OIDC + roles) y PR04 (media/eventos/registry/IAM), que aplicarán de forma autónoma contra este mismo state.
   - **Cómo verificar:** los comandos de la sección 8 (fmt -check, validate de módulo y raíz, grep de `LockID`, verificación de plan aditivo F1, head-bucket/describe-table, idempotencia con `plan -detailed-exitcode`), y que CI muestra `terraform` y `gitleaks` en verde. Deja claro que el `apply` lo ejecuta el RUNNER MAD de forma autónoma con credenciales de su entorno tras inspeccionar el plan, y que el CI permanece plan-only (nunca aplica).
   - **Teardown:** existe `make destroy`/`terraform destroy` documentado para desmontar el backend (recursos de bajo costo S3 + DynamoDB on-demand).
   - **Notas:** PR apilado sobre `feat/01-monorepo-v1-skeleton`; mergear bottom-up con merge-commit o rebase-merge (NO squash) para no desincronizar la pila.
   - Termina el cuerpo del PR con la línea: `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.
4. **Devuelve como salida final la URL del PR** (la que imprime `gh pr create`). Si `gh pr create` falla porque la base remota aún no existe, empuja primero la rama base o reporta el bloqueo con detalle; no cambies la base a `main`.

---

### Recordatorios finales (no negociables)
- PROHIBIDO cualquier secreto en el repo (claves AWS, passwords de cámara, tokens). El apply autónomo usa las credenciales del ENTORNO del runner, jamás commiteadas. El código `RWCHBY` está quemado y no debe reaparecer. NO commitees `terraform.tfstate*` ni `.terraform/`.
- **El RUNNER MAD aplica de forma AUTÓNOMA** el backend de estado contra AWS REAL (bootstrap en dos fases, idempotente) y verifica que los recursos EXISTEN de verdad (head-bucket, describe-table, plan sin cambios). SIEMPRE inspecciona el `plan` antes del apply y ABORTA si no es estrictamente aditivo (F1). **El CI de terraform permanece PLAN-ONLY**: NO usa credenciales AWS y NO hace `plan`/`apply` en este PR (en PRs posteriores hará a lo sumo `plan` read-only vía OIDC).
- **F1 (state aditivo y monótono):** hay UN solo state de producción (`environments/prod`) con lock en DynamoDB, compartido por toda la pila de infra. Aplica SÓLO desde la rama apilada más alta con todo el HCL acumulado; NUNCA reaplicar esta rama (la más baja) una vez que PR03/PR04/PR11 hayan aplicado contra el mismo state. Antes de cualquier apply, plan + inspección; abortar ante cualquier destroy/replace.
- **F2 (apply autónomo acotado y justificado):** el runner aplica HCL pre-merge SÓLO porque proviene de esta pila curada y el plan se inspecciona aditivo; no aplica HCL arbitrario de terceros y se limita a los módulos enumerados de este PR. CI plan-only cierra la escalada desde PRs no confiables.
- **F3 (tags unificados):** `default_tags` capitalizados `{Project=cam-counter, ManagedBy=terraform, Env=prod}` + tags lógicos minúscula `project=cam-counter`/`managed_by=mad-runner` en TODOS los recursos. NUNCA `ManagedBy="mad-runner"`; la verificación de `managed_by=mad-runner` busca la clave en minúscula.
- No toques `rtsp-enable/`, el bucket existente de artefactos, ni los archivos del edge movidos en PR01.
- No crees el provider OIDC, los roles IAM, ni los buckets de media/releases: eso es de PRs posteriores (PR03/PR04/PR11).
- PR pequeño-mediano, apilado sobre `feat/01-monorepo-v1-skeleton`, con verificación automática fuerte: la estática corre en x86 CI sin hardware ni credenciales; la de despliegue/idempotencia/plan-aditivo la corre el runner contra AWS REAL.
