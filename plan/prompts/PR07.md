# edge: LineCounter (half-plane + hysteresis + per-track idempotency) + SQLite store with per-camera monotonic crossing_seq

# PR07 — edge: LineCounter (cruce de línea por semiplano + histéresis + idempotencia por track) + store SQLite con crossing_seq monótono por cámara

Eres un agente Claude CLI que trabaja en el repositorio `cam-counter` (GitHub: `jlsaco/cam-counter`, cuenta AWS `950639281773`, región `us-east-1`). Vas a entregar UN solo Pull Request, pequeño-mediano, con verificación automática fuerte que corre en x86 SIN hardware (sin Hailo, sin cámara, sin Raspberry Pi). Trabajas en una pila de PRs apilados: este PR (PR07) ramifica sobre la rama del PR anterior (PR06). Lee TODO este prompt antes de empezar; es autocontenido.

## 1) Contexto

`cam-counter` es un producto edge-first de CONTEO DE PERSONAS que cruzan una LÍNEA-UMBRAL configurable, en tiempo real y en el borde (Raspberry Pi 5 + acelerador Hailo-8 + YOLOv8s, cámara EZVIZ/Hikvision por RTSP). El sistema evoluciona desde un detector monolítico hacia un paquete Python testeable y multi-cámara/multi-sitio. Todo vive en este monorepo.

El pipeline lógico del borde es: `capture -> detect(Hailo) -> track(centroid+IoU) -> count(cruce de línea por semiplano + histéresis + idempotencia por track) -> present(MJPEG) + clip + sink(SQLite)`. Este PR implementa exactamente la etapa `count` (LineCounter) y la persistencia local (`store.py` sobre SQLite).

Estado del repo y de la pila ANTES de este PR (asume que los PRs previos YA están mergeados en la rama base sobre la que ramificas):
- PR01 movió el sistema del borde a `v1/` (con `git mv`, conservando historia) y creó el esqueleto del repo y el directorio `contracts/` con los JSON Schemas de los contratos compartidos.
- PR05 creó el paquete Python del borde en `v1/edge/` con nombre de paquete `cam_counter_edge` (NO uses el nombre genérico `counter`). Contiene al menos: `cam_counter_edge/detector.py` (Detector real Hailo), `cam_counter_edge/dummy.py` (DummyDetector para CI sin hardware), `cam_counter_edge/types.py` (tipos/dataclasses compartidos), validación de slugs, y configuración de `pytest` + `ruff`. Hay un `pyproject.toml`/`setup` que permite `pip install -e v1/edge`.
- PR06 añadió `cam_counter_edge/tracker.py`: un tracker centroid+IoU que asigna `track_id` estables, con la propiedad CRÍTICA de que un `track_id` PUEDE REUSARSE después de que un track se retira por antigüedad (`max_age`) y reaparece otro distinto.

IMPORTANTE sobre la realidad del checkout: si al inspeccionar el repo NO encuentras `v1/edge/cam_counter_edge/` (porque estás partiendo de un estado más temprano del que asume la pila), NO inventes: primero ejecuta `git log --oneline -20` y `ls -R v1/ 2>/dev/null` para entender el estado real, y crea solo lo mínimo que falte para que tu código y tus tests sean coherentes con los contratos descritos aquí (paquete `cam_counter_edge`, tipos mínimos que necesites). NUNCA cambies el nombre del paquete a `counter`. Mantén compatibilidad con los nombres de módulos ya existentes si los hay; reutiliza tipos y validadores en lugar de duplicarlos.

### Convenciones transversales que DEBES respetar (no re-litigar)

- **Coordenadas NORMALIZADAS 0..1** (float) relativas al frame de inferencia ORIGINAL, origen arriba-izquierda. NUNCA píxeles en ningún contrato almacenado. Las cajas del sistema usan el orden `[xmin, ymin, xmax, ymax]` (el Detector ya reordena desde el `[ymin,xmin,ymax,xmax,score]` que entrega el NMS de Hailo). Los centroides de track y los extremos de línea A,B son `(x, y)` normalizados.
- **Identificadores**: `site_id`, `device_id`, `camera_id` son slugs ASCII en minúscula que cumplen el regex `^[a-z0-9][a-z0-9-]{1,62}$` (PROHIBIDOS `#` y `/`, porque `#` delimita claves compuestas de DynamoDB y `/` delimita rutas S3). `camera_id` es globalmente único y vale `'{device_id}-cam{N}'`. NO reconstruyas `device_id` partiendo `camera_id`: trata `device_id` y `camera_id` como campos separados y explícitos. Reutiliza el validador de slug de PR05 si existe; valida ANTES de construir cualquier clave/`event_id`.
- **Signo de dirección del cruce (el contrato compartido MÁS frágil)**: `side(P) = sign(cross(B-A, P-A))`, donde `cross(u,v) = u.x*v.y - u.y*v.x`. Un cruce ocurre cuando el signo de `side` cambia (flip) entre centroides consecutivos del mismo track. Cada cámara tiene un flag `positive_side` (+1 o -1) que selecciona qué flip cuenta como `'in'`. El valor de dirección almacenado/de cable es EXACTAMENTE `'in'` o `'out'`; `'subieron'`/`'bajaron'` son `positive_label`/`negative_label` (etiquetas humanas), y los términos `positive/negative` (UI) o `up/down` (cloud) son SOLO mapeos de presentación, no se almacenan.
- **`event_id` DETERMINISTA**: `event_id = sha1(f"{site_id}|{device_id}|{camera_id}|{track_id}|{crossing_seq}")` en hex minúscula. Es determinista a propósito para que la sincronización a la nube sea idempotente (conditional put en DynamoDB). sha1 aquí es NO criptográfico (solo dedupe); documéntalo en el código (comentario) para que `gitleaks`/security-review no lo marquen.
- **`crossing_seq`**: contador MONÓTONO PERSISTIDO POR CÁMARA (no por track, no reiniciable jamás), vive en SQLite. Cada cruce obtiene un `crossing_seq` único global-por-cámara aunque el `track_id` se reuse. Esto cierra el hueco de idempotencia: dos cruces distintos NUNCA colisionan en `event_id` aunque compartan `track_id`.
- **SQLite es el contrato único de persistencia local**: modo WAL, `busy_timeout`, transacciones cortas, una sola conexión de escritura, migraciones por `PRAGMA user_version`. Tablas relevantes a este PR: `events` (con `UNIQUE(event_id)` y flag `synced`), `counters` (PK lógica `camera_id` + `day_utc` + `direction`), y un mecanismo de `crossing_seq` monótono por cámara (tabla propia o columna dedicada).
- **CrossingEvent** es un contrato compartido. Sus campos canónicos (snake_case, `schema_version=1`) son: `event_id` (string), `site_id`, `device_id`, `camera_id`, `track_id` (string), `direction` (`'in'`|`'out'`), `label` (etiqueta humana resuelta en el momento del evento), `line_version` (int, config en vigor), `ts_event_ms` (int epoch ms UTC, autoritativo), `ts_event_iso` (ISO-8601 UTC), `confidence` (float), `clip_key` (string|null), `clip_status` (`'pending'`|`'uploading'`|`'uploaded'`|`'failed'`), `schema_version` (int), `synced` (0/1, solo local), `created_at`. En este PR los campos de clip pueden nacer como `clip_key=null` y `clip_status='pending'` (el buffer/recorder de clips llega en PR08). Si PR05 ya define un tipo/`dataclass`/Pydantic para CrossingEvent en `cam_counter_edge/types.py` o en `v1/api/schemas.py`, REUTILÍZALO; si no existe en el borde, define una `dataclass` mínima en `types.py` coherente con estos campos (no dupliques si ya existe).
- **Cero secretos en el repo** (regla dura): nada de claves AWS de larga vida, contraseñas de cámara, tokens ni credenciales en código, tests, fixtures ni mensajes de commit. Este PR no necesita ningún secreto.

## 2) Objetivo

Añadir al paquete `cam_counter_edge` dos módulos nuevos, con tests deterministas que corren en x86 sin hardware:

1. **`cam_counter_edge/line_counter.py`** — `LineCounter` por cámara:
   - Configura la línea por dos extremos normalizados `A=(ax,ay)`, `B=(bx,by)` y un `positive_side` (+1/-1), más `site_id`/`device_id`/`camera_id`, `positive_label`/`negative_label`, `line_version`, y parámetros de robustez (`min_frames` de histéresis, `cooldown`).
   - Método `process(tracks, ts_event_ms) -> list[CrossingEvent]` donde `tracks` es la colección de tracks vivos (cada uno con `track_id` y centroide normalizado; usa la forma que exponga el tracker de PR06). Calcula `side(P)=sign(cross(B-A, P-A))` para cada centroide; detecta cruce por flip de signo entre el centroide previo y el actual del mismo track; aplica HISTÉRESIS (el track debe permanecer `min_frames` frames consecutivos en el NUEVO semiplano antes de confirmar el cruce) para que el jitter sobre la línea NO produzca doble conteo; emite EXACTAMENTE UN evento por track por cruce (flag `counted` + `cooldown` para no re-emitir mientras no haya un cruce genuino nuevo).
   - Para cada cruce confirmado: obtiene `crossing_seq` llamando a `store.next_crossing_seq(camera_id)` (monótono persistido), construye el `event_id` determinista, resuelve `direction` (`'in'`/`'out'`) según el signo del flip y `positive_side`, resuelve `label` (positive/negative según corresponda), rellena `ts_event_ms`/`ts_event_iso`/`line_version`/`confidence`/`schema_version`, y devuelve el `CrossingEvent`. El LineCounter NO escribe en la DB directamente más allá de pedir el `next_crossing_seq`; la persistencia del evento la decide el caller (o exponla como helper que llame a `store.insert_event` + `store.bump_counter` de forma idempotente — documenta claramente cuál es el contrato). Mantén la lógica de geometría pura y testeable de forma aislada (función auxiliar `signed_side(A,B,P)` o similar) para poder asertar el signo directamente.

2. **`cam_counter_edge/store.py`** — capa de repositorio sobre SQLite:
   - Apertura con WAL (`PRAGMA journal_mode=WAL`), `PRAGMA busy_timeout`, una conexión de escritura.
   - Migraciones idempotentes vía `PRAGMA user_version` (re-ejecutar `migrate()` no rompe nada ni duplica tablas/datos).
   - Tablas: `events` (columnas = campos de CrossingEvent + `UNIQUE(event_id)`), `counters` (clave lógica `camera_id`/`day_utc`/`direction`, columna `count`), y `crossing_seq` (por cámara, monótono — tabla `crossing_seq(camera_id TEXT PRIMARY KEY, seq INTEGER NOT NULL)` o equivalente).
   - APIs:
     - `insert_event(event) -> bool`: IDEMPOTENTE. Inserta el evento; si `event_id` ya existe (`UNIQUE`), no lanza y devuelve `False` (o señal equivalente clara) sin duplicar; en inserción nueva devuelve `True`. Usa `INSERT OR IGNORE`/`INSERT ... ON CONFLICT DO NOTHING` o captura de `IntegrityError`.
     - `next_crossing_seq(camera_id) -> int`: TRANSACCIONAL y monótono. Crea la fila si no existe partiendo de 0, incrementa atómicamente y devuelve el nuevo valor; NUNCA se reinicia; concurrencia segura para una sola conexión de escritura.
     - `bump_counter(camera_id, day_utc, direction, delta=1)`: upsert transaccional del contador.
     - `get_counters(camera_id, day_utc=None) -> ...`: lectura de contadores.
     - `get_recent_events(camera_id, limit=...) -> list`: eventos recientes ordenados por `ts_event_ms` descendente.
   - Si insertas un evento y bumpeas contador, hazlo de forma que el contador solo suba cuando el evento es genuinamente nuevo (coherente con la idempotencia de `insert_event`), para no inflar contadores en reintentos.

## 3) Alcance y NO-alcance

EN ALCANCE (lo que ESTE PR debe entregar):
- `v1/edge/cam_counter_edge/line_counter.py`
- `v1/edge/cam_counter_edge/store.py`
- Tests: `v1/edge/tests/test_line_counter.py` y `v1/edge/tests/test_store.py`
- Si y solo si falta, adiciones mínimas a `cam_counter_edge/types.py` (campos de CrossingEvent / LineConfig) sin romper imports existentes.

FUERA DE ALCANCE — NO TOCAR:
- `rtsp-enable/` (utilidad Java + box64 que reactiva el RTSP de la cámara). NO toques su lógica ni sus scripts.
- `systemd/hailo-personas.service` y el servicio `hailo-personas`: debe seguir ejecutable; no lo modifiques.
- `detection/` original / el detector monolítico fuera de `v1/` si aún existe; no lo reescribas.
- `cam_counter_edge/detector.py` (Detector Hailo real) y `cam_counter_edge/dummy.py`: úsalos/impórtalos si los necesitas en tests, pero no cambies su comportamiento.
- `cam_counter_edge/tracker.py` de PR06: consúmelo, NO lo modifiques (salvo necesidad imprescindible y mínima; si crees que hace falta, prefiere adaptar tu LineCounter a su API actual).
- Infraestructura Terraform (`terraform/`), buckets S3, DynamoDB, IAM, OIDC, OTA, API/UI (`v1/api`, `v1/ui`), workflows de release/promote: NADA de eso en este PR.
- NO crees recursos AWS. NO ejecutes `terraform apply`. NO subas nada a ningún bucket.
- NO renombres el paquete a `counter`. NO introduzcas dependencias pesadas nuevas; usa la librería estándar (`sqlite3`, `hashlib`, `math`, `datetime`) salvo que el paquete ya dependa de algo equivalente.

## 4) Restricciones

- **Stack**: Python (borde). Tests con `pytest`; linter `ruff`. Sin hardware: todo debe pasar en CI x86 (sin Hailo, sin cámara, sin Pi) usando geometría sintética determinista y una DB SQLite temporal (usa `tmp_path` de pytest o `:memory:` donde aplique; ten en cuenta que `:memory:` no comparte estado entre conexiones, así que para WAL real usa un archivo en `tmp_path`).
- **Edge-first / offline-tolerante**: toda la lógica de conteo y persistencia es 100% local; nada en este PR depende de la nube ni de la red.
- **Multi-cámara**: todo indexado por `site_id`/`device_id`/`camera_id`; `crossing_seq` y `counters` son por cámara. Diseña `store.py` y `LineCounter` para soportar varias cámaras concurrentes sobre la misma DB.
- **Cero secretos en el repo**: PROHIBIDO incluir cualquier secreto (claves AWS, contraseñas de cámara como el código de verificación, tokens) en código, tests, fixtures o mensajes de commit. Este PR no requiere ninguno.
- **Prefijos/identidad AWS** (solo para coherencia conceptual; NO creas recursos aquí): cuenta `950639281773`, región `us-east-1`, prefijo `cam-counter-`. No aplican a este PR salvo para no introducir nombres en conflicto.
- **Determinismo**: los tests no deben depender del reloj real para asertar conteos; pasa `ts_event_ms` explícito a `process(...)`. Donde uses `ts_event_iso`, derívalo de `ts_event_ms` de forma determinista (UTC).

## 5) Rama base y rama nueva

- **Rama base** (sobre la que ramificas y a la que apuntará el PR): la rama del PR anterior, `feat/06-edge-tracker`. Si esa rama no existe en el remoto (porque la pila aún no la publicó), haz `git fetch origin` y verifica; si tras el fetch sigue sin existir, RAMIFICA SOBRE `main` y deja constancia explícita en el cuerpo del PR de que la base real fue `main` por ausencia de `feat/06-edge-tracker`. Indica también en tu salida final cuál fue la base efectiva.
- **Rama nueva a crear**: `feat/07-edge-linecounter-store`.

Comandos sugeridos para preparar las ramas:
```
git fetch origin
# base preferida:
git checkout feat/06-edge-tracker 2>/dev/null || git checkout main
git pull --ff-only 2>/dev/null || true
git checkout -b feat/07-edge-linecounter-store
```

## 6) Pasos sugeridos

1. Inspecciona el estado real: `git log --oneline -20`, `ls -R v1/edge 2>/dev/null`, lee `v1/edge/cam_counter_edge/types.py`, `tracker.py`, `detector.py`, `dummy.py` y el `pyproject.toml` para conocer la API exacta del tracker (cómo expone `track_id` y el centroide) y los tipos ya definidos. Lee también `contracts/` para alinear el contrato CrossingEvent y `v1/api/schemas.py` si existe.
2. Si falta el validador de slug, localízalo (PR05) y reúsalo; si no existe, añade uno mínimo coherente con el regex y úsalo en `LineCounter`/`store` antes de construir `event_id`/claves.
3. Implementa `store.py` primero (WAL, migraciones por `user_version`, `next_crossing_seq`, `insert_event` idempotente, `bump_counter`, `get_counters`, `get_recent_events`). Asegura que `migrate()` es idempotente.
4. Implementa `line_counter.py`: función pura `signed_side(A,B,P)`, máquina de estado por track (signo previo, frames en el nuevo lado, flag `counted`, `cooldown`), emisión de un único `CrossingEvent` por cruce con `event_id` determinista usando `store.next_crossing_seq`.
5. Escribe los tests (sección 7/8). Cúbrelos exactamente.
6. Corre `ruff` y `pytest` localmente hasta verde. Limpia, sin código muerto. Comentario explícito de "sha1 no criptográfico (dedupe only)".
7. Commit(s) atómicos con mensaje claro. Recuerda el trailer de co-autoría exigido por el repo en el commit:
   ```
   Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
   ```

## 7) Definition of Done (objetivo y verificable)

- `LineCounter` emite EXACTAMENTE UN `CrossingEvent` por track por cruce, con `direction` `'in'`/`'out'` correcta derivada de `positive_side`; el `event_id` es determinista según la fórmula `sha1(site|device|camera|track|crossing_seq)` con `crossing_seq` proveniente del contador monótono por cámara de `store`.
- El jitter de un centroide oscilando SOBRE la línea produce como máximo UN evento (la histéresis `min_frames` lo garantiza); existe un test que lo demuestra.
- `store.py` usa WAL, migraciones idempotentes vía `user_version`, `insert_event` idempotente (segunda inserción del mismo `event_id` no duplica ni lanza), `bump_counter` transaccional, y `crossing_seq` monótono persistido por cámara (nunca reiniciado).
- Existe un test que alimenta geometría conocida y asERTA que el signo del producto cruzado mapea a la dirección esperada (test de convención de signo — el contrato más frágil).
- Existe un test donde un track se retira y un NUEVO track REAPARECE con el MISMO `track_id`, produciendo DOS cruces -> DOS `event_id` DISTINTOS (sin colisión), gracias al `crossing_seq` monótono por cámara.
- Existen tests de: cruces en ambos sentidos (`'in'` y `'out'`); dedupe por `event_id` (insertar el mismo evento dos veces deja un solo registro); idempotencia de la migración.
- Todo corre en x86 sin hardware. `ruff` limpio en los dos módulos nuevos. Ningún secreto añadido. No se modificaron archivos fuera de alcance.

## 8) Verificación (comandos concretos a ejecutar y dejar en verde)

Ejecuta y deja pasando, desde la raíz del repo (instala el paquete editable si hace falta: `pip install -e v1/edge`):

```
cd v1/edge && python3 -m pytest -q tests/test_line_counter.py tests/test_store.py
cd v1/edge && python3 -m pytest -q -k 'idempot or dedup or sign or jitter or seq_collision or reappear'
cd v1/edge && ruff check cam_counter_edge/line_counter.py cam_counter_edge/store.py
```

Requisitos sobre los nombres de los tests: nombra las funciones de test de modo que el filtro `-k 'idempot or dedup or sign or jitter or seq_collision or reappear'` seleccione, como mínimo: el test de idempotencia (migración e `insert_event`), el de dedupe por `event_id`, el de convención de signo, el de jitter, y el de track reusado/reaparecido que produce dos `event_id` distintos (usa `seq_collision` o `reappear` en su nombre). Confirma que ese filtro selecciona y pasa esos tests (no 0 seleccionados).

Antes de abrir el PR, asegúrate además de que:
```
git status            # solo los archivos en alcance modificados
git diff --stat       # diff limpio, revisable de abajo a arriba
```

## 9) Entrega: abrir el Pull Request

1. Haz commit de tus cambios en la rama `feat/07-edge-linecounter-store` (con el trailer de co-autoría) y `git push -u origin feat/07-edge-linecounter-store`.
2. Abre el PR con la GitHub CLI, con **base = la rama del PR anterior** para que el diff quede apilado y limpio:
   ```
   gh pr create \
     --base feat/06-edge-tracker \
     --head feat/07-edge-linecounter-store \
     --title "PR07 — edge: LineCounter (semiplano + histéresis + idempotencia por track) + store SQLite con crossing_seq monótono por cámara" \
     --body "<cuerpo>"
   ```
   Si `feat/06-edge-tracker` no existe como base válida en el remoto, usa `--base main` y DÉJALO EXPLÍCITO en el cuerpo.
3. El cuerpo del PR debe explicar, claro y conciso:
   - **Qué**: nuevos módulos `line_counter.py` y `store.py` + tests; en una frase cada API pública.
   - **Por qué**: cierra la etapa `count` del pipeline edge-first; el `crossing_seq` monótono por cámara garantiza `event_id` único e idempotencia de sync aun con `track_id` reusado (corrige el hueco de idempotencia identificado en auditoría).
   - **Cómo verificar**: pega los tres comandos de la sección 8 y resume qué cubre cada test (signo, jitter, dedupe, idempotencia migración, reaparición de track -> dos event_id distintos, ambos sentidos).
   - **Alcance/NO-alcance**: confirma que no se tocó `rtsp-enable/`, `systemd/`, infra ni API/UI, y que no se añadieron secretos.
   - Trailer al final del cuerpo:
     ```
     🤖 Generated with [Claude Code](https://claude.com/claude-code)
     ```
4. **DEVUELVE como salida final la URL del PR** creado (la que imprime `gh pr create`). Si algo impide crear el PR, devuelve el error exacto y el estado de `git status`/`git log --oneline -5`.

## Recordatorio final (no negociable)
- Cero secretos en el repo (código, tests, fixtures, mensajes de commit). Está PROHIBIDO incluir contraseñas de cámara, claves AWS de larga vida o tokens.
- No modifiques archivos fuera de alcance. No crees recursos en la nube.
- El PR debe ser pequeño-mediano y completamente verificable en x86 sin hardware.
