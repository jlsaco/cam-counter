# infra(PR03): OIDC provider + roles GHA plan/deploy separados (apply autónomo del runner) + plan read-only en CI

# infra: GitHub Actions OIDC provider + roles plan/deploy SEPARADOS (apply AUTÓNOMO del runner) + wire OIDC read-only plan en CI

# PR03 — Infra: GitHub Actions OIDC provider + roles plan/deploy SEPARADOS + apply autónomo del runner + plan read-only en CI

Eres un agente Claude CLI ejecutándose en un runner MAD con credenciales AWS amplias en su ENTORNO (jamás commiteadas) y acceso a GitHub (gh CLI autenticado). Trabajas en el monorepo `cam-counter`. Este documento es TU ÚNICA fuente de contexto: es totalmente autocontenido. Ejecútalo tal cual, sin pedir aclaraciones.

DOS ACTORES DISTINTOS — no los confundas nunca:
- **RUNNER MAD (tú)**: tienes credenciales AWS amplias en tu entorno. ERES QUIEN EJECUTA `terraform apply` de forma AUTÓNOMA dentro de este PR, contra la cuenta REAL `950639281773` / `us-east-1`. Las credenciales vienen del ENTORNO del runner, NUNCA del repo.
- **GitHub Actions CI**: permanece SOLO-PLAN (fmt/validate/plan read-only vía OIDC). NUNCA hace `apply` (evita escalada de privilegios desde PRs).

---

## 1) Contexto

`cam-counter` (repo GitHub `jlsaco/cam-counter`, https://github.com/jlsaco/cam-counter) está evolucionando de un detector de personas (Raspberry Pi 5 + Hailo-8 + cámara EZVIZ/Hikvison por RTSP) a un PRODUCTO de conteo de personas edge-first, multi-cámara/multi-sitio, con UI LAN y flota actualizable por OTA. Todo vive en este monorepo.

La infraestructura cloud se gestiona con Terraform en `terraform/`, módulos por servicio bajo `terraform/modules/<servicio>/`, una sola raíz "live" en `terraform/environments/prod/`, estado remoto en S3 + lock en DynamoDB. Cuenta AWS `950639281773`, región `us-east-1`, prefijo de nombres `cam-counter-`.

Este PR (PR03) es el TERCERO de una pila de PRs apilados. Los anteriores YA están mergeados y APLICADOS (sus recursos existen de verdad en AWS) en la rama base sobre la que ramificas:
- PR00: bootstrap (`CLAUDE.md`, `.claude/`, scripts de verificación de toolchain/identidad). Aquí se documentaron las INVARIANTES TRANSVERSALES de la pila (estado aditivo/monótono, apply autónomo acotado, esquema de tags unificado) que este PR DEBE respetar.
- PR01: el sistema de borde se movió a `v1/`; se creó `contracts/`, `scripts/version.py` (SemVer derivado de tags git, seguro sin tags) y el esqueleto del repo.
- PR02: backend de estado remoto de Terraform (bucket S3 de tfstate + tabla DynamoDB de lock con PK exactamente `LockID`), APLICADO de forma AUTÓNOMA por el runner (bootstrap con backend local + `terraform init -migrate-state` al backend S3), layout de módulos/entorno y un workflow de CI que hace `terraform fmt -check` y `terraform validate` (+ plan read-only). Aquí se definió el proveedor AWS pineado `~> 5.x` con `default_tags` CAPITALIZADOS `{ Project = "cam-counter", ManagedBy = "terraform", Env = "prod" }` (OJO: `ManagedBy = "terraform"`, NUNCA `ManagedBy = "mad-runner"`) y, ADEMÁS, los tags lógicos en MINÚSCULA `project = "cam-counter"` y `managed_by = "mad-runner"` aplicados a TODOS los recursos. El estado remoto compartido ya quedó migrado a S3.

ESTADO REMOTO COMPARTIDO: hay UN solo state de producción (`terraform/environments/prod`), con lock en DynamoDB. TODOS los applies de la pila comparten ese state. Tu apply en este PR escribe sobre ese mismo state remoto.

### Invariantes transversales OBLIGATORIAS (de la auditoría; documentadas en PR00 en `CLAUDE.md`/`.claude/README.md`; este PR las CUMPLE)
- **F1 — STATE ADITIVO Y MONÓTONO**: el state `environments/prod` es compartido por toda la pila apilada. El runner SOLO debe ejecutar `terraform apply` desde la rama apilada MÁS ALTA que contiene TODO el HCL acumulado hasta ese punto; NUNCA reaplicar una rama inferior después de que una superior ya aplicó (reaplicar la rama de PR03 después de que PR04 haya aplicado DESTRUIRÍA los recursos de PR04 que no están en la config de PR03). ANTES de cualquier apply, ejecuta `terraform plan` e INSPECCIÓNALO: si aparece CUALQUIER `destroy` o `replace` de un recurso YA existente, ABORTA el apply. El plan de este PR debe ser ESTRICTAMENTE ADITIVO (solo "to add": el proveedor OIDC — si no existía ya — y los dos roles + sus políticas).
- **F2 — APPLY AUTÓNOMO ACOTADO Y JUSTIFICADO**: el runner aplica HCL ANTES del merge humano. Esto se justifica EXCLUSIVAMENTE porque el HCL proviene de esta pila controlada y curada (los módulos enumerados en este PR, nada más) y porque el plan se inspecciona para ser estrictamente aditivo (F1). El runner NO aplica HCL arbitrario de terceros. GitHub Actions CI permanece plan-only, cerrando la escalada desde PRs no confiables. Trade-off aceptado y declarado: el apply autónomo del runner reintroduce la materialización en AWS de código aún no mergeado por un humano; se acota a (a) HCL curado de esta pila, (b) inspección de plan aditivo, (c) CI plan-only para PRs no confiables.
- **F3 — TAGS UNIFICADOS**: `default_tags` capitalizados `{ Project = "cam-counter", ManagedBy = "terraform", Env = "prod" }` MÁS tags lógicos en MINÚSCULA `project = "cam-counter"` y `managed_by = "mad-runner"` en TODOS los recursos. La verificación de `managed_by=mad-runner` busca la clave en MINÚSCULA. NUNCA uses `ManagedBy = "mad-runner"` (capitalizado): la clave capitalizada `ManagedBy` vale `terraform`.

IMPORTANTE: ANTES de empezar, INSPECCIONA el árbol real para conocer los nombres/rutas exactos que dejaron PR00–PR02 (no asumas; verifica):
- `ls -la terraform/ terraform/modules/ terraform/environments/prod/ .github/workflows/ scripts/`
- Lee la configuración del backend remoto y del provider que dejó PR02 (típicamente `terraform/environments/prod/backend.tf`, `providers.tf`, `versions.tf` o equivalentes). Anota el NOMBRE EXACTO del bucket de tfstate y de la tabla DynamoDB de lock; los necesitarás para la política read-only del rol de plan y para inicializar el backend remoto en tu apply. CONFIRMA que los `default_tags` de PR02 son `{ Project = "cam-counter", ManagedBy = "terraform", Env = "prod" }` y que existen los tags lógicos en minúscula `project`/`managed_by`; si PR02 los dejó de otra forma, ADÁPTATE a lo que existe pero respeta F3 al añadir tus recursos.
- Revisa cómo PR02 estructuró su workflow de CI de Terraform (nombre del archivo en `.github/workflows/`, triggers, path filters) para EXTENDERLO de forma coherente, no duplicarlo.
- Verifica tu identidad/credenciales AWS del runner antes de aplicar: `aws sts get-caller-identity` debe devolver la cuenta `950639281773`.

Si algún nombre/ruta difiere de lo descrito, ADÁPTATE a lo que existe en el repo; el repo es la verdad. El backend remoto YA debe existir (lo aplicó PR02); si por orden de ejecución no existiera, primero asegúrate de que el state backend esté aplicado (PR02 es dependencia dura de este PR).

---

## 2) Objetivo

Crear un nuevo módulo Terraform `terraform/modules/iam-github-oidc/`, cablearlo en la raíz `prod`, **aplicarlo de forma AUTÓNOMA con el runner contra AWS REAL**, y dejar el plan read-only de CI por OIDC. El módulo provisiona:

1. El **proveedor OIDC** de GitHub Actions: `token.actions.githubusercontent.com` (un único `aws_iam_openid_connect_provider`; si tu organización ya lo tuviera creado fuera de Terraform, el módulo debe poder consumirlo vía variable booleana `create_oidc_provider` con `data` source de respaldo — ver Pasos). El apply debe ser idempotente: si el proveedor ya existe, no falla (impórtalo al state O fija `create_oidc_provider=false` en el HCL/tfvars commiteado — ver Paso 7; la decisión DEBE ser PERSISTENTE para que applies posteriores de la pila no reintenten crearlo).
2. **DOS roles IAM SEPARADOS** (principio de privilegio mínimo, separación plan vs apply):
   - `cam-counter-gha-plan` — usado por jobs de CI en Pull Requests (y plan en `main`). Trust: `aud = sts.amazonaws.com` y `sub` `StringLike`/`StringEquals` restringido a `repo:jlsaco/cam-counter:pull_request` y `repo:jlsaco/cam-counter:ref:refs/heads/main`. Política **SOLO LECTURA**: lectura de recursos para `terraform plan` + acceso al estado remoto (s3:GetObject/ListBucket sobre el bucket de tfstate y su prefijo, y dynamodb:GetItem/PutItem/DeleteItem sobre la tabla de lock SOLO para poder adquirir/soltar el lock del plan — el lock requiere escritura en esa tabla; ese es el único permiso de escritura permitido para el rol de plan, y está acotado a la tabla de lock por ARN). NUNCA debe poder hacer apply de recursos.
   - `cam-counter-gha-deploy` — rol de apply asumible SOLO desde GitHub Environment `prod`/`main`/tags. Trust: `aud = sts.amazonaws.com` y `sub` restringido a `repo:jlsaco/cam-counter:environment:prod` y/o `repo:jlsaco/cam-counter:ref:refs/heads/main` y tags `repo:jlsaco/cam-counter:ref:refs/tags/*`. Política con permisos de **apply** (capaz de crear/modificar los recursos del producto). NUNCA debe ser asumible desde el contexto `pull_request`. NOTA: en este modelo CI sigue SIN hacer apply de infra; el `cam-counter-gha-deploy` queda creado para uso futuro/operación (p.ej. workflows de release/promote que publican OBJETOS S3, lo cual NO es apply de infraestructura), pero quien aplica la infra de esta pila es el RUNNER MAD con las credenciales de su entorno (no este rol, no GitHub Actions).
3. Cablear el módulo en la raíz `terraform/environments/prod/` y exponer outputs `gha_plan_role_arn` y `gha_deploy_role_arn` (además de `oidc_provider_arn`).
4. Añadir/extender el workflow de CI para que el job de **plan** (en PRs que tocan `terraform/`) asuma el rol **plan** vía OIDC con `aws-actions/configure-aws-credentials`, ejecute un `terraform plan` de SOLO LECTURA y publique/adjunte el plan. El `apply` NUNCA se ejecuta desde GitHub Actions (ni en PR ni en push ni en tag).
5. **APLICAR de forma AUTÓNOMA** con el runner: `terraform init` (backend remoto compartido) + `validate` + `plan` (INSPECCIONADO para ser estrictamente aditivo, F1) + `terraform apply -auto-approve`, contra la cuenta REAL `950639281773`/`us-east-1`. Verificar que el OIDC provider y AMBOS roles existen de verdad en IAM. El apply debe ser idempotente (un segundo apply = "0 to add, 0 to change, 0 to destroy").
6. Etiquetar TODOS los recursos según F3: `default_tags` capitalizados heredados de la raíz prod `{ Project = "cam-counter", ManagedBy = "terraform", Env = "prod" }` MÁS los tags lógicos en MINÚSCULA `project = "cam-counter"` y `managed_by = "mad-runner"` (vía `var.tags` y/o merge en el módulo).
7. Tras el apply, registrar las **VARIABLES de repositorio** (`vars.*`) con los ARNs REALES ya creados (NO secrets, NO credenciales estáticas).
8. Documentar EXACTAMENTE qué claim `sub` emite GitHub en cada contexto (`pull_request` / push a `main` / tag / `environment:prod`) y mapear cada contexto a su rol.

---

## 3) Alcance y NO-alcance

### En alcance
- Nuevo módulo `terraform/modules/iam-github-oidc/` (`main.tf`, `variables.tf`, `outputs.tf`, `versions.tf`, `README.md`).
- Wiring del módulo en `terraform/environments/prod/` + outputs `gha_plan_role_arn`, `gha_deploy_role_arn`, `oidc_provider_arn`.
- **Apply AUTÓNOMO del runner** del proveedor OIDC + ambos roles contra AWS REAL, con inspección de plan aditivo (F1), verificación de recursos reales en IAM e idempotencia.
- Workflow de CI de Terraform (extender el de PR02 o añadir uno nuevo coherente) con el job de plan que asume el rol plan vía OIDC (CI sigue plan-only).
- Registro de las repository **variables** `GHA_PLAN_ROLE_ARN`, `GHA_DEPLOY_ROLE_ARN` con los ARNs REALES tras el apply (`gh variable set`), más `AWS_REGION`, `AWS_ACCOUNT_ID` si aún no existen.
- Documentación del mapeo `sub`→rol (en el `README.md` del módulo y/o en `CLAUDE.md` o `docs/`, según donde PR00/PR01 hayan ubicado la doc de infra; sé coherente con lo existente) y del comando de teardown (`terraform destroy`).

### NO-alcance (NO tocar)
- NO crear las tablas DynamoDB de producto (events, devices), NI el bucket de media, NI el bucket de releases, NI el rol per-Pi (eso es PR04 y PR11).
- NO modificar `rtsp-enable/` ni su lógica.
- NO modificar el sistema de borde en `v1/` (código Python del detector, systemd, etc.).
- NO hacer que GitHub Actions ejecute `apply` de infra. CI sigue SOLO-PLAN (fmt/validate/plan read-only por OIDC). El `apply` lo hace el RUNNER MAD de forma autónoma con las credenciales de su entorno.
- NO tocar el bucket existente `cam-counter-rpi-artifacts-950639281773` (backup de binarios; reservado).
- NO cambiar el provider/versión pineados ni los `default_tags` que dejó PR02; reutilízalos (asegúrate de que cumplan F3: capitalizados `ManagedBy=terraform` + lógicos minúscula `project=cam-counter` / `managed_by=mad-runner`).
- NO crear tags git ni promover versiones.
- NO commitear NINGUNA credencial AWS: el apply usa las credenciales del ENTORNO del runner, jamás del repo.

---

## 4) Restricciones

- **Stack infra**: Terraform, provider AWS pineado `~> 5.x`. Módulos en `terraform/modules/<servicio>/`, raíz live única en `terraform/environments/prod/`. Estado remoto S3 + lock DynamoDB COMPARTIDO (un solo state de prod). Tags F3: `default_tags { Project = "cam-counter", ManagedBy = "terraform", Env = "prod" }` MÁS lógicos minúscula `project = "cam-counter"` / `managed_by = "mad-runner"`. Prefijo `cam-counter-`. Cuenta `950639281773`. Región `us-east-1`. Tablas (cuando apliquen, no aquí) `PAY_PER_REQUEST` con PITR. Directory-per-env (no workspaces).
- **F1 (state aditivo y monótono)**: aplica SOLO desde esta rama apilada (que contiene PR02+PR03); NUNCA reapliques una rama inferior tras haber aplicado una superior. Inspecciona el plan y ABORTA si aparece cualquier `destroy`/`replace` de un recurso existente.
- **F2 (apply autónomo acotado)**: el runner aplica HCL curado de esta pila antes del merge humano; CI permanece plan-only. Declarado y aceptado el trade-off (ver §1).
- **CERO secretos en el repo**: NINGUNA clave AWS de larga vida en git. El apply autónomo usa las credenciales del ENTORNO del runner (jamás commiteadas). Las GitHub Actions asumen AWS por OIDC (solo plan). ARNs/región/cuenta van por **VARIABLES de repositorio** (`vars.*`), no secrets. El trust de OIDC se acota a `repo:jlsaco/cam-counter` con condiciones por `sub` y `aud=sts.amazonaws.com` (NUNCA wildcard de repo). Si introduces un patrón que parezca clave estática, el PR se rechaza.
- **Separación de privilegios (CRÍTICO)**: el rol de plan NO debe poder hacer apply; el rol de deploy NO debe ser asumible desde `pull_request`. Un PR malicioso no debe poder invocar apply. CI nunca hace apply de infra (solo plan-only por OIDC).
- **`configure-aws-credentials` por OIDC (solo CI)**: usa permisos de job `permissions: { id-token: write, contents: read }` y `pull-requests: write` solo si publicas el plan como comentario. Fija `role-to-assume: ${{ vars.GHA_PLAN_ROLE_ARN }}`, `aws-region: ${{ vars.AWS_REGION }}`. Nada de `aws-access-key-id`/`aws-secret-access-key`.
- **Apply autónomo idempotente y PERSISTENTE**: el primer apply crea los recursos; un segundo apply NO debe cambiar nada ("0 to add, 0 to change, 0 to destroy"). Si el OIDC provider ya existía fuera de Terraform, gestiona el caso de forma PERSISTENTE (import al state compartido, o `create_oidc_provider=false` FIJADO en el HCL/tfvars commiteado — NUNCA solo en un `-var` efímero de CLI) para que el SIGUIENTE plan/apply de la pila (p.ej. desde PR04, que reaplica el root con este módulo) siga dando 0 cambios.
- **Edge-first / multi-cámara**: no aplica directamente a este PR, pero no introduzcas nada que rompa esos principios.

---

## 5) Rama base y rama nueva

- **Rama base** (de la que ramificas y contra la que abrirás el PR): `feat/02-tf-state-backend` (la rama del PR02, del que depende este PR; su state backend YA debe estar aplicado).
- **Rama nueva a crear**: `feat/03-iam-github-oidc`.

Comandos:
```bash
git fetch origin
git checkout feat/02-tf-state-backend || git checkout -b feat/02-tf-state-backend origin/feat/02-tf-state-backend
git pull --ff-only origin feat/02-tf-state-backend || true
git checkout -b feat/03-iam-github-oidc
```
Si `feat/02-tf-state-backend` no existiera en remoto (orden de merge), usa como base la rama apilada más reciente disponible o `main`, y abre el PR con `--base` apuntando a esa misma rama. Documenta en el cuerpo del PR qué base usaste y por qué. RECUERDA F1: solo aplicarás desde esta rama (PR03), que es por ahora la MÁS ALTA de la pila; cuando exista PR04 (rama superior), NO reapliques esta rama PR03.

---

## 6) Pasos sugeridos

1. **Inspecciona** el árbol existente (sección 1) y anota nombres exactos: bucket tfstate, tabla de lock DynamoDB, ruta de la raíz prod, nombre del workflow de CI de PR02, ARN cuenta `950639281773`/región `us-east-1`, y el ESQUEMA de tags real (debe ser F3: `ManagedBy=terraform` capitalizado + `managed_by=mad-runner` minúscula). Confirma tu identidad AWS del runner: `aws sts get-caller-identity` (debe ser la cuenta `950639281773`).

2. **Crea `terraform/modules/iam-github-oidc/`**:
   - `versions.tf`: `terraform { required_version = ">= 1.5.0" }` y `required_providers { aws = { source = "hashicorp/aws", version = "~> 5.0" } }` (coherente con PR02). Si usas `data "tls_certificate"` para el thumbprint, declara también el provider `tls`.
   - `variables.tf`:
     - `github_org` (default `"jlsaco"`), `github_repo` (default `"cam-counter"`).
     - `create_oidc_provider` (bool, default `true`).
     - `oidc_provider_arn` (string, default `""`) para el caso en que ya exista el proveedor.
     - `tfstate_bucket_name` (string) y `tfstate_lock_table_name` (string) — para acotar la política read-only del rol plan al estado y al lock reales.
     - `plan_role_name` (default `"cam-counter-gha-plan"`), `deploy_role_name` (default `"cam-counter-gha-deploy"`).
     - `tags` (map, default `{}`) — para inyectar los tags lógicos minúscula `project`/`managed_by` (F3) si no bastan los `default_tags`.
   - `main.tf`:
     - `aws_iam_openid_connect_provider` para `https://token.actions.githubusercontent.com` con `client_id_list = ["sts.amazonaws.com"]` y los thumbprints adecuados (incluye `data "tls_certificate"` para derivar thumbprint, o usa el thumbprint conocido de GitHub; documenta la elección). Crea condicionalmente con `count = var.create_oidc_provider ? 1 : 0`; un `local.oidc_provider_arn` que resuelva a la creada o a la pasada por variable.
     - **Trust del rol PLAN** (`aws_iam_policy_document` con `Action = sts:AssumeRoleWithWebIdentity`, `Principal Federated = local.oidc_provider_arn`):
       - `condition StringEquals token.actions.githubusercontent.com:aud = "sts.amazonaws.com"`.
       - `condition StringLike token.actions.githubusercontent.com:sub` con valores `repo:jlsaco/cam-counter:pull_request` y `repo:jlsaco/cam-counter:ref:refs/heads/main`.
     - **Política PLAN (solo lectura)**: permisos de lectura/describe necesarios para `terraform plan` de los recursos del producto (p.ej. `s3:Get*`, `s3:List*`, `dynamodb:Describe*`, `dynamodb:List*`, `iam:Get*`, `iam:List*`, etc., acotado y razonado) MÁS acceso al estado remoto: `s3:GetObject`/`s3:ListBucket` sobre `arn:aws:s3:::<tfstate_bucket>` y `.../*`, y `dynamodb:GetItem`/`PutItem`/`DeleteItem`/`DescribeTable` SOLO sobre `arn:aws:dynamodb:us-east-1:950639281773:table/<lock_table>` (escritura mínima imprescindible para el lock del plan). NO incluyas acciones de creación/modificación/borrado de recursos de producto.
     - **Trust del rol DEPLOY**: igual estructura pero `sub` `StringLike` solo en `repo:jlsaco/cam-counter:environment:prod`, `repo:jlsaco/cam-counter:ref:refs/heads/main` y `repo:jlsaco/cam-counter:ref:refs/tags/*`. NUNCA `pull_request`.
     - **Política DEPLOY**: permisos suficientes para `apply` de los recursos del producto + estado remoto completo (lectura/escritura del state y del lock). Puede ser más amplia, pero mantén el espíritu de mínimo privilegio acotado al prefijo/recursos `cam-counter-` donde sea viable.
     - **Tags (F3)**: aplica a TODOS los recursos los tags lógicos minúscula `project = "cam-counter"` y `managed_by = "mad-runner"` (merge de `var.tags` con los anteriores). NO confíes solo en `default_tags`: garantiza que la clave en MINÚSCULA `managed_by` con valor `mad-runner` esté presente. NUNCA `ManagedBy = "mad-runner"` (la clave capitalizada `ManagedBy` viene de `default_tags` y vale `terraform`).
   - `outputs.tf`: `oidc_provider_arn`, `plan_role_arn`, `deploy_role_arn`.
   - `README.md`: tabla del mapeo `sub`→rol (ver Paso 5) + decisiones (thumbprint, escritura mínima del lock para plan, cómo se resolvió de forma PERSISTENTE el huevo-y-gallina del proveedor OIDC) + nota de teardown (`terraform destroy`) + nota de que CI es plan-only y el apply lo hace el runner MAD de forma autónoma (F2) + nota de que el state es aditivo/monótono (F1) + nota de tags F3.

3. **Wiring en `terraform/environments/prod/`**: añade un bloque `module "iam_github_oidc" { source = "../../modules/iam-github-oidc" ... }` pasando `tfstate_bucket_name` y `tfstate_lock_table_name` reales (referénciales desde los recursos/locals de PR02 si están en la misma raíz, o como literales documentados), y pasando `tags = { project = "cam-counter", managed_by = "mad-runner" }` (F3). Añade a los outputs de la raíz prod:
   ```hcl
   output "oidc_provider_arn"   { value = module.iam_github_oidc.oidc_provider_arn }
   output "gha_plan_role_arn"   { value = module.iam_github_oidc.plan_role_arn }
   output "gha_deploy_role_arn" { value = module.iam_github_oidc.deploy_role_arn }
   ```

4. **CI (plan-only, sin cambios respecto al modelo OIDC)**: extiende el workflow de Terraform de PR02 (o crea `.github/workflows/terraform-plan.yml` coherente). El job de **plan** debe:
   - Disparar en `pull_request` con `paths: ['terraform/**', '.github/workflows/terraform*.yml']`.
   - `permissions: { id-token: write, contents: read, pull-requests: write }`.
   - `actions/checkout@v4` (con `fetch-depth: 0` si necesitas `git describe`).
   - `aws-actions/configure-aws-credentials@v4` con `role-to-assume: ${{ vars.GHA_PLAN_ROLE_ARN }}`, `aws-region: ${{ vars.AWS_REGION }}`, `role-session-name: cam-counter-gha-plan`. SIN claves estáticas.
   - `terraform init` (con backend remoto) + `terraform plan -no-color -input=false` (SOLO plan; jamás `apply`). Opcional: publicar el plan como comentario del PR.
   - Asegúrate de que NINGÚN workflow de GitHub Actions ejecute `terraform apply` (ni en PR, ni en push, ni en tag). El `cam-counter-gha-deploy` queda CREADO para operación futura, pero en este modelo el apply de la pila lo realiza el RUNNER MAD con las credenciales de su entorno, NO GitHub Actions. Deja claro en el README que CI es plan-only por diseño (F2).

5. **Documentación del mapeo `sub`→rol** (en `README.md` del módulo): tabla EXACTA. GitHub emite el claim `sub` así:
   - Pull request: `repo:jlsaco/cam-counter:pull_request` → **rol PLAN**.
   - Push a `main`: `repo:jlsaco/cam-counter:ref:refs/heads/main` → **rol PLAN** (plan-on-main).
   - Tag `vX.Y.Z`: `repo:jlsaco/cam-counter:ref:refs/tags/vX.Y.Z` → **rol DEPLOY** (uso operativo futuro).
   - Job con `environment: prod`: `repo:jlsaco/cam-counter:environment:prod` → **rol DEPLOY** (uso operativo futuro).
   Incluye nota: `aud` siempre `sts.amazonaws.com`. Nunca wildcard de repo (`repo:*`). Incluye nota: CI solo usa el rol PLAN (plan-only); el apply autónomo de la pila lo hace el runner MAD por entorno (F2). Aclara que los workflows futuros de release/promote (PR11) que escriben OBJETOS S3 con el rol deploy NO son `terraform apply` de infra y por tanto NO violan el "CI plan-only".

6. **Formatea y valida en local (offline-safe)** antes de aplicar:
   ```bash
   terraform fmt -recursive terraform/
   terraform -chdir=terraform/modules/iam-github-oidc init -backend=false
   terraform -chdir=terraform/modules/iam-github-oidc validate
   ```
   Corrige hasta que pase.

7. **APPLY AUTÓNOMO con el runner contra AWS REAL** (este es el corazón del nuevo requisito). Desde la raíz prod, usando las credenciales del ENTORNO del runner (verifica `aws sts get-caller-identity` = `950639281773`):
   ```bash
   terraform -chdir=terraform/environments/prod init        # backend remoto S3 compartido (el de PR02)
   terraform -chdir=terraform/environments/prod validate
   terraform -chdir=terraform/environments/prod plan -input=false -out=tfplan.pr03
   ```
   - **GUARD F1 (OBLIGATORIO antes de aplicar)**: INSPECCIONA el plan (`terraform -chdir=terraform/environments/prod show tfplan.pr03` o el resumen del comando anterior). El plan debe ser ESTRICTAMENTE ADITIVO: solo "X to add, 0 to change, 0 to destroy" (el proveedor OIDC si no existía + los dos roles + políticas). Si aparece CUALQUIER `destroy` o `replace` de un recurso ya existente (p.ej. del state backend de PR02), ABORTA el apply, NO continúes, e investiga (probable señal de estar aplicando desde una rama equivocada o de un drift). Solo si el plan es aditivo, procede:
   ```bash
   terraform -chdir=terraform/environments/prod apply -input=false -auto-approve tfplan.pr03
   ```
   - Caso huevo-y-gallina del OIDC provider: si AWS YA tuviera `arn:aws:iam::950639281773:oidc-provider/token.actions.githubusercontent.com`, el apply fallaría por EntityAlreadyExists. Resuélvelo de forma AUTÓNOMA, idempotente y **PERSISTENTE** (no un `-var` efímero de CLI):
     - PREFERIDO: `terraform import` del proveedor existente al recurso del módulo (`aws_iam_openid_connect_provider.this[0]`), dejando `create_oidc_provider=true` (default). Así el default converge sin recrear en applies posteriores de la pila.
     - ALTERNATIVA: FIJAR `create_oidc_provider=false` y `oidc_provider_arn` del existente en el HCL/tfvars COMMITEADO del root prod (p.ej. `terraform/environments/prod/terraform.tfvars` o en el bloque `module`), no en un `-var` de CLI. Documenta cuál usaste en el README.
     - No debe requerir intervención humana. La decisión DEBE ser estable para que un plan desde un checkout LIMPIO del root (simulando el apply de PR04) siga dando 0 cambios (ver verificación).
   - **Verifica IDEMPOTENCIA**: vuelve a correr `terraform -chdir=terraform/environments/prod plan -input=false` y confirma que reporta "0 to add, 0 to change, 0 to destroy".

8. **VERIFICA RECURSOS REALES en IAM** (DoD; todos deben existir de verdad):
   ```bash
   aws iam get-open-id-connect-provider \
     --open-id-connect-provider-arn arn:aws:iam::950639281773:oidc-provider/token.actions.githubusercontent.com
   aws iam get-role --role-name cam-counter-gha-plan
   aws iam get-role --role-name cam-counter-gha-deploy
   ```
   Confirma con `aws iam get-role` que el trust del rol PLAN incluye `pull_request` y el del DEPLOY NO lo incluye (revisa el `AssumeRolePolicyDocument` devuelto). Confirma que los recursos llevan el tag en MINÚSCULA `project=cam-counter` y `managed_by=mad-runner` (F3): `aws iam list-role-tags --role-name cam-counter-gha-plan` debe mostrar la clave `managed_by` con valor `mad-runner` (NO `ManagedBy=mad-runner`).

9. **Registra las variables de repositorio con los ARNs REALES** ya creados (NO secrets). Toma los ARNs de los outputs del state:
   ```bash
   PLAN_ARN=$(terraform -chdir=terraform/environments/prod output -raw gha_plan_role_arn)
   DEPLOY_ARN=$(terraform -chdir=terraform/environments/prod output -raw gha_deploy_role_arn)
   gh variable set GHA_PLAN_ROLE_ARN   --body "$PLAN_ARN"   --repo jlsaco/cam-counter
   gh variable set GHA_DEPLOY_ROLE_ARN --body "$DEPLOY_ARN" --repo jlsaco/cam-counter
   gh variable set AWS_REGION     --body "us-east-1"   --repo jlsaco/cam-counter
   gh variable set AWS_ACCOUNT_ID --body "950639281773" --repo jlsaco/cam-counter
   ```
   Si no tienes permisos `gh` para escribir variables, documéntalo claramente en el cuerpo del PR como paso pendiente del operador (pero NO inventes ARNs: usa los reales de los outputs). JAMÁS uses `gh secret set` para esto.

10. **Teardown documentado**: deja claro en el README/CLAUDE.md que existe `terraform -chdir=terraform/environments/prod destroy` para desmontar; estos recursos IAM/OIDC son de costo cero. Etiquetado `project=cam-counter` / `managed_by=mad-runner` (minúscula, F3) para trazabilidad y limpieza.

---

## 7) Definition of Done (objetivo y verificable)

1. El módulo `terraform/modules/iam-github-oidc/` crea el proveedor OIDC `token.actions.githubusercontent.com` y DOS roles. El trust de ambos fija `aud = sts.amazonaws.com` y `sub` acotado a `repo:jlsaco/cam-counter` (NO wildcard de repo). El trust del rol PLAN incluye el `sub` de `pull_request` (para que los jobs de plan en PR puedan asumirlo). El rol DEPLOY está restringido a `environment:prod`/`refs/heads/main`/tags y NO incluye `pull_request`.
2. La raíz `terraform/environments/prod/` expone los outputs `oidc_provider_arn`, `gha_plan_role_arn` y `gha_deploy_role_arn`.
3. **(F1) El plan se INSPECCIONÓ antes de aplicar y resultó ESTRICTAMENTE ADITIVO** (0 destroy, 0 replace sobre recursos de PR02); el apply se realizó SOLO desde esta rama apilada (la más alta de la pila hasta ahora) y NO se reaplicará una rama inferior tras una superior.
4. **El runner APLICÓ de forma AUTÓNOMA** (`terraform apply -auto-approve`) el proveedor OIDC + ambos roles contra la cuenta REAL `950639281773`/`us-east-1`, y se VERIFICÓ que existen de verdad en IAM: `aws iam get-open-id-connect-provider` (proveedor OIDC) y `aws iam get-role --role-name cam-counter-gha-plan` y `--role-name cam-counter-gha-deploy` devuelven los recursos. El apply es IDEMPOTENTE y PERSISTENTE: un segundo `plan` (y un plan desde checkout limpio del root, simulando PR04) reporta "0 to add, 0 to change, 0 to destroy"; la resolución del huevo-y-gallina del OIDC quedó persistida (import al state o `create_oidc_provider=false` fijado en HCL/tfvars commiteado, NO `-var` de CLI).
5. **(F3) Los recursos llevan los tags lógicos en MINÚSCULA `project=cam-counter` y `managed_by=mad-runner`** (verificado con `aws iam list-role-tags`), además de los `default_tags` capitalizados `{ Project=cam-counter, ManagedBy=terraform, Env=prod }`. NUNCA `ManagedBy=mad-runner`. Existe `terraform destroy` documentado para teardown.
6. El job de plan de CI usa `aws-actions/configure-aws-credentials` vía OIDC con el rol PLAN, ejecuta un `terraform plan` de solo lectura, NO contiene NINGUNA clave AWS estática y NINGÚN workflow de GitHub Actions invoca `terraform apply` (CI plan-only, F2).
7. Las repository **variables** `GHA_PLAN_ROLE_ARN` y `GHA_DEPLOY_ROLE_ARN` quedan registradas con los ARNs REALES creados (más `AWS_REGION`, `AWS_ACCOUNT_ID`), como variables y NO secrets; si faltó permiso `gh`, queda documentado el paso del operador con los ARNs reales de los outputs.
8. La documentación mapea cada contexto de claim `sub` de GitHub a su rol; `terraform validate` pasa; `terraform fmt -check -recursive terraform/` pasa. Ninguna credencial AWS commiteada (el apply usó credenciales del entorno del runner, F2).

---

## 8) Verificación (ejecuta estos comandos; todos deben pasar)

```bash
# 1) El módulo inicializa (sin backend) y valida (offline-safe)
terraform -chdir=terraform/modules/iam-github-oidc init -backend=false && \
terraform -chdir=terraform/modules/iam-github-oidc validate

# 2) Formato canónico en todo terraform/
terraform fmt -check -recursive terraform/

# 3) El módulo declara el proveedor OIDC, el repo correcto, el aud y el contexto pull_request
grep -q 'token.actions.githubusercontent.com' terraform/modules/iam-github-oidc/*.tf && \
grep -q 'repo:jlsaco/cam-counter' terraform/modules/iam-github-oidc/*.tf && \
grep -q 'sts.amazonaws.com' terraform/modules/iam-github-oidc/*.tf && \
grep -q 'pull_request' terraform/modules/iam-github-oidc/*.tf && echo OK_GREPS

# 4) No hay NINGUNA clave AWS estática en .github/ ni terraform/
if grep -rqiE 'aws_access_key_id|aws_secret_access_key' .github/ terraform/; then echo 'STATIC KEY FOUND'; exit 1; else echo 'NO STATIC KEYS'; fi

# 5) Sanity: la raíz prod expone los outputs
grep -q 'oidc_provider_arn' terraform/environments/prod/*.tf && \
grep -q 'gha_plan_role_arn' terraform/environments/prod/*.tf && \
grep -q 'gha_deploy_role_arn' terraform/environments/prod/*.tf && echo OK_OUTPUTS

# 6) Sanity: el rol deploy NO admite pull_request en su trust (revisión manual + grep dirigido)
#    Verifica leyendo main.tf que el bloque del trust del rol DEPLOY no contenga 'pull_request'.

# 7) Sanity F3: el wiring/módulo usa el tag lógico minúscula managed_by=mad-runner (NO ManagedBy=mad-runner)
grep -rq 'managed_by' terraform/ && \
grep -rq 'mad-runner' terraform/ && \
if grep -rq 'ManagedBy *= *"mad-runner"' terraform/; then echo 'BAD: ManagedBy capitalizado = mad-runner'; exit 1; else echo OK_TAGS_F3; fi

# 8) Sanity: el workflow de plan usa OIDC y no claves estáticas, y NINGÚN workflow hace apply (CI plan-only)
grep -rq 'configure-aws-credentials' .github/workflows/ && \
grep -rq 'id-token' .github/workflows/ && \
! grep -rq 'terraform apply' .github/workflows/ && echo OK_WORKFLOW

# 9) APPLY AUTÓNOMO ya ejecutado: el state remoto contiene los recursos y el plan es idempotente y ADITIVO (F1)
terraform -chdir=terraform/environments/prod plan -input=false -detailed-exitcode ; \
  test $? -eq 0 && echo OK_IDEMPOTENTE   # exit 0 = sin cambios pendientes; 2 = habría cambios -> FALLO de idempotencia/F1

# 10) RECURSOS REALES en IAM (deben existir de verdad tras el apply del runner)
aws iam get-open-id-connect-provider \
  --open-id-connect-provider-arn arn:aws:iam::950639281773:oidc-provider/token.actions.githubusercontent.com >/dev/null && \
aws iam get-role --role-name cam-counter-gha-plan   >/dev/null && \
aws iam get-role --role-name cam-counter-gha-deploy >/dev/null && echo OK_IAM_REAL

# 11) El trust real del rol DEPLOY NO admite pull_request, el del PLAN sí
aws iam get-role --role-name cam-counter-gha-deploy --query 'Role.AssumeRolePolicyDocument' --output json | grep -q 'pull_request' && { echo 'DEPLOY ADMITE PULL_REQUEST (MAL)'; exit 1; } || echo OK_DEPLOY_SIN_PR
aws iam get-role --role-name cam-counter-gha-plan   --query 'Role.AssumeRolePolicyDocument' --output json | grep -q 'pull_request' && echo OK_PLAN_CON_PR

# 12) F3 en recursos reales: el tag MINÚSCULA managed_by=mad-runner está presente (NO la clave capitalizada)
aws iam list-role-tags --role-name cam-counter-gha-plan --query "Tags[?Key=='managed_by'].Value" --output text | grep -q 'mad-runner' && echo OK_TAG_REAL_F3

# 13) Las variables de repo quedaron con los ARNs reales (NO secrets)
gh variable list --repo jlsaco/cam-counter | grep -E 'GHA_PLAN_ROLE_ARN|GHA_DEPLOY_ROLE_ARN' && echo OK_VARS
```

Los comandos 1–8 son offline-safe (no requieren red). Los comandos 9–13 verifican el APPLY REAL del runner contra AWS y por tanto requieren las credenciales del entorno del runner; si por orden de dependencias el state backend de PR02 aún no estuviera aplicado, primero asegúralo (PR02 es dependencia dura). El `apply` real lo ejecuta el RUNNER MAD de forma autónoma; GitHub Actions NUNCA lo ejecuta.

---

## 9) Entrega — abrir el Pull Request

1. Asegúrate de que el árbol esté limpio salvo tus cambios y que NO haya credenciales ni archivos de plan binarios commiteados: `git status` (añade `tfplan.pr03` a `.gitignore` si hiciera falta; no lo commitees).
2. Commit (mensaje claro, en imperativo). Termina el mensaje del commit con la línea de co-autoría EXACTA:
   ```
   Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
   ```
3. Push de la rama nueva: `git push -u origin feat/03-iam-github-oidc`.
4. Abre el PR con `gh`, **base = `feat/02-tf-state-backend`** (rama del PR anterior; PR apilado). Si usaste otra base por indisponibilidad, usa esa y explícalo en el cuerpo:
   ```bash
   gh pr create \
     --base feat/02-tf-state-backend \
     --head feat/03-iam-github-oidc \
     --title "infra(PR03): OIDC provider + roles GHA plan/deploy separados (apply autónomo del runner) + plan read-only en CI" \
     --body "$(cat <<'EOF'
   ## Qué
   - Nuevo módulo `terraform/modules/iam-github-oidc/`: proveedor OIDC `token.actions.githubusercontent.com` + DOS roles separados `cam-counter-gha-plan` (solo lectura, asumible desde `pull_request` y `main`) y `cam-counter-gha-deploy` (apply, gated a `environment:prod`/`main`/tags, NUNCA desde `pull_request`).
   - Wiring en `terraform/environments/prod/` + outputs `oidc_provider_arn`, `gha_plan_role_arn` y `gha_deploy_role_arn`.
   - **APPLY AUTÓNOMO del runner MAD** contra AWS REAL (cuenta `950639281773`/`us-east-1`): `terraform apply -auto-approve` del proveedor OIDC + ambos roles, tras INSPECCIONAR el plan para confirmar que es ESTRICTAMENTE ADITIVO (F1: 0 destroy/replace sobre PR02). Verificado con `aws iam get-open-id-connect-provider` y `aws iam get-role`. Idempotente y persistente (segundo plan y plan desde checkout limpio = 0 cambios). Recursos etiquetados (F3) `project=cam-counter` / `managed_by=mad-runner` (minúscula) + `default_tags` `{Project, ManagedBy=terraform, Env}`.
   - Job de CI de `terraform plan` (solo PRs que tocan `terraform/`) que asume el rol PLAN vía OIDC con `aws-actions/configure-aws-credentials`, ejecuta plan read-only y NUNCA hace apply (CI plan-only por diseño, F2). Cero claves estáticas; ARNs/región/cuenta por variables de repositorio (`vars.*`) con los ARNs REALES ya creados.

   ## Por qué (trade-offs declarados)
   - DOS actores: el RUNNER MAD aplica la infra de forma autónoma con las credenciales de su entorno (jamás commiteadas); GitHub Actions CI permanece SOLO-PLAN por OIDC para evitar escalada de privilegios desde PRs.
   - F2 (apply autónomo acotado): el runner aplica HCL curado de esta pila antes del merge humano; se justifica porque el HCL proviene de esta pila controlada y el plan se inspecciona para ser estrictamente aditivo. El runner NO aplica HCL arbitrario.
   - F1 (state aditivo/monótono): solo se aplica desde la rama apilada más alta; nunca reaplicar una rama inferior tras una superior; plan inspeccionado y abortado ante cualquier destroy/replace.
   - Separar plan/apply evita que un PR malicioso invoque `apply`; el rol DEPLOY queda creado para operación futura (release/promote escriben OBJETOS S3, NO apply de infra), NO asumible desde `pull_request`.
   - Trust por OIDC acotado a `repo:jlsaco/cam-counter` con `aud=sts.amazonaws.com` y condiciones por `sub` distintas por contexto; cumple "cero secretos en git".

   ## Verificación contra recursos REALES
   - `aws iam get-open-id-connect-provider --open-id-connect-provider-arn arn:aws:iam::950639281773:oidc-provider/token.actions.githubusercontent.com`
   - `aws iam get-role --role-name cam-counter-gha-plan` y `--role-name cam-counter-gha-deploy`
   - Idempotencia/F1: `terraform -chdir=terraform/environments/prod plan -detailed-exitcode` → exit 0 (sin cambios, plan aditivo).
   - Trust real: DEPLOY sin `pull_request`, PLAN con `pull_request`.
   - F3: `aws iam list-role-tags --role-name cam-counter-gha-plan` muestra `managed_by=mad-runner` (minúscula).

   ## Mapeo sub→rol
   - `pull_request` → rol PLAN. `push main` → PLAN. tag → DEPLOY (operación futura). `environment:prod` → DEPLOY (operación futura). `aud` siempre `sts.amazonaws.com`. CI usa solo el rol PLAN.

   ## Cómo verificar (offline + real)
   - `terraform -chdir=terraform/modules/iam-github-oidc init -backend=false && terraform -chdir=terraform/modules/iam-github-oidc validate`
   - `terraform fmt -check -recursive terraform/`
   - greps de la sección Verificación (OIDC, repo, aud, pull_request, sin claves estáticas, outputs presentes, tags F3, workflow sin `apply`).
   - comandos 9–13 (idempotencia/F1 + `aws iam get-*` + trust real + tag real F3 + `gh variable list`).

   ## Idempotencia del proveedor OIDC (persistente)
   - Si el proveedor ya existía, se resolvió de forma PERSISTENTE (import al state compartido o `create_oidc_provider=false` fijado en HCL/tfvars commiteado), NO con `-var` efímero de CLI, para que el apply de PR04 (que reaplica el root) siga dando 0 cambios.

   ## Teardown / costo
   - Recursos IAM/OIDC de costo cero. Teardown: `terraform -chdir=terraform/environments/prod destroy`. Tags `project=cam-counter` / `managed_by=mad-runner` (minúscula) para trazabilidad y limpieza.

   ## Notas
   - El `apply` lo ejecutó el RUNNER MAD de forma autónoma con las credenciales de su ENTORNO (jamás commiteadas). GitHub Actions NUNCA hace apply de infra.
   - Variables de repositorio `GHA_PLAN_ROLE_ARN`, `GHA_DEPLOY_ROLE_ARN`, `AWS_REGION`, `AWS_ACCOUNT_ID` registradas con valores REALES (variables, NO secrets). Si faltó permiso `gh`, queda el paso del operador con los ARNs reales de los outputs.

   PR apilado sobre PR02 (`feat/02-tf-state-backend`). Mergear bottom-up.

   🤖 Generated with [Claude Code](https://claude.com/claude-code)
   EOF
   )"
   ```
5. **DEVUELVE la URL del PR** como salida final de tu ejecución (imprímela claramente), junto con los ARNs reales creados (`gha_plan_role_arn`, `gha_deploy_role_arn`) y el ARN del proveedor OIDC, para trazabilidad.

---

## Prohibiciones explícitas
- PROHIBIDO commitear secretos o claves AWS de larga vida (`aws_access_key_id`, `aws_secret_access_key`, tokens, contraseñas) en cualquier archivo del repo. El apply autónomo usa las credenciales del ENTORNO del runner; toda autenticación de CI es por OIDC; ARNs/región/cuenta por variables de repositorio.
- PROHIBIDO que GitHub Actions ejecute `terraform apply` (en PR, push o tag). CI es plan-only por OIDC. El apply de la pila lo hace el RUNNER MAD de forma autónoma (F2).
- PROHIBIDO aplicar si el plan NO es estrictamente aditivo: si `terraform plan` muestra CUALQUIER `destroy` o `replace` de un recurso existente, ABORTA (F1). PROHIBIDO reaplicar esta rama (PR03) después de que una rama superior (p.ej. PR04) haya aplicado.
- PROHIBIDO usar wildcard de repo en el trust OIDC (`repo:*`); acota siempre a `repo:jlsaco/cam-counter`.
- PROHIBIDO que el rol PLAN tenga permisos de apply, o que el rol DEPLOY sea asumible desde `pull_request`.
- PROHIBIDO un apply NO idempotente: un segundo apply (y un plan desde checkout limpio del root) debe reportar "0 to add, 0 to change, 0 to destroy". PROHIBIDO resolver el huevo-y-gallina del OIDC con un `-var` efímero de CLI no persistido.
- PROHIBIDO usar `ManagedBy = "mad-runner"` (capitalizado); el tag lógico es la clave en MINÚSCULA `managed_by = "mad-runner"` y la capitalizada `ManagedBy` vale `terraform` (F3).
- PROHIBIDO tocar `rtsp-enable/`, el bucket `cam-counter-rpi-artifacts-950639281773`, o crear recursos fuera del alcance (media/events/devices/releases buckets, rol per-Pi).
