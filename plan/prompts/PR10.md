# edge+ui: supervisor multi-cámara (Hailo VDevice compartido) + worker cloud-sync (tolerante a offline, manifest-no-registry) + Playwright E2E + prueba de integración REAL contra AWS (recursos de PR04)

# edge+ui: supervisor multi-cámara (Hailo VDevice compartido) + worker cloud-sync (tolerante a offline, manifest-no-registry) + Playwright E2E + prueba de integración REAL contra AWS (recursos de PR04)

# PR10 — Supervisor multi-cámara (Hailo VDevice compartido) + worker cloud-sync (tolerante a offline) + Playwright E2E + integración end-to-end contra recursos AWS REALES

Eres un agente Claude CLI que debe entregar UN Pull Request completo, pequeño-mediano, construible y verificable, en el monorepo `cam-counter`. Este prompt es TOTALMENTE AUTOCONTENIDO: no tienes más contexto que el aquí escrito. Lee los archivos del repo para confirmar el estado real antes de actuar.

---

## 1) Contexto

**Repositorio:** `github.com/jlsaco/cam-counter`, rama por defecto `main`. Es un monorepo. Todo el trabajo vive en este repo.

**Producto:** `cam-counter` evoluciona de un detector de personas a un PRODUCTO de CONTEO de personas que cruzan una LÍNEA-UMBRAL configurable, en TIEMPO REAL y EN EL BORDE (Raspberry Pi 5 + acelerador Hailo-8 + YOLOv8s; cámara EZVIZ/Hikvision por RTSP a 1080p@15fps). Dos sentidos independientes ("subieron"/"bajaron"). Al cruzar una persona se captura un clip y se sube a S3, y el evento se registra en DynamoDB. Hay una UI LOCAL en la LAN servida desde el Pi.

**Filosofía EDGE-FIRST / tolerante a offline:** el Pi cuenta y persiste en LOCAL (SQLite) aunque NO haya internet; la nube solo recibe sincronización/histórico. La config de la línea vive en local. MULTI-CÁMARA y multi-sitio desde la v1: todo indexado por `site_id`/`device_id`/`camera_id`; un mismo Pi puede manejar varias cámaras (holgura para 3-4).

**Modelo de despliegue de la pila (IMPORTANTE — léelo):** esta es una pila de PRs apilados que se ejecuta de forma AUTÓNOMA por un RUNNER MAD. Hay DOS actores distintos que NO se deben confundir:
- **RUNNER MAD:** tiene credenciales AWS amplias en su ENTORNO (jamás commiteadas). ES QUIEN EJECUTA `terraform apply` de forma AUTÓNOMA dentro de los PRs de infraestructura (PR02, PR03, PR04, PR11), contra la cuenta REAL `950639281773` / `us-east-1`. Por tanto, **cuando este PR se ejecuta, los recursos AWS de los PRs de infra anteriores YA ESTÁN APLICADOS de verdad** (bucket de media, tabla de eventos, tabla de registro, rol IAM per-Pi de mínimo privilegio).
- **GITHUB ACTIONS CI:** permanece SOLO-PLAN (fmt/validate/plan read-only vía OIDC). NUNCA hace `apply` de infraestructura (evita escalada de privilegios desde PRs no confiables). Para el código (Python/UI) CI corre los tests headless sin hardware. (NOTA de coherencia con PR11: los workflows de release/promote SÍ publican OBJETOS S3 — artefactos/manifiestos — con el rol de deploy OIDC gated por Environment; eso NO es `terraform apply` de infraestructura y NO contradice la regla "CI nunca aplica infra". PR10 no introduce esos workflows.)

Este PR (PR10) NO crea ni aplica infraestructura: **CONSUME** los recursos ya aplicados por PR04 y los VALIDA con una prueba de integración real (ver §3). PR10 DEPENDE de que PR04 ya haya aplicado.

**Estado del código en este punto de la pila (IMPORTANTE):** este PR es el número 10 de una pila de PRs apilados. Asume que TODO lo siguiente YA EXISTE en la rama base sobre la que ramificas (no lo crees tú, ya está hecho por PRs anteriores):
- El sistema de borde legacy se movió a `v1/` con `git mv` (historia preservada). El detector monolítico legacy era `detection/yolo_personas_mt.py` (pipeline de 3 hilos captura->inferencia Hailo+anotado->encode/MJPEG en `:8080`; salida NMS de Hailo en formato `[ymin,xmin,ymax,xmax,score]` NORMALIZADO 0..1). El servicio systemd legacy es `hailo-personas` con rutas ABSOLUTAS bajo `/home/pi/Documents/hailo-ezviz-personas/...`.
- `v1/edge/` contiene el paquete Python **`cam_counter_edge`** (NO `counter`, el nombre top-level genérico fue rechazado por colisión de namespace) instalable con `pip install -e v1/edge`. Ya contiene, de PRs anteriores:
  - `cam_counter_edge/detector.py` con `Detector` (atado a Hailo, único componente atado a hardware) y `DummyDetector` (permite testear TODA la lógica de conteo en x86/CI sin hardware). El `Detector` reordena la salida Hailo `[ymin,xmin,ymax,xmax]` al sistema-wide `[xmin,ymin,xmax,ymax]`. Coordenadas NORMALIZADAS 0..1 relativas al frame original, origen arriba-izquierda, en TODAS partes.
  - `cam_counter_edge/types.py` (tipos compartidos) y validación de slugs.
  - `cam_counter_edge/tracker.py` (tracker centroid+IoU con ids estables tras reaparición).
  - `cam_counter_edge/counter.py` o `linecounter.py` (`LineCounter`: half-plane + histéresis + idempotencia por-track) y `store.py` (SQLite WAL) con el contador MONÓTONO PERSISTIDO por-cámara `crossing_seq`.
  - `cam_counter_edge/config.py` (config de línea por-cámara en SQLite + hot-reload por `config_version` SIN reiniciar el proceso) y `clip.py` (buffer circular de JPEG -> MP4 async + cola `clip_uploads`, claves S3 collision-safe).
- `v1/api/` (FastAPI) sirve la API JSON/WS bajo `/api/*` y la SPA React prebuilt en cualquier otra ruta (mismo origen, sin CORS, sin auth cloud — confianza LAN con gate opcional de token compartido en escrituras). `v1/api/schemas.py` define UNA vez los modelos Pydantic (`CrossingEvent`, `LineConfig`, `Counters`, `DeviceInfo`, `Camera`, `WsEnvelope`), espejados como JSON Schema en `contracts/`. Existe una FUENTE FAKE (env-gated `CAMCOUNTER_FAKE_SOURCE=1`) que reproduce un MJPEG en bucle + cruces guionizados para E2E determinista y dev local SIN Pi.
- `v1/ui/` (React + TypeScript + Vite + Tailwind) con la SPA: vídeo en vivo (MJPEG por cámara), línea de conteo como overlay SVG client-side en coords normalizadas (arrastrable a 60fps sin round-trip), contadores y editor de línea.
- `terraform/` con módulos por servicio y `environments/prod`, estado remoto en S3 + lock en DynamoDB. **Estos recursos YA FUERON APLICADOS de verdad por el runner MAD en PR02/PR03/PR04** (no son plan ni mocks): el bucket de media de producto `cam-counter-media-950639281773`, la tabla DynamoDB de eventos `cam-counter-events`, la tabla de registro de dispositivos `cam-counter-devices` y el rol IAM per-Pi de mínimo privilegio (rol STS corto por dispositivo, acotado al prefijo de media propio del device y a su partición de clave en DynamoDB), cuyo ARN PR04 exporta como output `edge_role_arn`. TODOS los recursos están etiquetados con `default_tags` capitalizados `{Project="cam-counter", ManagedBy="terraform", Env="prod"}` MÁS los tags lógicos en MINÚSCULA `project=cam-counter` y `managed_by=mad-runner` (esa clave de trazabilidad/limpieza se busca SIEMPRE en minúscula: nunca `ManagedBy="mad-runner"`).
- `contracts/` con los JSON Schema de los contratos compartidos.
- `scripts/version.py` (deriva SemVer vía `git describe`, degradando limpio sin tags).

**SI AL LEER EL REPO algo de lo anterior NO existe** (porque ejecutas sobre una base distinta a la esperada), NO lo inventes ni lo recrees: adapta los imports/rutas a lo que realmente encuentres, y documenta en el cuerpo del PR cualquier discrepancia. Confirma siempre con `ls`/lectura de archivos antes de asumir rutas. Para los nombres EXACTOS de recursos AWS (bucket de media, tablas, ARN del rol per-Pi) prefiere leerlos de los outputs de `terraform/environments/prod` (p.ej. `terraform -chdir=terraform/environments/prod output -raw edge_role_arn`) o de los contratos, antes de hardcodearlos.

---

## 2) Objetivo

Cerrar el lazo de ejecución multi-cámara y de sincronización a la nube tolerante a offline, más cobertura E2E de la UI, y DEMOSTRAR que el bloque de sync funciona de verdad contra los recursos AWS REALES ya desplegados por PR04:

1. **Supervisor multi-cámara** (`v1/edge/cam_counter_edge/app.py`, entrypoint `cam-counter-edge`): carga la config de N cámaras, crea UN solo Hailo VDevice COMPARTIDO con un lock corto alrededor de `infer()`, lanza un `CameraPipeline` por cámara (capture->detect->track->count->present) reusando colas `maxsize=2` que descartan el frame viejo, reinicia INDIVIDUALMENTE un pipeline caído sin tumbar a los demás, y expone `/healthz` con salud DE PRODUCTO por-cámara.
2. **Worker cloud-sync** (`v1/edge/cam_counter_edge/sync.py`): drena eventos locales no sincronizados a la nube de forma idempotente y tolerante a offline, escribiendo SOLO heartbeat al registro (nunca lee el registro para decidir trabajo).
3. **Suite Playwright E2E** (`v1/ui/e2e/`) contra FastAPI + fuente fake.
4. **Servicio systemd** `cam-counter-edge.service` que COEXISTE con `hailo-personas` (sin cutover; rollback = re-habilitar `hailo-personas`).
5. **Job de CI** que corre todo headless sin hardware, incluido un test de sync offline->online (con fakes).
6. **Prueba de INTEGRACIÓN end-to-end contra los recursos AWS REALES de PR04** (NUEVO, núcleo del requisito): valida el worker cloud-sync contra el bucket de media REAL y la tabla de eventos REAL ya desplegados — PutObject real, conditional-put real, demostración de IDEMPOTENCIA por reintento del mismo `event_id`, read-back, y CLEANUP de los datos de prueba. Idealmente asumiendo el ROL per-Pi de PR04 para validar también que el IAM acotado funciona. Esta prueba degrada a SKIP si no hay credenciales (nunca rompe CI sin OIDC), pero su Definition of Done EXIGE que pase contra AWS REAL en el entorno del runner; y cuando hay credenciales un SKIP cuenta como FALLO (ver §3, gating estricto).

---

## 3) Alcance y NO-alcance

### Dentro de alcance (lo que ESTE PR debe entregar)
- `v1/edge/cam_counter_edge/app.py`: supervisor + `CameraPipeline` + entrypoint `cam-counter-edge` (registrar en el `[project.scripts]`/`console_scripts` del packaging de `v1/edge`).
- `/healthz` del supervisor: salud DE PRODUCTO, no solo liveness. Debe reportar POR-CÁMARA al menos: `fps`, `latency_ms`, `hailo_busy`, **`frames_processed`** (creciente), **`last_inference_ts`** (reciente). El endpoint distingue una cámara que responde 200 pero NO procesa frames (frames=0) de una sana. Devuelve estado agregado (p.ej. 200 si todas sanas; 503/degraded si alguna no procesa).
- `v1/edge/cam_counter_edge/sync.py`: worker cloud-sync.
- `v1/systemd/cam-counter-edge.service`: unit nuevo que COEXISTE con `hailo-personas` (no eliminar el legacy). Usa un mecanismo de render de ruta (placeholder reemplazado por instalador) en lugar de path absoluto fijo, coherente con cómo el repo ya maneja `hailo-personas` (que se renderiza con un `sed` en instalación). NO hagas cutover: el servicio nuevo queda definido pero el legacy sigue siendo el que arranca por defecto hasta que el orquestador decida.
- `v1/ui/e2e/`: suite Playwright (config + specs).
- Job de CI (en `.github/workflows/`, extendiendo lo existente con path-filters) que corra: `ruff`, `mypy`, `tsc`, `vite build`, `pytest`, Playwright headless, y un test de sync offline. La prueba de integración real contra AWS se incluye como suite ADICIONAL gated por credenciales (ver más abajo): en CI sin OIDC se SALTA (skip con aviso) y NUNCA rompe el build.
- **Prueba de integración real contra AWS** en `v1/edge/tests/` (p.ej. `test_sync_integration_aws.py`), marcada con un marker pytest claro (p.ej. `@pytest.mark.integration_aws`) y gated por presencia de credenciales. Ver §"Prueba de integración end-to-end contra AWS REAL".
- Doc del checklist de smoke EN HARDWARE (en `docs/` o `v1/docs/`, según convención del repo; busca dónde viven los docs y respétalo): presupuesto del VDevice compartido (`4 * 6.6ms < 66ms`), `/healthz frames>0`, cruce manual, Hailo presente, frames>0.
- Doc breve (en el mismo lugar de docs) de cómo correr la prueba de integración real contra AWS y cómo se limpia (incluye la nota de teardown: los recursos los gestiona el runner MAD vía `terraform destroy`/script de teardown; esta prueba NO crea recursos persistentes, solo objetos/ítems de prueba que ELIMINA al final). Documenta también el comando estricto del runner (que distingue "skip legítimo sin credenciales" de "skip indebido con credenciales" y exige PASS real).

### Comportamientos OBLIGATORIOS del worker cloud-sync (`sync.py`)
- Lee de SQLite los eventos `WHERE synced=0`.
- Sube el clip al bucket de media por SigV4 con el rol STS corto por-Pi; **retry-safe**: usa `If-None-Match: *` (o key temporal + copy) para NO pisar un upload parcial previo del mismo `event_id` en reintento.
- Hace conditional-put del `CrossingEvent` en la tabla `cam-counter-events` con el `clip_key`; **idempotente** vía el `event_id` DETERMINISTA — un conditional put rechazado por duplicado NO es error, marca `synced=1` igual.
- Escribe heartbeat al registro de dispositivos SOLO con `UpdateItem` (`reported_version`, `last_seen_at`, `status`). **NUNCA lee el registro para decidir qué subir o qué versión correr** (fuente única de verdad de la versión deseada = manifiesto de canal en S3, fuera del alcance de este PR; aquí solo respetamos la regla de no-leer-registro). Mantén la tolerancia offline manifest-no-registry intacta.
- **Offline explícito:** cuando la red está caída, el CONTEO sigue contando y persistiendo en local; el sync NO bloquea el conteo. Al reconectar, drena el backlog con retry/backoff acotado y SIN duplicados (un mismo evento subido dos veces produce el mismo `event_id` -> dedupe en DynamoDB y en S3).
- Las llamadas a AWS deben ser inyectables/mockeables (cliente boto3 o equivalente pasado por parámetro/factory) para que CI pueda stubear "red caída luego arriba" sin AWS real, Y para que la prueba de integración real inyecte un cliente boto3 real (o uno construido a partir de credenciales STS asumiendo el rol per-Pi).

### Prueba de integración end-to-end contra AWS REAL (NÚCLEO DEL REQUISITO)
Añade una prueba que valide el worker cloud-sync contra los recursos REALES ya desplegados en PR04 (bucket de media + tabla de eventos), NO mocks. Debe demostrar el **contrato A** (`event_id` determinista + conditional put) contra DynamoDB REAL. Requisitos exactos:
- **(a) Subir un clip REAL a S3:** `PutObject` real al bucket de media REAL (`cam-counter-media-950639281773`), bajo un prefijo de prueba CLARAMENTE MARCADO, p.ej. `media/_selftest/...` (o un `site_id`/`device_id` de selftest dedicado dentro del esquema de claves canónico). El objeto debe ser pequeño (un MP4/clip de prueba mínimo).
- **(b) Escribir un CrossingEvent REAL a DynamoDB:** `PutItem` real con **conditional put** a la tabla `cam-counter-events`, con el `clip_key` apuntando al objeto subido, usando un `event_id` determinista de prueba.
- **(c) Verificar IDEMPOTENCIA:** reintentar el MISMO `event_id` NO duplica — el conditional put debe ser RECHAZADO por la condición (ConditionalCheckFailed) y el worker debe tratarlo como éxito idempotente (marca `synced=1`, no error). La prueba ASERTA que el segundo intento no crea un segundo ítem.
- **(d) Read-back:** `GetItem` del evento (y `HeadObject` del clip) para confirmar que quedaron escritos de verdad.
- **(e) CLEANUP:** ELIMINA los datos de prueba al final (`DeleteItem` del evento + `DeleteObject` del clip), SIEMPRE (usa `try/finally` o fixture de teardown) para no contaminar las tablas/bucket reales, incluso si una aserción falla.
- **IAM acotado (PRIORITARIO):** la prueba ASUME el ROL per-Pi de mínimo privilegio creado en PR04 (`sts:AssumeRole` -> credenciales temporales) y opera con ESAS credenciales, validando así que el IAM acotado realmente permite las operaciones esperadas (escribir solo en su prefijo/partición). **Fuente canónica del ARN del rol per-Pi:** léelo del output `edge_role_arn` de PR04 vía `terraform -chdir=terraform/environments/prod output -raw edge_role_arn` (con el backend remoto inicializado), o de una variable de entorno inyectada por el orquestador (p.ej. `CAMCOUNTER_EDGE_ROLE_ARN`). NO reconstruyas el ARN a partir de placeholders de `site_id`/`device_id`. Comportamiento ante fallo (F7/F10): cuando la integración real está HABILITADA (flag + credenciales presentes), si NO se resuelve el ARN del rol o el `AssumeRole` FALLA, la prueba debe FALLAR con un mensaje claro que lo atribuya a configuración faltante de PR04 (trust del rol per-Pi que no lista al principal del runner, u output ausente) — **NUNCA degradar en silencio a las credenciales del entorno ni a un SKIP**. Solo se permite el fallback documentado a las credenciales del entorno si el ORQUESTADOR lo activa explícitamente con una variable dedicada (p.ej. `CAMCOUNTER_AWS_INTEGRATION_ALLOW_ENV_CREDS=1`); en ese caso deja un TODO claro y documenta que NO valida el IAM acotado. PRIORIZA asumir el rol.
- **Gating por credenciales (skip vs fail, F8 — sin ambigüedad):** la prueba detecta presencia de credenciales AWS utilizables (flag `CAMCOUNTER_AWS_INTEGRATION=1` + credenciales resolubles por boto3, o capacidad de asumir el rol). DOS casos:
  - Si NO hay credenciales utilizables (CI sin OIDC): `pytest.skip("sin credenciales AWS; integración real omitida")` con aviso explícito. NUNCA falla en CI sin OIDC.
  - Si `CAMCOUNTER_AWS_INTEGRATION=1` Y hay credenciales válidas (entorno del runner): un SKIP de la prueba de integración cuenta como FALLO. Implementa un MODO ESTRICTO de runner: cuando el flag esté activo y existan credenciales, la suite `integration_aws` DEBE producir al menos un PASS real (que efectivamente subió/escribió/leyó/limpió); si la única salida es SKIP, el harness del runner lo convierte en FALLO. Provee el comando exacto del runner que lo garantiza (ver §8: un wrapper/`conftest` que, con el flag activo + credenciales, transforma "0 passed / N skipped" en exit-code != 0, o un test-guardián que falla si la integración no se ejecutó). El objetivo es que NO se pueda dar por cumplido el DoD con un skip indebido cuando SÍ hay credenciales.
- **Aislamiento:** la prueba usa identificadores de selftest dedicados (`site_id`/`device_id`/`camera_id` slugs válidos del estilo `selftest`/`selftest-dev`/`selftest-dev-cam0`) y un sufijo único (p.ej. timestamp) en el `track_id`/`crossing_seq` para que ejecuciones concurrentes no choquen, manteniendo a la vez el `event_id` determinista DENTRO de una misma corrida para poder probar la idempotencia.
- Reutiliza el MISMO código del worker `sync.py` (no reimplemente la lógica): la prueba inyecta el cliente boto3 real en el worker y ejerce su ruta real de subida + conditional-put + marcado idempotente.

### Pruebas con fakes (SE MANTIENEN — son la base)
- Conserva TODAS las pruebas unitarias/funcionales existentes con `DummyDetector`/fakes y stubs de AWS. La prueba de integración real es ADICIONAL, no las reemplaza. CI sin OIDC sigue verde con solo las pruebas de fakes.

### Fuera de alcance (NO TOCAR)
- **NO** modifiques la lógica de `rtsp-enable/` (utilidad Java + box64 que reactiva el RTSP de la cámara). Es intocable.
- **NO** elimines ni alteres el servicio `hailo-personas` ni su unit legacy. Debe seguir ejecutable. El rollback declarado es re-habilitar `hailo-personas`.
- **NO** hagas cutover del servicio (no deshabilites `hailo-personas` ni habilites `cam-counter-edge` por defecto).
- **NO** crees, modifiques ni apliques recursos de infraestructura Terraform en este PR. Este PR CONSUME los recursos ya APLICADOS por el runner MAD en PR04; no añadas módulos nuevos, no ejecutes `terraform apply` aquí. (El `apply` autónomo es responsabilidad de los PRs de infra; PR10 solo valida contra lo ya desplegado.)
- **NO** reescribas los contratos compartidos (`contracts/`, `v1/api/schemas.py`). Consúmelos. Cualquier cambio de campo sería BREAKING y requiere bump de `schema_version` + PR coordinado: no es el alcance.
- **NO** reutilices el bucket existente `cam-counter-rpi-artifacts-950639281773` (es backup de binarios ops, reservado). El media va al bucket de producto `cam-counter-media-950639281773`.
- **NO** introduzcas el agente OTA ni los workflows de release/promote (eso es el PR siguiente).
- **NO** dejes datos de prueba residuales en S3/DynamoDB reales: la prueba de integración SIEMPRE limpia lo que crea.

---

## 4) Restricciones

- **Stack:** Backend/borde en Python. UI en React + TypeScript + Vite + Tailwind. E2E con Playwright contra la UI local. Infra Terraform (no se toca aquí; sus recursos ya fueron aplicados por el runner MAD).
- **Dos actores, sin confusión:** el RUNNER MAD aplica infraestructura de forma autónoma en los PRs de infra (no en este). GitHub Actions CI permanece plan-only vía OIDC y NUNCA hace `apply` de infra. Este PR no aplica nada; valida contra recursos reales preexistentes.
- **Edge-first / offline-tolerante:** el conteo y la persistencia local NUNCA dependen de la red ni de DynamoDB. El sync es best-effort y desacoplado.
- **Multi-cámara:** UN solo Hailo VDevice compartido sirve a N pipelines bajo un proceso supervisor, con lock corto alrededor de `infer()`. Presupuesto: `4 cámaras * ~6.6ms/inferencia < 66ms` (margen para 15fps por cámara). Cada `CameraPipeline` reusa colas `maxsize=2` que descartan el frame viejo (ir siempre "en vivo").
- **Coordenadas:** TODA la geometría en floats normalizados 0..1 relativos al frame original, origen arriba-izquierda. Nunca píxeles en contratos almacenados.
- **Identificadores:** `site_id`, `device_id`, `camera_id` son slugs ASCII minúsculas que cumplen `^[a-z0-9][a-z0-9-]{1,62}$` (SIN `#`, SIN `/`). `camera_id` = `'{device_id}-cam{N}'`. Valida el regex ANTES de construir cualquier clave S3/DynamoDB. NO reconstruyas `device_id` por split de `camera_id`; usa campos separados. (Los identificadores de selftest de la prueba de integración deben cumplir el regex.)
- **`event_id` determinista** = `sha1` minúscula-hex de `site_id|device_id|camera_id|track_id|crossing_seq` (sha1 NO criptográfico, solo dedupe). Es la base de la idempotencia del sync; no lo recalcules con otra fórmula. La prueba de integración real DEMUESTRA este contrato (contrato A) contra DynamoDB REAL.
- **CERO SECRETOS EN EL REPO.** PROHIBIDO commitear cualquier credencial, contraseña, token, o el código/clave de la cámara. Sin claves AWS de larga vida: las credenciales son OIDC (GitHub Actions->AWS, solo para CI plan), o STS corto por-dispositivo, o las credenciales del ENTORNO del runner MAD (jamás commiteadas). Las credenciales de cámara y de AWS se leen de entorno/SSM/SQLite/ficheros en `shared/`, nunca hardcodeadas. La prueba de integración real usa credenciales del entorno (o STS asumiendo el rol per-Pi), NUNCA credenciales en el repo. `gitleaks` corre en CI; no introduzcas nada que lo dispare.
- **AWS:** cuenta `950639281773`, región `us-east-1`, prefijo `cam-counter-`. Buckets: media de producto = `cam-counter-media-950639281773`; tabla de eventos = `cam-counter-events`; tabla de registro = `cam-counter-devices`. Las claves de media siguen `media/{site_id}/{device_id}/{camera_id}/{yyyy}/{mm}/{dd}/{event_id}.{ext}`. Item de eventos DynamoDB: `PK=CAM#{site_id}#{device_id}#{camera_id}`, `SK=TS#{ts_event_ms:013d}#{event_id}`. Registro DynamoDB: `PK=DEVICE#{device_id}`. Estos recursos YA EXISTEN de verdad (aplicados por PR04) y están etiquetados con `default_tags` capitalizados `{Project="cam-counter", ManagedBy="terraform", Env="prod"}` MÁS los tags lógicos en minúscula `project=cam-counter` / `managed_by=mad-runner` (la clave `managed_by` SIEMPRE en minúscula; nunca `ManagedBy="mad-runner"`).
- **Testing:** todo el conteo/store/clip/sync se testea en x86 CI vía `DummyDetector` (sin Hailo), fuente fake, y stubs de AWS. La prueba de integración real contra AWS es ADICIONAL, gated por credenciales (skip sin ellas; PASS real exigido en el runner con credenciales). Los comportamientos de hardware real (Hailo) van en un checklist de smoke EN-PI separado, NUNCA como gate de CI.

---

## 5) Rama base y rama nueva

- **Rama base (de la que ramificas):** `feat/09-fastapi-react-ui` (la rama del PR del que depende este, PR09). Es una pila de PRs apilados: este PR ramifica sobre la rama anterior, NO sobre `main`. Recuerda además que PR10 DEPENDE de que PR04 ya haya APLICADO sus recursos AWS (es lo que la prueba de integración valida).
- **Rama nueva a crear:** `feat/10-supervisor-sync-e2e`.

Procedimiento:
```bash
git fetch origin
# Asegúrate de partir de la rama del PR anterior:
git checkout feat/09-fastapi-react-ui || git checkout -b feat/09-fastapi-react-ui origin/feat/09-fastapi-react-ui
git pull --ff-only origin feat/09-fastapi-react-ui || true
git checkout -b feat/10-supervisor-sync-e2e
```
Si `feat/09-fastapi-react-ui` NO existe en remoto (porque los PRs previos aún no se subieron en este workspace pero su trabajo SÍ está presente localmente), parte de la rama local actual que contenga ese trabajo. NO ramifiques sobre `main` si eso pierde el trabajo de PR01-PR09. Confirma con `git log --oneline -15` que la base tiene `v1/`, `cam_counter_edge` y `v1/api` antes de empezar.

**Política de merge de la pila (documéntala en el cuerpo del PR):** el PR se abre con `base = feat/09-fastapi-react-ui` para que el diff quede limpio y revisable de abajo hacia arriba. NO uses squash en esta pila (rompe las bases apiladas); merge-commit o rebase-merge.

---

## 6) Pasos sugeridos

1. **Reconocimiento:** lee la estructura real. `ls v1/edge/cam_counter_edge/`, `ls v1/api/`, `ls v1/ui/`, `cat v1/edge/pyproject.toml` (o setup), `ls contracts/`, `cat v1/api/schemas.py` (firmas de `CrossingEvent` y `DeviceInfo`), y el unit `hailo-personas.service` para imitar su patrón de render de ruta. Confirma el nombre exacto de los módulos de tracker/counter/store/config/clip y sus APIs públicas. Lee los outputs de `terraform/environments/prod` para obtener los nombres/ARN EXACTOS del bucket de media, la tabla de eventos y el rol IAM per-Pi (`edge_role_arn`); no hardcodees si puedes leerlos.
2. **`app.py` supervisor:** implementa `CameraPipeline` (un hilo o pequeño grupo de hilos por cámara con colas `maxsize=2` drop-old) y el supervisor que crea UN VDevice compartido y un `threading.Lock` corto solo alrededor de `detector.infer()`. Reinicio individual de pipeline caído (vigila excepciones por-pipeline y relanza el hilo sin tumbar al resto). Para CI usa `DummyDetector` (sin VDevice real). Mantén un registro por-cámara de `frames_processed` y `last_inference_ts` para `/healthz`.
3. **`/healthz` producto:** sirve JSON con métricas por-cámara y estado agregado. Diseña para que un test pueda forzar "frames=0" y observar estado degradado/503.
4. **`sync.py`:** clase/worker con bucle de drenaje: query `synced=0` -> subir clip (If-None-Match) -> conditional-put evento -> marcar `synced=1`; heartbeat por `UpdateItem`. Backoff acotado. Cliente AWS inyectable (factory/parámetro) — clave para que CI use stubs y la integración real use boto3 real. Maneja "red caída" (excepción de red) sin perder eventos ni duplicar.
5. **Tests Python (fakes)** en `v1/edge/tests/`:
   - `test_supervisor.py`: un pipeline por cámara sobre VDevice compartido (mock), lock corto, reinicio de pipeline caído, `/healthz` con `frames_processed`/`last_inference_ts`, y caso "health 200 pero frames=0 -> degradado".
   - `test_sync.py`: subida idempotente de clip (reintento no pisa parcial), conditional-put idempotente (duplicado rechazado -> `synced=1` sin error), heartbeat solo por `UpdateItem`, ASERCIÓN de que el sync NUNCA llama a un read del registro para decidir trabajo.
   - Test offline/backlog: stub de red "caída luego arriba" -> el conteo persiste durante el corte, el backlog drena al reconectar, CERO duplicados (mismo `event_id`). Nombra los tests para que casen con `-k 'idempot or sync_retry or offline or backlog'`.
6. **Test de integración REAL contra AWS** en `v1/edge/tests/test_sync_integration_aws.py` (marker `@pytest.mark.integration_aws`, gated por credenciales):
   - Resuelve nombres de recursos: bucket de media y tabla de eventos desde entorno/outputs de terraform (valores por defecto coherentes con §4 si no hay otra fuente); el ARN del rol per-Pi desde el output `edge_role_arn` de PR04 (`terraform -chdir=terraform/environments/prod output -raw edge_role_arn`) o desde `CAMCOUNTER_EDGE_ROLE_ARN`. NO reconstruyas el ARN desde placeholders.
   - Si está habilitada la integración (`CAMCOUNTER_AWS_INTEGRATION=1`) y hay credenciales: asume el rol per-Pi (`sts:AssumeRole`) y construye un cliente boto3 con las credenciales temporales. Si NO se resuelve el ARN o el `AssumeRole` falla, FALLA con mensaje claro atribuido a configuración de PR04 (no degrades en silencio); solo usa las credenciales del entorno si el orquestador activa explícitamente el fallback (`CAMCOUNTER_AWS_INTEGRATION_ALLOW_ENV_CREDS=1`), con TODO documentado.
   - Inyecta ese cliente real en el worker `sync.py` y ejerce: (a) PutObject real del clip bajo `media/_selftest/...`; (b) conditional-put real del CrossingEvent; (c) reintento del MISMO `event_id` -> ConditionalCheckFailed -> tratado como éxito idempotente, ASERTANDO que no se duplicó; (d) GetItem + HeadObject de read-back; (e) CLEANUP en `finally`/fixture (DeleteItem + DeleteObject), garantizado pase lo que pase.
   - Gating estricto (F8): si no hay credenciales -> `pytest.skip(...)` con aviso claro. Si `CAMCOUNTER_AWS_INTEGRATION=1` Y hay credenciales -> un SKIP de la suite cuenta como FALLO; implementa el guardián (en `conftest.py` o un hook `pytest_sessionfinish`) que, con el flag activo + credenciales, falla la sesión si la suite `integration_aws` no produjo al menos un PASS. Registra el marker en `pyproject.toml`/`pytest.ini` para evitar warnings de marker desconocido.
7. **Playwright E2E** en `v1/ui/e2e/`: levanta FastAPI con `CAMCOUNTER_FAKE_SOURCE=1`, sirve la SPA, y prueba: cargar UI -> arrastrar línea -> flip de sentido -> guardar -> recargar (persiste) -> cruce guionizado incrementa el contador en vivo por WS. Configura `playwright.config.ts` para headless (chromium) y añade `test:e2e` a los scripts de `v1/ui/package.json` si no existe.
8. **systemd:** crea `v1/systemd/cam-counter-edge.service` con placeholder de ruta (no path absoluto fijo), `Restart=always`, coexistiendo con `hailo-personas`. Documenta que rollback = re-habilitar `hailo-personas`.
9. **CI:** extiende `.github/workflows/` con un job path-filtered que corra ruff/mypy + tsc + vite build + pytest + `playwright install --with-deps chromium` + Playwright headless + el test de sync offline. Sin hardware. La suite `integration_aws` se EXCLUYE por defecto en CI sin OIDC (skip por gating de credenciales); no rompe el build. `actions/checkout` con `fetch-depth: 0` si toca a `version.py`.
10. **Doc smoke EN-PI:** añade/edita el doc de checklist de hardware con el presupuesto del VDevice y los chequeos manuales. Añade además una nota breve de cómo correr la prueba de integración real contra AWS (flag de env, que asume el rol per-Pi, que limpia sus datos de prueba, el comando estricto del runner que distingue skip-sin-credenciales de skip-indebido-con-credenciales, y que el teardown de infra es responsabilidad del runner MAD vía `terraform destroy`/script de teardown — recursos de bajo costo: S3 + DynamoDB on-demand).
11. **Corre la prueba de integración real contra AWS en el entorno del runner** (que SÍ tiene credenciales) y CONFIRMA que pasa: PutObject + conditional-put + idempotencia (sin duplicado) + read-back + cleanup, asumiendo el rol per-Pi. Con credenciales, un SKIP NO cuenta: el DoD exige PASS real.
12. Corre TODA la verificación de la sección 8 localmente y deja el árbol verde.

---

## 7) Definition of Done (objetivo y verificable)

- [ ] `cam-counter-edge` corre UN pipeline por cámara sobre un único Hailo VDevice COMPARTIDO, con lock corto alrededor de `infer()`, reinicia individualmente un pipeline caído, y expone salud DE PRODUCTO por-cámara en `/healthz` (incluye `frames_processed` y `last_inference_ts`; distingue "200 pero frames=0" como degradado).
- [ ] `sync.py` sube clips (retry-safe, sin pisar parciales) y hace conditional-put de eventos idempotentemente, actualiza heartbeat del registro SOLO vía `UpdateItem`, y NUNCA lee el registro para decidir qué actualizar; un test offline->online (fakes) demuestra que el backlog drena SIN duplicados. La tolerancia offline manifest-no-registry se conserva.
- [ ] **La prueba de INTEGRACIÓN end-to-end contra AWS REAL pasa contra los recursos ya desplegados por PR04** (bucket de media + tabla de eventos): sube un clip real a `media/_selftest/...` (PutObject), escribe un CrossingEvent real con conditional put, DEMUESTRA idempotencia (reintentar el mismo `event_id` NO duplica — conditional put rechazado), hace read-back (GetItem/HeadObject) y LIMPIA los datos de prueba (DeleteItem/DeleteObject) al final. La prueba asume el ROL per-Pi de PR04 (validando el IAM acotado); si el ARN no se resuelve o el AssumeRole falla con la integración habilitada, FALLA con mensaje claro (config de PR04), NUNCA degrada en silencio. La prueba degrada a SKIP si no hay credenciales (no rompe CI sin OIDC); pero EN EL RUNNER (con `CAMCOUNTER_AWS_INTEGRATION=1` + credenciales) DEBE pasar contra AWS REAL y un SKIP cuenta como FALLO (gating estricto), y el caso de reintento DEBE demostrar que no se duplica.
- [ ] Se mantienen TODAS las pruebas con `DummyDetector`/fakes y stubs de AWS previas (la de integración real es ADICIONAL).
- [ ] El conteo y la persistencia continúan con la red caída (test offline explícito); `cam-counter-edge.service` COEXISTE con `hailo-personas` (el servicio legacy no se elimina; la doc declara rollback = re-habilitar `hailo-personas`).
- [ ] La suite Playwright E2E pasa headless contra FastAPI + fuente fake cubriendo: configurar-línea-y-ver-el-conteo, persistencia tras recarga, e incremento en vivo por WS.
- [ ] El gate de CI corre ruff/mypy + tsc + vite build + pytest + Playwright + test de sync offline, SIN hardware; la suite `integration_aws` se SALTA limpiamente en CI sin OIDC (skip con aviso), sin romper el build.
- [ ] CERO secretos nuevos en el repo; este PR NO ejecuta `terraform apply` ni crea recursos (consume los de PR04); `rtsp-enable/` y el servicio `hailo-personas` intactos; sin recursos Terraform nuevos. La prueba de integración no deja datos residuales en AWS.

---

## 8) Verificación (comandos concretos)

Ejecuta y deja TODO en verde antes de abrir el PR. Ajusta rutas si el reconocimiento mostró nombres distintos, pero estos comandos son los esperados:

```bash
# Tests de supervisor y sync (sin hardware, DummyDetector + stubs AWS)
cd v1/edge && python3 -m pip install -e . && python3 -m pytest -q tests/test_supervisor.py tests/test_sync.py

# Subconjunto de idempotencia / offline / backlog
cd v1/edge && python3 -m pytest -q -k 'idempot or sync_retry or offline or backlog'

# Prueba de integración REAL contra AWS (recursos de PR04). En el RUNNER (con credenciales) DEBE pasar;
# sin credenciales DEBE saltarse limpiamente (skip), nunca fallar.
# Gating ESTRICTO (F8): con el flag activo + credenciales, un SKIP cuenta como FALLO.
# El guardián de conftest/pytest_sessionfinish convierte "0 passed / N skipped" en exit-code != 0
# cuando CAMCOUNTER_AWS_INTEGRATION=1 Y hay credenciales resolubles.
cd v1/edge && CAMCOUNTER_AWS_INTEGRATION=1 python3 -m pytest -q -m integration_aws -rs
# Sin credenciales (simula CI sin OIDC): debe terminar en SKIP, no en FAIL.
cd v1/edge && python3 -m pytest -q -m integration_aws -rs

# Lint/typing del borde
cd v1/edge && python3 -m ruff check . && python3 -m mypy cam_counter_edge

# UI: typecheck + build + E2E headless con fuente fake
cd v1/ui && npm ci && npx tsc --noEmit && npm run build
cd v1/ui && npx playwright install --with-deps chromium && CAMCOUNTER_FAKE_SOURCE=1 npm run test:e2e

# Coexistencia/rollback documentado: alguno de estos debe casar
grep -rq 'rollback.*hailo-personas' docs/ v1/ || grep -q 'hailo-personas' v1/systemd/cam-counter-edge.service

# Sanidad: el servicio legacy y rtsp-enable siguen presentes e intactos
test -f v1/rtsp-enable/start_detection.sh || test -f rtsp-enable/start_detection.sh
ls v1/systemd/hailo-personas.service || ls systemd/hailo-personas.service

# Sin secretos: si gitleaks está disponible, no debe haber hallazgos en el diff
git diff --staged | grep -iE 'aws_secret|password=|RWCHBY|BEGIN .*PRIVATE KEY' && echo 'SECRETO DETECTADO - ABORTAR' || echo 'sin secretos en el diff'

# Confirmación post-integración (en el runner): no quedaron datos de prueba residuales en AWS.
# (La prueba limpia sola; esto es una verificación de sanidad opcional con las credenciales del runner.)
aws s3 ls s3://cam-counter-media-950639281773/media/_selftest/ --recursive | head || echo 'prefijo selftest vacío (esperado tras cleanup)'
```

Si algún comando falla por dependencias del entorno (p.ej. `mypy`/`ruff` no instalados), instálalos en un venv local del runner; NO comprometas el verde por falta de herramienta. Para la prueba de integración real, recuerda: en el RUNNER con credenciales DEBE PASAR (no basta con skip; el modo estricto convierte un skip indebido en fallo); el skip solo es aceptable cuando NO hay credenciales (CI sin OIDC).

---

## 9) Entrega

1. Commit(s) atómicos y mensajes claros en español. Termina cada mensaje de commit con:
   ```
   Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
   ```
2. Push de la rama `feat/10-supervisor-sync-e2e`.
3. Abre el Pull Request con `gh`, **base = `feat/09-fastapi-react-ui`** (apilado sobre el PR anterior), título y cuerpo claros. El cuerpo debe explicar QUÉ se hizo, POR QUÉ, y CÓMO verificarlo (incluye los comandos de la sección 8), más una nota de la política de merge de la pila (no-squash), de que NO hay cutover (rollback = re-habilitar `hailo-personas`), y de que la prueba de integración corrió contra AWS REAL (recursos de PR04), demostró idempotencia y limpió sus datos. Termina el cuerpo del PR con:
   ```
   🤖 Generated with [Claude Code](https://claude.com/claude-code)
   ```
   Comando sugerido:
   ```bash
   gh pr create --base feat/09-fastapi-react-ui --head feat/10-supervisor-sync-e2e \
     --title "edge+ui: supervisor multi-cámara (Hailo VDevice compartido) + cloud-sync offline-tolerante + Playwright E2E + integración REAL contra AWS (PR04)" \
     --body-file <(cat <<'EOF'
   ## Qué
   - Supervisor multi-cámara `cam_counter_edge/app.py` (entrypoint `cam-counter-edge`): un pipeline por cámara sobre UN Hailo VDevice compartido (lock corto en infer), reinicio individual de pipeline caído, `/healthz` con salud de producto por-cámara (frames_processed, last_inference_ts).
   - Worker `cam_counter_edge/sync.py`: drena eventos `synced=0`, sube clips retry-safe (If-None-Match), conditional-put idempotente de eventos, heartbeat al registro SOLO con UpdateItem (nunca lee el registro para decidir). Tolerante a offline (manifest-no-registry).
   - Prueba de INTEGRACIÓN end-to-end contra AWS REAL (recursos de PR04): PutObject real a `media/_selftest/...`, conditional-put real del CrossingEvent, demostración de idempotencia (reintentar el mismo event_id NO duplica), read-back (GetItem/HeadObject) y CLEANUP de los datos de prueba (DeleteItem/DeleteObject). Asume el rol per-Pi de PR04 (output `edge_role_arn`) para validar el IAM acotado; con la integración habilitada, fallo de AssumeRole/ARN no resuelto = FALLO claro (config de PR04), nunca degradación silenciosa. Degrada a SKIP sin credenciales (CI sin OIDC no se rompe); con credenciales un SKIP cuenta como FALLO (gating estricto).
   - Se mantienen las pruebas con DummyDetector/fakes y stubs de AWS (la de integración real es ADICIONAL).
   - Servicio `v1/systemd/cam-counter-edge.service` que COEXISTE con `hailo-personas` (sin cutover).
   - Suite Playwright E2E en `v1/ui/e2e/` contra FastAPI + fuente fake.
   - Job de CI: ruff/mypy + tsc + vite build + pytest + Playwright headless + test de sync offline (sin hardware); la suite integration_aws se salta limpiamente sin OIDC.

   ## Por qué
   Cierra el lazo multi-cámara y la sincronización a la nube edge-first/offline-tolerante, con cobertura E2E de la UI, y DEMUESTRA contra recursos AWS REALES (no mocks) que el bloque de sync funciona de verdad: contrato A (event_id determinista + conditional put) validado contra DynamoDB real, idempotencia probada por reintento. Sin tocar el servicio legacy ni crear infraestructura aquí (los recursos los aplicó el runner MAD en PR04).

   ## Cómo verificar
   (ver comandos en la descripción / sección de verificación; incluye la corrida de integration_aws con credenciales del runner en modo estricto y su skip sin credenciales)

   ## Notas de la pila y despliegue
   - PR apilado: base = `feat/09-fastapi-react-ui`. No usar squash al mergear (rompe bases apiladas); usar merge-commit o rebase-merge, bottom-up.
   - DOS actores: el runner MAD aplica infra de forma autónoma en los PRs de infra; GitHub Actions CI permanece plan-only vía OIDC y NUNCA hace apply de infra. ESTE PR no aplica nada: consume y valida los recursos REALES de PR04.
   - SIN cutover: `hailo-personas` sigue siendo el servicio activo. Rollback = re-habilitar `hailo-personas`.
   - Sin recursos Terraform nuevos; `rtsp-enable/` intacto; cero secretos. La prueba de integración no deja datos residuales en AWS (cleanup garantizado). Teardown de infra = `terraform destroy`/script de teardown por el runner MAD (recursos de bajo costo: S3 + DynamoDB on-demand).

   🤖 Generated with [Claude Code](https://claude.com/claude-code)
   EOF
   )
   ```
4. **DEVUELVE como salida final la URL del Pull Request creado.**

---

## Recordatorios finales (no negociables)
- CERO secretos en el repo. Nada de credenciales, contraseñas, tokens ni el código/clave de la cámara en ningún archivo. Credenciales por OIDC (CI plan) / STS por-dispositivo / entorno del runner MAD únicamente.
- DOS actores: runner MAD aplica infra de forma autónoma (en los PRs de infra, NO en este); CI sigue plan-only vía OIDC y NUNCA hace apply de infra. PR10 no aplica nada: consume y VALIDA contra los recursos REALES de PR04.
- NO toques `rtsp-enable/` ni el servicio `hailo-personas`; deben seguir ejecutables. Rollback = re-habilitar `hailo-personas`.
- El conteo y la persistencia local NUNCA dependen de la red. El sync es desacoplado, idempotente y tolerante a offline (manifest-no-registry).
- La prueba de integración real DEBE pasar contra AWS REAL en el runner (PutObject + conditional-put + idempotencia sin duplicado + read-back + cleanup), asumiendo el rol per-Pi (ARN desde el output `edge_role_arn` de PR04; AssumeRole fallido con integración habilitada = FALLO claro, nunca silencioso). Sin credenciales degrada a SKIP; con credenciales un SKIP cuenta como FALLO (modo estricto). Mantén además las pruebas con fakes.
- UN VDevice Hailo compartido para N cámaras, lock corto en `infer()`. Toda la lógica de conteo testeable en x86 con `DummyDetector` + stubs AWS, sin hardware.
- Coordenadas normalizadas 0..1; slugs validados antes de construir claves; `event_id` determinista para idempotencia.
- PR pequeño-mediano, apilado sobre `feat/09-fastapi-react-ui`, con verificación automática fuerte y verde antes de abrirlo.
