# edge: configuración de línea por cámara en SQLite + hot-reload (config_version) + buffer/grabador de clips + cola clip_uploads (claves a prueba de colisiones)

# PR08 — Edge: configuración de línea por cámara en SQLite + hot-reload + buffer/grabador de clips + cola `clip_uploads`

Eres un agente Claude CLI autónomo trabajando en un monorepo. Este prompt es TOTALMENTE AUTOCONTENIDO: contiene todo lo que necesitas. No asumas acceso a ninguna conversación previa.

---

## 1) Contexto

**Repositorio:** `github.com/jlsaco/cam-counter` (monorepo, todo vive aquí). Cuenta AWS `950639281773`, región `us-east-1`. El runner donde te ejecutas tiene credenciales AWS amplias y permisos de GitHub Actions, pero **este PR NO crea ni modifica recursos en AWS** (es puro código de borde + tests en x86, sin hardware).

**Producto:** `cam-counter` es un producto EDGE-FIRST de **conteo de personas que cruzan una LÍNEA-UMBRAL configurable**, en tiempo real y en el borde (Raspberry Pi 5 + acelerador Hailo-8 + cámara RTSP). Dos sentidos independientes (`in`/`out`). Al cruzar una persona se captura un CLIP (buffer de N segundos alrededor del evento) que luego se sube a S3, y el evento se registra en SQLite (y más tarde se sincroniza a la nube). La UI local (LAN, servida desde el Pi) permite configurar la línea y ver contadores. **Multi-cámara y multi-sitio desde la v1**: todo indexado por `site_id`/`device_id`/`camera_id`.

**El sistema de borde vive bajo `v1/edge/`** y es un paquete Python instalable llamado **`cam_counter_edge`** (NO uses el nombre genérico `counter`). La estructura relevante que ya existe al partir de la rama base de este PR es:

```
v1/edge/
  pyproject.toml                      # define el paquete cam_counter_edge (instalable con pip install -e .)
  cam_counter_edge/
    __init__.py
    types.py                          # dataclasses/modelos compartidos del borde (BBox, Track, LineConfig, CrossingEvent, etc.)
    slugs.py                          # validación de slugs (regex) y construcción de claves S3/DynamoDB
    detector.py                       # Detector(Hailo) + interfaz
    dummy.py                          # DummyDetector (sin Hailo, para CI en x86)
    tracker.py                        # tracker centroid+IoU con ids estables
    counter.py                        # LineCounter (half-plane + histéresis + idempotencia por track)
    store.py                          # capa de persistencia SQLite (WAL): events, counters, crossing_seq, etc.
  tests/
    test_detector.py
    test_tracker.py
    test_counter.py
    test_store.py
```

> IMPORTANTE: La rama base de este PR es la del PR anterior (PR07). Si al hacer checkout encuentras que algún archivo descrito arriba tiene un nombre o forma ligeramente distinta, **adáptate a lo que exista realmente en el árbol** (lee los archivos antes de tocarlos). No inventes contratos nuevos: reutiliza los tipos, la conexión SQLite, la validación de slugs y la firma de `LineCounter` que ya están en el repo.

### Convenciones transversales que DEBES respetar (ya vigentes en el repo)

- **Identificadores:** `site_id`, `device_id`, `camera_id` son slugs ASCII en minúsculas que cumplen `^[a-z0-9][a-z0-9-]{1,62}$` (NO `#`, NO `/`, porque `#` delimita claves compuestas de DynamoDB y `/` delimita rutas S3). `camera_id` es globalmente único = `'{device_id}-cam{N}'`. **La validación regex se aplica ANTES de construir cualquier clave.** Existe `cam_counter_edge/slugs.py` con una función de validación (p.ej. `validate_slug(s)` que lanza excepción si es inválido) y una para construir claves; úsala — no reimplementes el regex.
- **Coordenadas:** TODA la geometría (salida NMS de Hailo, extremos de la línea, overlay, anotación de clips) usa floats NORMALIZADOS `0..1` relativos al frame de inferencia ORIGINAL, origen arriba-izquierda. Los píxeles nunca aparecen en ningún contrato almacenado. El orden de bbox del sistema es `[xmin, ymin, xmax, ymax]`.
- **`event_id`** es DETERMINISTA = sha1 en hex minúsculas de `site_id|device_id|camera_id|track_id|crossing_seq`. `crossing_seq` es un **contador monótono PERSISTIDO POR CÁMARA** (no por track, no reiniciable) — esto ya lo gestiona `store.py` desde PR07 (función tipo `next_crossing_seq(camera_id)` o equivalente). Documenta sha1 como NO criptográfico (solo dedupe).
- **SQLite es el único contrato de persistencia del borde:** modo WAL, `busy_timeout`, transacciones cortas, **una sola conexión escritora**, migraciones vía `PRAGMA user_version`. La conexión y el patrón de migración ya existen en `store.py`; sigue exactamente ese patrón al añadir tablas/columnas (incrementa `user_version` y añade el paso de migración correspondiente).
- **Tres buckets S3 distintos, nunca confundidos** — en este PR SOLO se usa el **nombre/plantilla** del bucket de media (no se accede a S3):
  - `cam-counter-rpi-artifacts-950639281773` = backup de binarios EXISTENTE, RESERVADO, no tocar.
  - `cam-counter-media-950639281773` = media del producto (clips/gifs/snapshots). **Plantilla de clave:** `media/{site_id}/{device_id}/{camera_id}/{yyyy}/{mm}/{dd}/{event_id}.{ext}`.
  - `cam-counter-fleet-releases-950639281773` = artefactos OTA + manifiestos (no aplica aquí).
- **Cero secretos en git.** Prohibido cualquier credencial, contraseña, token o clave de cámara (p.ej. NO escribas la cadena `RWCHBY` ni ninguna otra credencial) en código, tests, fixtures o documentación. Las credenciales van por env/SSM/fichero en `shared/`. Si necesitas un valor de configuración, usa un placeholder o variable de entorno.
- **El conteo NUNCA debe bloquearse en IO o red.** La captura/inferencia/tracking/conteo es el camino crítico de baja latencia; la grabación de clips y el encolado de subidas son **asíncronos/encolados**.

---

## 2) Objetivo

Añadir al paquete de borde tres capacidades, todas testeables en x86 sin Hailo/cámara/red:

1. **Configuración de línea por cámara en SQLite con `config_version` monótono y concurrencia optimista**, más **hot-reload** de la geometría del `LineCounter` sin reiniciar el proceso.
2. **Buffer circular de clips + grabador asíncrono** que ensambla pre-roll + post-roll en un MP4 (con fallback a GIF), escribe a fichero temporal y hace rename atómico, y **encola una fila `pending` en `clip_uploads`** con la clave S3 planificada (construida desde la plantilla de media SOLO tras validar slugs).
3. **Tabla `clip_uploads`** (cola de subidas) en SQLite.

El conteo no debe bloquearse nunca por la grabación ni por la futura subida.

---

## 3) Alcance y NO-alcance

### Dentro de alcance (lo que SÍ haces)
- **`store.py`** (extender, sin romper lo existente):
  - Tabla **`camera_config`**: al menos `camera_id` (PK), `site_id`, `device_id`, geometría de la línea (extremos normalizados, p.ej. `ax, ay, bx, by` floats `0..1`), `positive_side` (+1/-1), `positive_label` (texto, p.ej. `'subieron'`), `negative_label` (texto, p.ej. `'bajaron'`), `config_version` (int monótono), `updated_at`. Reutiliza el modelo `LineConfig` de `types.py` si ya define estos campos.
  - Funciones: `get_line_config(camera_id) -> LineConfig | None`, `set_line_config(camera_id, config, expected_version) -> nuevo_config_version` con **concurrencia optimista**: si `expected_version` no coincide con el `config_version` actual en DB, **rechaza** (lanza una excepción específica, p.ej. `StaleConfigVersionError`, o un valor de error claramente distinguible). En éxito, **incrementa `config_version`** monótonamente y actualiza `updated_at`.
  - `get_config_version(camera_id) -> int` barato (lectura de una sola columna), pensado para llamarse una vez por frame.
  - Tabla **`clip_uploads`**: al menos `id` (autoincrement), `event_id`, `camera_id`, `local_path` (ruta del MP4/GIF temporal o final en `shared/`), `s3_key_planned` (clave de media construida desde la plantilla con slugs validados), `status` (`'pending'|'uploading'|'uploaded'|'failed'`), `attempts` (int, default 0), `created_at`, `updated_at`. Función `enqueue_clip_upload(...)` que inserta una fila `pending`.
  - Migración: incrementa `PRAGMA user_version` y añade el/los `CREATE TABLE` en el paso de migración correspondiente, siguiendo el patrón ya presente.
- **`config.py`** (NUEVO): clase **`ConfigWatcher`** que mantiene en memoria el `config_version` y la geometría actuales para una cámara, expone un método para llamarse **una vez por frame** que lee `get_config_version(camera_id)` (lectura barata, lock-free desde el punto de vista del camino de conteo) y, si cambió respecto al cacheado, recarga `get_line_config(...)` y **actualiza la geometría del `LineCounter` en caliente, sin reiniciar el proceso**. El `LineCounter` debe exponer (o tú añades) un método para reconfigurar su geometría (p.ej. `set_line(config)` / `reconfigure(config)`) que conserve el estado de idempotencia por track si es seguro, o lo reinicie de forma documentada. Mantén el cambio mínimo en `counter.py`.
- **`clip.py`** (NUEVO):
  - Buffer circular por cámara: un `collections.deque(maxlen=...)` de JPEGs ya codificados (pre-roll), con timestamps, dimensionado por una duración objetivo y FPS (parámetros de configuración).
  - Un **grabador asíncrono** (hilo o `asyncio`/cola de trabajo — elige uno y sé consistente) que, al recibir una orden de "cruce en T", toma el pre-roll del buffer + sigue capturando post-roll hasta completar la ventana, ensambla un **MP4** (usando `imageio-ffmpeg` o `cv2.VideoWriter`; añade `imageio-ffmpeg` a las deps de `pyproject.toml` si lo usas) con **fallback a GIF** si el encoder MP4 no está disponible. Escribe a **fichero temporal** (sufijo `.part` o ruta temp) y luego **rename atómico** (`os.replace`) a la ruta final dentro de `shared/`.
  - Tras escribir el clip, **encola una fila `pending` en `clip_uploads`** con `s3_key_planned` = clave de media construida desde la plantilla `media/{site_id}/{device_id}/{camera_id}/{yyyy}/{mm}/{dd}/{event_id}.{ext}` (la extensión coincide con el formato real escrito: `.mp4` o `.gif`), usando los slugs **ya validados**. La fecha `yyyy/mm/dd` se deriva del timestamp del evento en UTC.
  - **El conteo nunca bloquea:** la orden de grabar se entrega de forma no bloqueante (cola/evento); ninguna llamada de red ni IO pesada ocurre en el camino de conteo.
- **Documentación (en docstrings y/o un breve `v1/edge/cam_counter_edge/clip.py` header, o `v1/edge/README.md` si ya existe):** la subida real (que implementará PR10) DEBE ser idempotente y a prueba de reintentos: usar `PutObject` con `If-None-Match: *` **o** subir a una clave temporal y copiar, de modo que un reintento de subida del mismo `event_id` NUNCA sobrescriba un objeto parcial. Documenta también que `sha1` del `event_id` es no-criptográfico (solo dedupe).
- **Tests** en `v1/edge/tests/`:
  - `test_config.py`: (a) `set_line_config` incrementa `config_version`; (b) un `set_line_config` con `expected_version` desactualizado es **rechazado** (stale); (c) `ConfigWatcher` recarga la geometría en caliente — alimenta una secuencia de bbox que cruza la línea original, luego cambia la línea vía `set_line_config`, vuelve a llamar al watcher, y asserta que el conteo **cambia** sin reiniciar (la nueva línea cuenta distinto que la vieja). Usa `DummyDetector` y/o secuencias sintéticas de bbox deterministas (sin Hailo).
  - `test_clip.py`: (a) un "cruce" produce un clip de la duración esperada (nº de frames ≈ pre+post · FPS, con tolerancia) escrito atómicamente (el fichero final existe y no queda `.part`); (b) tras el clip hay **exactamente una** fila `pending` en `clip_uploads` con `s3_key_planned` que coincide con la plantilla de media construida desde slugs validados; (c) la clave se construye SOLO desde slugs válidos — un slug inválido (`'#'`, `'/'`, mayúsculas, vacío, >63 chars) es **rechazado** antes de construir la clave.
  - Asegúrate de que los tests corran **sin red, sin Hailo, sin cámara**, de forma determinista y rápida (usa SQLite en fichero temporal o `:memory:` según lo que use el resto de tests; usa rutas temporales con `tmp_path` de pytest).

### Fuera de alcance (lo que NO tocas)
- **NO** crees ni modifiques recursos AWS, Terraform, ni hagas llamadas reales a S3/DynamoDB. La subida real de clips es de PR10.
- **NO** toques `rtsp-enable/` (utilidad Java + box64 que reactiva el RTSP de la cámara). No tiene relación con este PR.
- **NO** toques el servicio systemd `hailo-personas` ni su mecanismo de instalación.
- **NO** reescribas la lógica del tracker (`tracker.py`) ni del detector (`detector.py`/`dummy.py`) salvo lo mínimo imprescindible. Si necesitas exponer un setter de línea en `counter.py`, hazlo con el mínimo cambio.
- **NO** añadas API/UI (FastAPI/React) — eso es PR09.
- **NO** introduzcas ningún secreto, credencial o contraseña en el repo (ver Restricciones).
- **NO** modifiques los contratos compartidos (`event_id` determinista, `crossing_seq` monótono por cámara, normalización de coordenadas, regex de slugs); solo los CONSUMES.

---

## 4) Restricciones

- **Stack borde:** Python (3.11+ asumido por el resto del paquete). Sin dependencias pesadas innecesarias. Si usas `imageio-ffmpeg` para MP4, decláralo en `pyproject.toml`; provee fallback a GIF (p.ej. `imageio` o `Pillow`) y degrada limpio si el encoder MP4 no está disponible. `numpy`/`opencv-python` ya están en el stack del borde.
- **Edge-first / offline-tolerante:** todo persiste en SQLite local; nada en este PR requiere red. La grabación de clips y el encolado son locales.
- **Multi-cámara:** todas las estructuras (config, buffer de clips, `clip_uploads`) están indexadas por `camera_id` (y llevan `site_id`/`device_id`). El `ConfigWatcher` y el buffer son por-cámara.
- **Cero secretos en el repo.** Prohibido cualquier credencial/contraseña/token/clave de cámara en código, tests, fixtures o docs (incluida la cadena `RWCHBY`). Usa placeholders o variables de entorno. Un escaneo de secretos (gitleaks) corre en CI; tu diff debe pasarlo limpio.
- **Prefijos/identidad AWS:** prefijo `cam-counter-`, cuenta `950639281773`, región `us-east-1`. Bucket de media: `cam-counter-media-950639281773`. Estos valores, si aparecen, deben ser constantes/configurables, no credenciales.
- **El conteo nunca bloquea en IO/red.** Verificable: el camino crítico (frame -> detect -> track -> count) no hace ninguna llamada de red ni IO de fichero bloqueante; clip + subida son asíncronos/encolados.
- **Linting:** el código nuevo debe pasar `ruff check`.

---

## 5) Ramas

- **Rama base** (de la que partes y contra la que abrirás el PR): **`feat/07-edge-linecounter-store`** (la rama del PR07, del que este PR depende). Haz `git fetch` y `git checkout feat/07-edge-linecounter-store` antes de empezar. Si esa rama remota no existe aún en `origin`, detente y reporta el problema en lugar de partir de `main` (la pila de PRs está apilada y la base correcta es PR07).
- **Rama nueva a crear:** **`feat/08-edge-config-hotreload-clips`** (ramifica desde `feat/07-edge-linecounter-store`).

```bash
git fetch origin
git checkout feat/07-edge-linecounter-store
git pull --ff-only origin feat/07-edge-linecounter-store
git checkout -b feat/08-edge-config-hotreload-clips
```

---

## 6) Pasos sugeridos

1. **Lee el árbol existente** bajo `v1/edge/` (especialmente `store.py`, `types.py`, `slugs.py`, `counter.py`, `tracker.py`, `dummy.py`, `pyproject.toml` y los tests) para reutilizar tipos, la conexión SQLite WAL, el patrón de migración `PRAGMA user_version`, la validación de slugs y la firma del `LineCounter`. **No dupliques ni reinventes** lo que ya exista.
2. **Extiende `store.py`:** añade tablas `camera_config` y `clip_uploads` vía una nueva migración (incrementa `user_version`). Implementa `get_line_config`, `set_line_config` (con concurrencia optimista y rechazo de versión stale), `get_config_version`, `enqueue_clip_upload`. Mantén una sola conexión escritora y transacciones cortas.
3. **Crea `config.py`** con `ConfigWatcher`: chequeo barato por frame de `config_version` + recarga en caliente de la geometría del `LineCounter`. Si hace falta, añade en `counter.py` un setter mínimo de geometría.
4. **Crea `clip.py`:** deque de JPEGs (pre-roll) + grabador asíncrono (pre+post -> MP4 con fallback GIF) + escritura temp + `os.replace` atómico a `shared/` + `enqueue_clip_upload` con `s3_key_planned` desde slugs validados. Garantiza que el camino de conteo nunca bloquea.
5. **Documenta** (docstrings/header) la estrategia de subida idempotente a prueba de reintentos (`If-None-Match: *` o temp-key+copy) y la naturaleza no-criptográfica de sha1 para `event_id`.
6. **Escribe `tests/test_config.py` y `tests/test_clip.py`** cubriendo los DoD. Deterministas, sin red/Hailo/cámara, con `tmp_path`.
7. **Actualiza `pyproject.toml`** si añadiste deps (p.ej. `imageio-ffmpeg`).
8. **Ejecuta la verificación** (sección 8). Itera hasta verde.
9. **Commit, push y abre el PR** (sección 9).

---

## 7) Definition of Done (objetivo y verificable)

1. `set_line_config` incrementa `config_version`, **rechaza** una versión stale (concurrencia optimista), y `ConfigWatcher` recarga la geometría **sin reiniciar el proceso** — un test asserta que el conteo cambia tras cambiar la línea en caliente.
2. `clip.py` bufferiza JPEGs, ensambla pre+post en un cruce, escribe temp y hace **rename atómico**, y encola **una** fila `pending` en `clip_uploads` con `s3_key_planned` que coincide con la plantilla de media `media/{site_id}/{device_id}/{camera_id}/{yyyy}/{mm}/{dd}/{event_id}.{ext}` construida desde **slugs validados**.
3. El camino de conteo **no hace ninguna llamada de red/IO bloqueante** (clip + subida son asíncronos/encolados); la documentación especifica subida a prueba de reintentos (`If-None-Match`/temp-copy).
4. Un slug inválido (`'#'`, `'/'`, mayúsculas, vacío, >63 chars) es rechazado **antes** de construir cualquier clave (test explícito).
5. La nueva migración SQLite incrementa `PRAGMA user_version` y no rompe las migraciones/tests existentes.
6. `ruff check` pasa sobre `config.py` y `clip.py`; `pytest` pasa en todo `v1/edge/`.
7. **Cero secretos** en el diff (gitleaks limpio); ningún valor de credencial/contraseña/clave de cámara introducido.

---

## 8) Verificación (comandos concretos)

Ejecuta exactamente estos comandos desde la raíz del repo y asegúrate de que TODOS pasan (verde):

```bash
# Instalar el paquete de borde en modo editable (incluye deps de test)
python3 -m pip install -e "v1/edge[dev]" 2>/dev/null || python3 -m pip install -e v1/edge

# Tests específicos de este PR
cd v1/edge && python3 -m pytest -q tests/test_config.py tests/test_clip.py

# Tests por palabra clave (los nuevos comportamientos)
cd v1/edge && python3 -m pytest -q -k 'hot_reload or stale_version or clip_duration or s3_key'

# Lint del código nuevo
cd v1/edge && ruff check cam_counter_edge/config.py cam_counter_edge/clip.py

# Suite completa del borde (no debe romperse nada de PRs anteriores)
cd v1/edge && python3 -m pytest -q
```

Además, verifica manualmente que **no hay secretos** en tu diff (p.ej. revisa el `git diff` y, si `gitleaks` está disponible, corre `gitleaks detect --no-git` o `gitleaks protect --staged` sobre el árbol de trabajo y confirma 0 hallazgos). NO debe aparecer ninguna credencial, contraseña ni clave de cámara.

> Nota: usa rutas ABSOLUTAS para `cd` o invoca pytest con `python3 -m pytest v1/edge/tests/...` desde la raíz, ya que el directorio de trabajo puede reiniciarse entre comandos.

---

## 9) Entrega: abrir el Pull Request (apilado)

1. Asegúrate de haber commiteado todo en `feat/08-edge-config-hotreload-clips`. El/los mensajes de commit deben terminar con:
   ```
   Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
   ```
2. Sube la rama: `git push -u origin feat/08-edge-config-hotreload-clips`.
3. Abre el PR con `gh`, con **base = la rama del PR anterior** (`feat/07-edge-linecounter-store`), NO `main` (es una pila de PRs apilados):
   ```bash
   gh pr create \
     --base feat/07-edge-linecounter-store \
     --head feat/08-edge-config-hotreload-clips \
     --title "PR08 — edge: config de línea por cámara + hot-reload + buffer/grabador de clips + cola clip_uploads" \
     --body "<cuerpo claro: QUÉ / POR QUÉ / CÓMO VERIFICAR>"
   ```
4. **Cuerpo del PR** (claro y conciso): 
   - **Qué:** tablas `camera_config` (config_version monótono + concurrencia optimista) y `clip_uploads` en SQLite; `config.py` `ConfigWatcher` con hot-reload de la línea sin reiniciar; `clip.py` buffer circular de JPEGs + grabador asíncrono pre+post -> MP4 (fallback GIF) con escritura temp + rename atómico y encolado `pending` con `s3_key_planned` desde slugs validados.
   - **Por qué:** habilita configurar la línea en caliente y capturar clips alrededor de cada cruce sin bloquear el camino crítico de conteo; prepara la cola de subida idempotente para PR10.
   - **Cómo verificar:** pega los comandos de la sección 8 y resume qué cubren los tests (hot-reload cambia el conteo, rechazo de versión stale, duración de clip, `s3_key_planned` correcta, rechazo de slug inválido).
   - Nota explícita: **sin recursos AWS, sin red, sin Hailo**; subida real diferida a PR10 (documentada como idempotente con `If-None-Match`/temp-copy).
   - Termina el cuerpo del PR con:
     ```
     🤖 Generated with [Claude Code](https://claude.com/claude-code)
     ```
5. **DEVUELVE como salida final la URL del PR** creado (la que imprime `gh pr create`).

---

## Recordatorios finales (prohibiciones absolutas)
- **PROHIBIDO** introducir cualquier secreto en el repo: credenciales AWS de larga vida, contraseñas, tokens, o la clave/código de la cámara (NO escribas `RWCHBY` ni similar). Usa env/SSM/placeholders.
- **PROHIBIDO** crear/modificar recursos AWS o Terraform en este PR.
- **PROHIBIDO** tocar `rtsp-enable/`, el servicio `hailo-personas`, o reescribir el tracker/detector más allá de lo mínimo.
- **OBLIGATORIO** que todos los tests corran en x86 sin hardware, deterministas, y que el conteo no bloquee nunca en IO/red.
- Si la rama base `feat/07-edge-linecounter-store` no existe en `origin`, **detente y repórtalo**; no partas de `main`.
