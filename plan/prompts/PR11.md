# PR11 — OTA flota: packaging + update-agent (soak/product-health rollback) + release/promote firmados (manifiesto = única fuente) + apply autónomo del bucket de releases (runner) validado contra AWS real

# ota+ci: empaquetado de releases + update-agent en la Pi (soak + rollback por salud de producto) + workflows firmados release/promote (manifiesto = única fuente de la versión deseada) + APPLY AUTÓNOMO del bucket de releases por el runner, validado contra AWS real

# PR11 — OTA de la flota + CI de versionado/releases: empaquetado, update-agent en la Pi (soak + rollback por salud de PRODUCTO), workflows firmados release/promote y APPLY AUTÓNOMO del bucket de releases (runner) con publicación de artefacto de prueba al bucket REAL

Eres un agente Claude CLI con acceso a un workspace git (monorepo `cam-counter`) y a herramientas de shell, edición de archivos y `gh` (GitHub CLI). Este prompt es TOTALMENTE AUTOCONTENIDO: no tienes más contexto que el aquí descrito. Lee y respeta cada sección. NO inventes hechos sobre el repo: si necesitas confirmar algo, ábrelo y léelo con tus herramientas.

> **MODELO DE DESPLIEGUE (LEE ESTO PRIMERO, APLICA A TODO EL PROMPT).** En esta pila hay DOS actores distintos que NUNCA se confunden:
> - **RUNNER MAD (TÚ, en este entorno):** dispones de credenciales AWS amplias en el ENTORNO del runner (jamás committeadas). ERES QUIEN EJECUTA `terraform apply` de forma **AUTÓNOMA** dentro de los PRs de infra, contra la cuenta REAL `950639281773` / `us-east-1`. En ESTE PR aplicas autónomamente el **bucket de releases** y validas contra el recurso REAL.
> - **GITHUB ACTIONS CI:** permanece **SOLO-PLAN** (fmt/validate/plan read-only vía OIDC con el rol `cam-counter-gha-plan`). **NUNCA** hace `apply` de infraestructura (evita escalada de privilegios desde PRs no confiables). Los workflows release/promote SÍ aplican el manifiesto (publican OBJETOS S3 — artefactos/manifiestos) usando el rol de deploy OIDC `cam-counter-gha-deploy` gated por Environment, pero eso **NO es `terraform apply` de infra** (distinción legítima: aplicar infra ≠ publicar artefactos).
>
> Donde versiones anteriores de este prompt decían "NO terraform apply / el apply es humano / merge-gated", eso queda DEROGADO: el **runner aplica autónomo** la infra de este PR (el bucket de releases) y CI sigue **plan-only**. Sé explícito y sin ambigüedad en tu implementación y en el cuerpo del PR.
>
> **JUSTIFICACIÓN DEL APPLY AUTÓNOMO PRE-MERGE (acota la tensión anti-escalada, F2 — LÉELO).** El runner aplica HCL a producción ANTES del merge humano. Esto se justifica EXCLUSIVAMENTE porque: (a) el HCL proviene de ESTA pila controlada y curada (no de HCL arbitrario de terceros); (b) el apply autónomo se restringe ÚNICAMENTE a los módulos enumerados de este PR (el bucket de releases) — el runner NO aplica HCL fuera de ese alcance; (c) antes de cada apply el runner INSPECCIONA el `terraform plan` y exige que sea ESTRICTAMENTE ADITIVO (solo "to add"), abortando ante cualquier `destroy`/`replace`. GitHub Actions CI permanece plan-only precisamente para cerrar la escalada desde PRs no confiables; el runner asume ese trade-off de forma consciente y acotada. Declara esta justificación en el cuerpo del PR.
>
> **STATE ADITIVO Y MONÓTONO (invariante de la pila, F1 — LÉELO).** El state `environments/prod` es UN solo state compartido por toda la pila apilada. El runner SOLO ejecuta `terraform apply` desde la rama apilada MÁS ALTA que contiene TODO el HCL acumulado hasta ese punto; NUNCA reaplica una rama inferior después de que una superior ya aplicó (reaplicar una rama que no conoce los módulos de una rama superior haría que `terraform plan` muestre DESTRUIR esos recursos). En la rama de este PR (apilada sobre PR10, que NO añade Terraform) el root `environments/prod` YA contiene TODOS los módulos de PR02–PR04; este PR solo AÑADE el bucket de releases. ANTES de cualquier apply, ejecuta `terraform plan` e INSPECCIÓNALO: si aparece CUALQUIER `destroy` o `replace` de un recurso ya existente (de PR02/PR03/PR04), **ABORTA el apply** y reporta. El plan debe ser ESTRICTAMENTE ADITIVO (solo el bucket de releases "to add", 0 change, 0 destroy).

---

## 1) Contexto

`cam-counter` (repo GitHub `jlsaco/cam-counter`, cuenta AWS `950639281773`, región `us-east-1`) evoluciona de un detector de personas mono-propósito a un PRODUCTO de conteo de personas edge-first, multi-cámara/multi-sitio, con UI local en la LAN y una flota de Raspberry Pi 5 + acelerador Hailo-8 actualizable por OTA. Todo vive en este monorepo.

Este PR es el **PR11** y el **ÚLTIMO** de una pila de 12 PRs apilados (PR00 … PR11). Cada PR ramifica sobre la rama del PR anterior. Cuando este PR se ejecuta, **YA están mergeados/apilados los PR00..PR10**, por lo que el workspace ya contiene (asúmelo y VERIFÍCALO leyendo el árbol antes de empezar):

- **PR00**: `CLAUDE.md`, `.claude/`, `scripts/verify_toolchain.sh` (verifica binarios; el assert de identidad AWS solo corre si hay credenciales). PR00 documenta en `CLAUDE.md`/`.claude/README.md` las INVARIANTES transversales de la pila: F1 (state aditivo/monótono — solo aplicar desde la rama acumulada más alta, plan estrictamente aditivo, abortar ante destroy/replace), F2 (apply autónomo acotado y justificado por el runner; CI plan-only) y F3 (esquema de tags unificado). También aclara que los workflows de release/promote SÍ publican objetos S3 con el rol de deploy OIDC, lo cual NO es `terraform apply` de infra.
- **PR00b**: secreto de cámara `RWCHBY` rotado/quemado y parametrizado vía env/SSM/`shared/`; `gitleaks` configurado en CI (sobre working tree/diff). **No debe haber ningún secreto en el repo.**
- **PR01**: el sistema del borde se movió a `v1/` vía `git mv` (historia preservada); el servicio `hailo-personas` sigue ejecutable mediante un **unit renderizado por instalador con placeholder** (NO con rutas absolutas hardcodeadas). Existe el directorio `contracts/` con los JSON Schemas compartidos. Existe `scripts/version.py` que deriva la versión SemVer canónica vía `git describe` y **degrada limpio sin tags** (devuelve algo tipo `0.0.0-dev.<n>+g<sha>`; `--json` emite `{\"version\": \"...\", \"git_sha\": \"...\"}`). Existe el esqueleto del repo.
- **PR02**: backend de estado remoto de Terraform (S3 + lock DynamoDB), layout `terraform/modules/<servicio>` + `terraform/environments/prod`, CI `terraform fmt/validate`. **El RUNNER aplicó AUTÓNOMAMENTE este backend** (bootstrap con backend LOCAL + `terraform init -migrate-state` al backend S3, idempotente, resolviendo el huevo-y-la-gallina sin intervención humana). Hay **UN solo state de producción** (`environments/prod`) con lock en DynamoDB que TODOS los applies de la pila comparten. PR02 fijó `default_tags` CAPITALIZADOS `{Project=\"cam-counter\", ManagedBy=\"terraform\", Env=\"prod\"}` MÁS, en cada recurso, los tags lógicos en MINÚSCULA `project=cam-counter` y `managed_by=mad-runner`. (NUNCA `ManagedBy=\"mad-runner\"` capitalizado: `ManagedBy` permanece en `terraform`.)
- **PR03**: proveedor OIDC de GitHub Actions + DOS roles separados: `cam-counter-gha-plan` (trust `sub=repo:jlsaco/cam-counter:pull_request`, read-only + acceso de lectura al state) y `cam-counter-gha-deploy` (trust limitado a `environment:prod` y/o `refs/heads/main` + tags, permisos para publicar releases/manifiestos, gated por Environment). Ambos con `aud=sts.amazonaws.com`. **El RUNNER aplicó AUTÓNOMAMENTE** el provider OIDC y ambos roles (resolviendo la idempotencia del provider OIDC de forma PERSISTENTE — import al state o `create_oidc_provider=false` fijado en el HCL/tfvars commiteado, no en un `-var` efímero — para que applies posteriores de la pila den 0 cambios). PR03 reusó los `default_tags` de PR02 (`ManagedBy=terraform` capitalizado) + tags lógicos `managed_by=mad-runner` en minúscula. **El apply autónomo de infra lo hace el runner; CI sigue plan-only.**
- **PR04**: bucket de media `cam-counter-media-950639281773`, tabla DynamoDB de eventos, tabla de **device registry** `cam-counter-devices` (PK `DEVICE#{device_id}`, GSI1 `CHANNEL#{release_channel}` / `DEVICE#{device_id}`), IAM least-privilege por-Pi (STS corto) con un trust ESTABLE hacia el principal base del runner (no la sesión asumida) para que PR10 pudiera asumirlo, outputs de Terraform (entre ellos `edge_role_arn`). **El RUNNER aplicó AUTÓNOMAMENTE** estos recursos y verificó que existen de verdad contra AWS real (`aws s3api head-bucket`, `aws dynamodb describe-table`, `aws iam get-role`). PR04 también usó `default_tags` capitalizados (`ManagedBy=terraform`) + tags lógicos `managed_by=mad-runner` en minúscula. El registry `cam-counter-devices` ya EXISTE como recurso real (lo usarás para espejar `desired_version` y para `fleet-status`).
- **PR05**: paquete Python del borde `cam_counter_edge` (NO `counter`), `Detector` (Hailo) + `DummyDetector` + tipos compartidos + validación de slugs, pytest en CI sin hardware.
- **PR06**: tracker centroid+IoU con tests de id estable tras reaparición.
- **PR07**: `LineCounter` (half-plane + histéresis + idempotencia por-track) + store SQLite con **crossing_seq monótono persistido por cámara** (no por track, no reiniciable).
- **PR08**: config de línea por-cámara en SQLite + hot-reload (`config_version`) + buffer/grabador de clips + cola `clip_uploads` con claves a prueba de colisiones.
- **PR09**: app FastAPI (schemas/device/health/cameras/config/counters/history/stream + WS) que sirve la SPA React/Vite/Tailwind + adaptador del motor + fuente FAKE.
- **PR10**: supervisor multi-cámara (VDevice Hailo compartido) + worker cloud-sync (offline-tolerante, **basado en manifiesto, NO en registry**) + Playwright E2E. **El cloud-sync de PR10 fue VALIDADO con una prueba de INTEGRACIÓN end-to-end contra los recursos REALES de PR04** (PutObject a media real bajo prefijo de prueba, CrossingEvent real a DynamoDB con conditional put, prueba de idempotencia de reintento, GetItem/HeadObject de vuelta, y limpieza final), idealmente asumiendo el rol per-Pi de PR04 vía `edge_role_arn`. Asume que el contrato A (event_id determinista + conditional put) ya está demostrado contra DynamoDB real.

### Contratos compartidos relevantes para ESTE PR (no re-litigar; respétalos al pie de la letra)

**TRES buckets S3 distintos, NUNCA conflados:**
1. `cam-counter-rpi-artifacts-950639281773` = backup de binarios EXISTENTE, **RESERVADO / NO TOCAR**.
2. `cam-counter-media-950639281773` = media de producto (clips), creado y APLICADO en PR04.
3. `cam-counter-fleet-releases-950639281773` = **artefactos OTA + manifiestos de canal** (lo crea/aplica ESTE PR; es el destino de publicación del workflow de release y del artefacto de prueba que tú validarás contra AWS REAL).

Todos los buckets NUEVOS: privados, BlockPublicAccess all-true, SSE-S3, bucket-owner-enforced, política deny TLS-only. **El bucket de releases lo crea ESTE PR como módulo Terraform en `terraform/modules/fleet-releases` + wiring en `environments/prod`, y el RUNNER lo APLICA de forma AUTÓNOMA contra AWS real** (no es trabajo humano). Verifica primero qué existe en `terraform/` antes de crear nada nuevo: si ya hay un módulo del bucket de releases de un PR previo, REÚSALO; si falta, créalo. **CI sigue solo fmt/validate/plan; el apply del bucket lo hace el runner en este PR.**

**RELEASE MANIFEST (CI ↔ flota), única fuente de la versión deseada.** Objeto por canal `channels/<channel>/manifest.json` en el bucket de releases. Forma canónica (debe validar contra `contracts/channel_manifest.schema.json`):
```json
{
  \"schema_version\": 1,
  \"channel\": \"canary\",
  \"version\": \"0.1.0\",
  \"sequence\": 1,
  \"artifact\": {
    \"key\": \"releases/0.1.0/cam-counter-edge-0.1.0-arm64.tar.gz\",
    \"sha256\": \"<hex>\",
    \"size_bytes\": 1234,
    \"sig_key\": \"releases/0.1.0/cam-counter-edge-0.1.0-arm64.tar.gz.minisig\"
  },
  \"native_blob\": { \"key\": \"native/<...>\", \"sha256\": \"<hex>\" },
  \"min_agent_version\": \"0.1.0\",
  \"released_at\": \"2026-01-01T00:00:00Z\",
  \"released_by\": \"github-actions\",
  \"git_sha\": \"<hex>\",
  \"previous_version\": null
}
```
- **Escritores ÚNICOS**: los workflows release/promote (regla single-writer, concurrencia optimista S3 If-Match/ETag).
- **Lector**: el update-agent OTA, vía IAM SigV4, **NUNCA presigned URLs** (para que una Pi mucho tiempo offline no choque con enlaces expirados).
- Canales válidos: `canary` y `stable`.

**DEVICE REGISTRY (espejo/observabilidad, DynamoDB `cam-counter-devices`):** PK `DEVICE#{device_id}`, GSI1PK `CHANNEL#{release_channel}` / GSI1SK `DEVICE#{device_id}`. Campos: `device_id`, `site_id`, `camera_ids` (lista), `release_channel` ('canary'|'stable'), `desired_version` (lo escribe el workflow cloud; = manifest version del canal), `reported_version` (lo escribe la Pi), `last_good_version`, `last_update_status` ('idle'|'downloading'|'verifying'|'activating'|'healthy'|'rolled_back'|'failed'), `last_update_error`, `last_seen_at` (ISO-8601 UTC), `agent_version`, `status` ('online'|'offline'|'updating'|'degraded'), `hardware {model, hailo_fw}`. **El agente NUNCA lee `desired_version` del registry para decidir**; solo hace heartbeat (UpdateItem `reported_version`/`last_seen_at`/`status`/`last_update_status`). El registry `desired_version` lo refleja únicamente el workflow de release/promote. Este registry YA EXISTE como recurso real (aplicado en PR04).

**VERSION STRING (única fuente de verdad):** tags git SemVer anotados (`vX.Y.Z`, prereleases `-rc.N`) derivados por `scripts/version.py` vía `git describe` (sin archivo VERSION committeado). La MISMA cadena fluye por: bundle-manifest del artefacto, campo `version` del channel manifest, `desired_version`/`reported_version` del registry, y `/api/device` `app_version`. Drift = comparación de strings. **CI de release/version DEBE usar `actions/checkout` con `fetch-depth: 0`** o `git describe` produce versiones erróneas.

**Layout en dispositivo (immutable + atomic activation):**
- `/opt/cam-counter/releases/<version>/` (inmutable, una carpeta por versión instalada).
- `/opt/cam-counter/current` = symlink atómico a la release activa. **`releases/` y `current` viven en el MISMO filesystem** (rename atómico garantizado; documenta esto).
- `/opt/cam-counter/shared/` = estado mutable que sobrevive a cada upgrade/rollback (DB SQLite, config de línea, `CAM_IP`, sysroot box64, lib SDK). **`shared/` PUEDE ser otro mount (solo datos)** — nunca lo cruces con un rename atómico.
- Deps pesadas del host (box64, ffmpeg, hailo-all, el `.hef`) quedan FUERA del payload OTA (instaladas en provisioning). El `native_blob` se referencia por key+sha256 en el manifiesto, vive en S3, **NO va embebido en el tarball**.
- Instalaciones atómicas: extraer a temp, luego `os.replace`/rename (mismo FS); jamás borrar `last_good` hasta confirmar salud; ficheros `.part` se descartan al **arranque del agente** (no solo al boot del SO).

**Identificadores:** `site_id`, `device_id`, `camera_id` son slugs ASCII minúscula `^[a-z0-9][a-z0-9-]{1,62}$` (sin `#`, sin `/`). `camera_id` = `{device_id}-cam{N}`.

**Cero secretos en git** (ver sección 4).

**Tags de trazabilidad/limpieza (REQUISITO DURO, esquema F3 unificado):** TODOS los recursos AWS que cree/aplique el runner en este PR deben llevar las `default_tags` CAPITALIZADAS heredadas del provider `{Project=\"cam-counter\", ManagedBy=\"terraform\", Env=\"prod\"}` MÁS los tags lógicos en MINÚSCULA `project=cam-counter` y `managed_by=mad-runner` en CADA recurso. NUNCA uses `ManagedBy=\"mad-runner\"` capitalizado: la clave `ManagedBy` permanece en `terraform`; el atributo del runner es la clave en MINÚSCULA `managed_by=mad-runner`. La verificación de trazabilidad busca la clave `managed_by` en MINÚSCULA. Esto permite trazar y limpiar lo que aplicó el runner. Los costos son bajos (S3 + DynamoDB on-demand) y debe existir un camino de teardown (`terraform destroy` o script).

---

## 2) Objetivo

Implementar el subsistema **FLEET-OTA-UPDATE** completo, su **CI de versionado/releases**, y el **APPLY AUTÓNOMO del bucket de releases por el runner validado contra AWS real**, de modo que:

1. Se pueda **empaquetar** una release del borde desde `v1/` como tarball arm64 determinista, **sin secretos de cámara ni rutas absolutas `/home/pi`**, con un **bundle-manifest** que fija el `native_blob` por key+sha256 (los blobs nativos NO van embebidos).
2. Un **update-agent en la Pi** (PULL-based, sin Greengrass/SSM) reconcilie la versión deseada **leída ÚNICAMENTE del channel manifest en S3** (vía SigV4, nunca presigned, nunca del registry), verifique **sha256 LUEGO firma minisign** contra una pubkey fijada, instale atómicamente, reinicie el servicio, haga **health-check de PRODUCTO con ventana de SOAK**, y haga **rollback seguro** a `last_good` ante cualquier fallo — todo offline-tolerante (un solo salto a la versión current del canal al reconectar, nunca una cola).
3. Existan workflows GitHub Actions **`release.yml`** (on tag `v*`: gate de tests completo, build, firma con clave traída de Secrets Manager vía OIDC, publicar al bucket de releases, auto-apuntar canary con sequence monótono + If-Match, espejar `desired_version` en el registry) y **`promote.yml`** (workflow_dispatch, prod-Environment-gated, **REPOINT-only** canary→stable y rollback a `previous_version`, sin rebuild, rechaza si el gate de salud de canary no se cumple). Estos workflows publican objetos S3 con el rol de deploy OIDC `cam-counter-gha-deploy` gated por Environment; **NO ejecutan `terraform apply` de infra** (eso lo hizo el runner). Publicar objetos S3 ≠ aplicar infra.
4. **El RUNNER cree (si falta) y APLIQUE de forma AUTÓNOMA el módulo Terraform del bucket de releases `cam-counter-fleet-releases-950639281773` contra AWS real**, compartiendo el state remoto de `environments/prod` con lock en DynamoDB, de forma **idempotente** (un segundo apply = \"0 to add, 0 to change\") y **ESTRICTAMENTE ADITIVA** (el plan solo AÑADE el bucket de releases: 0 change, 0 destroy sobre los recursos de PR02–PR04; aborta si aparece cualquier destroy/replace), y **verifique que el recurso existe de verdad** (`aws s3api head-bucket`, comprobación de BlockPublicAccess/SSE/política TLS).
5. **Se VALIDE contra el bucket REAL ya aplicado**: publicar un **artefacto de prueba** (clearly-marked, bajo un prefijo de prueba como `releases/_selftest/...` y `channels/_selftest/...`), escribir/leer su **channel manifest** de prueba, verificar que el manifiesto valida contra el schema, **leerlo de vuelta** (HeadObject/GetObject), y **LIMPIAR** todos los objetos de prueba al final (DeleteObject) para no contaminar el bucket. Esta validación demuestra que el bloque \"publicar release → leer manifiesto del canal\" funciona de verdad contra AWS REAL.
6. Se cree el **primer tag anotado `v0.1.0`** para que `scripts/version.py` produzca una versión de release real.

---

## 3) Alcance y NO-alcance

### Alcance (lo que SÍ debes crear/modificar)
- `ota/packaging/make-release.sh` — empaquetador del tarball desde `v1/`.
- `ota/agent/` — update-agent en Python (módulos + tests). Lenguaje Python.
- `ota/systemd/cam-counter-update.timer` + `ota/systemd/cam-counter-update.service` (oneshot, jitter, `Persistent=true`).
- `ota/tools/validate_manifest.py` — valida un manifiesto contra `contracts/channel_manifest.schema.json`.
- `ota/tools/` — herramienta `fleet-status` (enumera el canal vía GSI1 del registry y reporta drift desired vs reported).
- `ota/tests/` — suite pytest (ejecutable en x86 sin Pi/Hailo/cámara) MÁS una prueba de **integración real** gated por credenciales (ver más abajo y sección 8).
- `contracts/channel_manifest.schema.json` — si NO existe aún, créalo aquí; si ya existe de un PR previo, NO lo dupliques: reúsalo y verifica que coincide con la forma canónica de la sección 1.
- `scripts/publish_manifest.py` — publica/repunta un channel manifest con If-Match + sequence monótono y espeja `desired_version` en el registry.
- `scripts/build_edge_artifact.sh` — wrapper de build invocado por CI (puede delegar en `make-release.sh`).
- `.github/workflows/release.yml` y `.github/workflows/promote.yml`.
- **`terraform/modules/fleet-releases/`** — módulo del bucket de releases (si falta) + wiring en `terraform/environments/prod`. **El RUNNER lo APLICA autónomamente** en este PR (ver sección 6, paso de apply autónomo).
- **`ota/tools/selftest_release_bucket.py`** (o nombre equivalente) — script de validación que publica un artefacto+manifiesto de prueba al bucket REAL, verifica el manifiesto del canal, lo lee de vuelta y LIMPIA todo. Gated por presencia de credenciales (si no hay credenciales → skip con aviso, nunca falla en CI sin OIDC).
- El tag anotado `v0.1.0` (créalo localmente; el push del tag se documenta, ver sección 9).
- Documentación: una sección de OTA en `docs/` (o `ota/README.md`) que describa el flujo, el layout en dispositivo, la regla \"manifiesto = única fuente de versión deseada\", el contrato de health, el checklist de smoke on-Pi, **el apply autónomo del bucket por el runner (con la justificación F2 y la regla de state aditivo/monótono F1) y cómo se verifica contra AWS real**, y **el teardown** (`terraform destroy`/script) con la nota de bajo costo (S3 + DynamoDB on-demand).

### NO-alcance (NO tocar)
- **NO** modifiques la lógica de `v1/rtsp-enable/` (utilidad Java + box64 que reactiva RTSP). Es intocable.
- **NO** uses ni modifiques el bucket `cam-counter-rpi-artifacts-950639281773` (reservado).
- **NO** rompas ni renombres el servicio `hailo-personas`; sigue ejecutable. El **cutover** de `hailo-personas` a `cam-counter-edge` lo decide el orquestador humano (no en este PR; documenta que el rollback de cutover = re-habilitar `hailo-personas`).
- **NO** reescribas historia git, **NO** muevas archivos de PRs anteriores, **NO** toques otros módulos `terraform/` salvo para añadir/ajustar el módulo del bucket de releases y su wiring en `environments/prod` (que SÍ aplicarás autónomamente).
- **NO** apliques NI destruyas otros recursos de la pila (state backend, OIDC, media, tablas, IAM per-Pi): esos ya los aplicó el runner en PRs anteriores. En ESTE PR el runner solo aplica/verifica **el bucket de releases**. El apply debe ser ADITIVO y compartir el state de `environments/prod`: si el `terraform plan` muestra cualquier `destroy`/`replace`/`change` de un recurso de PR02–PR04, **ABORTA** (regla F1, state aditivo y monótono).
- **NO** apliques desde una rama inferior de la pila: aplica SOLO desde esta rama (la más alta, que acumula todo el HCL de PR02–PR04 + el bucket de releases). Reaplicar una rama inferior tras una superior destruiría recursos: PROHIBIDO (F1).
- **NO** hagas que GitHub Actions CI ejecute `terraform apply`: CI permanece **plan-only** (fmt/validate/plan read-only vía OIDC). El apply autónomo del bucket lo hace el RUNNER en este PR. (Los workflows release/promote SÍ publican objetos S3 con el rol de deploy OIDC; eso NO es apply de infra.)
- **NO** introduzcas Cognito/CloudFront/API Gateway ni dependencias de DynamoDB para que el agente decida actualizarse (el agente jamás requiere DynamoDB para actualizarse).
- **NO** uses presigned URLs en el agente.
- **NO** publiques manifiestos a prod (canales reales `canary`/`stable`) ni promuevas automáticamente sin tag + aprobación de Environment. La validación contra AWS real de este PR usa SOLO prefijos de prueba claramente marcados (`_selftest`) y los LIMPIA al final; NO toca los canales productivos.
- **NO** introduzcas secretos en el repo: el apply autónomo usa las credenciales del ENTORNO del runner (jamás committeadas); OIDC sigue siendo solo para GitHub Actions.

---

## 4) Restricciones

- **Stack:** Borde/OTA en **Python**. Empaquetado en **bash** (compatible POSIX/bash, pasa `bash -n` y `shellcheck`). CI en GitHub Actions YAML. Infra en Terraform (~> 5.x, prefijo `cam-counter-`, us-east-1, cuenta `950639281773`, `default_tags` CAPITALIZADOS `{Project=\"cam-counter\", ManagedBy=\"terraform\", Env=\"prod\"}` MÁS las etiquetas lógicas de trazabilidad en MINÚSCULA `project=cam-counter` y `managed_by=mad-runner` en cada recurso; NUNCA `ManagedBy=\"mad-runner\"` capitalizado).
- **Modelo de despliegue (DURO):** el **RUNNER** ejecuta `terraform apply -auto-approve` de forma AUTÓNOMA para el bucket de releases, contra la cuenta REAL `950639281773`/`us-east-1`, con el state remoto compartido de `environments/prod` y lock DynamoDB. El apply DEBE ser **idempotente** (segundo apply = \"0 to add, 0 to change\") y **estrictamente ADITIVO** (F1: el plan solo añade el bucket de releases; aborta ante cualquier destroy/replace/change sobre PR02–PR04). El runner SOLO aplica desde ESTA rama (la más alta de la pila), NUNCA reaplica una rama inferior tras una superior (F1). El apply autónomo pre-merge se justifica porque el HCL proviene de esta pila controlada y curada, se restringe al módulo del bucket de releases, y el plan se inspecciona para ser estrictamente aditivo (F2). **GitHub Actions CI permanece SOLO-PLAN** (fmt/validate/plan read-only vía OIDC); NUNCA hace apply de infra.
- **Edge-first / offline-tolerante:** el agente debe funcionar y converger sin conectividad continua; el estado mutable en `shared/` sobrevive a todo upgrade/rollback.
- **Multi-cámara/multi-sitio:** el health-check de producto evalúa por-cámara (`frames_processed` creciente por cámara). El supervisor (PR10) corre N cámaras bajo un proceso; el OTA actualiza el bundle, no por-cámara.
- **CERO SECRETOS EN EL REPO (REQUISITO DURO):**
  - Ninguna clave AWS de larga vida. GitHub Actions asume AWS vía **OIDC** (provider `token.actions.githubusercontent.com`, trust acotado a `repo:jlsaco/cam-counter` con condiciones de ref/tag/environment y `aud=sts.amazonaws.com`). El **runner** usa las credenciales de su ENTORNO (jamás committeadas) para el apply autónomo.
  - La **clave privada minisign de firma** se obtiene en tiempo de job desde **AWS Secrets Manager** vía el rol OIDC; **JAMÁS** se committea ni se imprime en logs.
  - La **pubkey minisign** fijada en el agente SÍ va en el repo/artefacto (es pública), idealmente en `ota/agent/keys/cam-counter-release.pub` y embebida/instalada en la Pi en provisioning.
  - El secreto de cámara `RWCHBY` está QUEMADO y NO debe aparecer en ningún archivo ni en el artefacto producido. El empaquetador debe **templatizar/excluir** secretos y rutas `/home/pi`.
  - `gitleaks` corre en CI: no introduzcas literales que parezcan secretos. El apply autónomo NO escribe secretos al repo.
- **AWS naming/prefijos:** bucket de releases EXACTO `cam-counter-fleet-releases-950639281773`; registry EXACTO `cam-counter-devices`; roles `cam-counter-gha-plan` / `cam-counter-gha-deploy`. No conflar buckets.
- **Versionado:** la cadena de `scripts/version.py` es la única fuente; CI de release usa `fetch-depth: 0`.
- **Validación contra AWS real:** la prueba de integración del bucket de releases corre SOLO si hay credenciales en el entorno; sin credenciales → **skip con aviso** (nunca rojo en CI sin OIDC). Usa prefijos `_selftest` y LIMPIA al final.

---

## 5) Rama base y rama nueva

- **Rama base (de la que partes):** `feat/10-supervisor-sync-e2e` (la rama del PR10, del que depende este PR). Si esa rama no existe localmente, créala desde su origen remoto (`git fetch origin && git checkout -b feat/10-supervisor-sync-e2e origin/feat/10-supervisor-sync-e2e`). Si tampoco existe remota porque la pila aún no se mergeó, parte de la rama que represente el HEAD de la pila acumulada; en caso de duda detente y reporta, pero por contrato la base de este PR es la rama de PR10. PR10 depende de que PR04 ya haya aplicado: asume que el registry y el media bucket reales existen. Esta rama de PR11 es la rama apilada MÁS ALTA: acumula todo el HCL de PR02–PR04 y es la ÚNICA desde la que el runner debe aplicar (F1).
- **Rama nueva a crear (este PR):** `feat/11-ota-release-promote`.

```bash
git fetch origin
git checkout feat/10-supervisor-sync-e2e   # base = rama del PR anterior
git checkout -b feat/11-ota-release-promote
```

El Pull Request se abrirá con **base = `feat/10-supervisor-sync-e2e`** (apilado), NO contra `main`.

---

## 6) Pasos sugeridos

1. **Reconocimiento.** Lee el árbol actual: confirma qué hay en `v1/`, `contracts/`, `scripts/version.py`, `terraform/` (en especial si ya existe un módulo del bucket de releases y el wiring en `environments/prod`, y CONFIRMA que el root `environments/prod` contiene los módulos de PR02–PR04 para garantizar que esta rama es la acumulada más alta — F1), `.github/workflows/`, y si ya existe `contracts/channel_manifest.schema.json`. Lee `scripts/version.py` para conocer su salida exacta (`--json`). NO asumas; verifica.

2. **`contracts/channel_manifest.schema.json`.** Si no existe, créalo con JSON Schema (draft 2020-12) que valide la forma canónica de la sección 1 (campos requeridos, tipos, `channel` enum `[\"canary\",\"stable\"]`, `version` patrón SemVer, `sequence` entero ≥ 1, `previous_version` string|null, `artifact`/`native_blob` con `key`+`sha256`).

3. **`ota/packaging/make-release.sh`.** Empaqueta `v1/` en `cam-counter-edge-<version>-arm64.tar.gz`:
   - Deriva `<version>` de `scripts/version.py` (sin pasar `--json` para el nombre, o parsea el JSON; sé consistente).
   - **Excluye/templatiza** secretos de cámara y rutas absolutas `/home/pi`. No incluyas `.git`, caches, ni archivos de estado.
   - Genera un **bundle-manifest** dentro del tarball (p.ej. `bundle-manifest.json`) que contenga `version`, `git_sha`, y el `native_blob` fijado por `{key, sha256}` (NO embebas el blob).
   - Build **determinista** (orden estable de archivos, mtime fijo, sin metadatos variables) para que el sha256 sea reproducible.
   - Flags requeridos por la verificación: `--dry-run` (imprime lo que haría, incluyendo la versión), `--list-bundle-files` (lista los archivos que entrarían en el tarball, sin construirlo).
   - Pasa `bash -n` y `shellcheck`.

4. **`ota/agent/`** (Python). Implementa el update-agent con estos pasos exactos:
   - Lee el **canal asignado** desde **config local provisionada** (p.ej. `/opt/cam-counter/shared/agent.toml` o `agent.json`), **nunca de la red**. Haz la ruta configurable por env para testear.
   - `GET channels/<channel>/manifest.json` del bucket de releases **vía SigV4 (boto3 / firma IAM), nunca presigned**. Este es el **ÚNICO** origen de la versión deseada.
   - Si `desired != current` y la versión no tiene **failed-marker**: descarga `artifact` + `.sha256` + `.minisig`.
   - Verifica **sha256 PRIMERO**, **firma minisign DESPUÉS** contra la **pubkey fijada**. Falla cierra el flujo (no instala).
   - Extrae a temp, luego **rename atómico** a `releases/<version>/` (mismo FS). Swap atómico del symlink `current`.
   - `systemctl restart cam-counter.service`.
   - **Health-check de PRODUCTO con SOAK** (ventana p.ej. 90–120s, configurable): hace polling de `/api/health` (o `/healthz`) y EXIGE: servicio active, **`frames_processed` creciente por cámara**, `last_inference_ts` reciente, `db_schema_version` esperado, `config_version` cargado, **y no crash-loop** (contador `NRestarts` de systemd estable durante la ventana). Un 200 estático NO basta.
   - **Éxito:** registra `last_good = <version>`, heartbeat al registry (UpdateItem `reported_version`/`last_seen_at`/`status=online`/`last_update_status=healthy`).
   - **Fallo (health-fail, frames=0, crash-loop, o non-200):** revierte el symlink a `last_good`, reinicia, escribe un **failed-marker por versión** (no se reintenta esa versión), heartbeat `last_update_status=rolled_back`/`failed` + `last_update_error`.
   - **NUNCA borra `last_good`**; conserva **≥ 2 releases**; descarta ficheros `.part` al **arranque del agente**.
   - Abstrae las dependencias externas (S3, systemctl, HTTP health, reloj) detrás de interfaces inyectables para poder testear sin Pi/AWS/red.

5. **`ota/systemd/cam-counter-update.timer` + `.service`.** Service `Type=oneshot` que ejecuta el agente; timer con `Persistent=true` (corre el run perdido tras estar offline) y **jitter** (`RandomizedDelaySec`) para no thundering-herd la flota.

6. **`scripts/publish_manifest.py`.** Publica/repunta `channels/<channel>/manifest.json` con:
   - **S3 If-Match/ETag** (concurrencia optimista, single-writer).
   - **`sequence` monótono** (lee el actual, incrementa).
   - Espeja `desired_version` en el registry (`cam-counter-devices`) para todos los devices del canal (vía GSI1), como observabilidad.
   - Soporta modo **repoint-only** (no rebuild): copia el artefacto/firma ya publicados de una versión a otro canal sin reconstruir.

7. **`ota/tools/validate_manifest.py`** y **`ota/tools/` fleet-status.** El validador toma `--schema contracts/channel_manifest.schema.json` y valida un manifiesto (o genera uno de ejemplo y lo valida en CI). `fleet-status` enumera un canal vía GSI1 del registry y muestra desired vs reported (con `--dry-run`/mock para CI).

8. **`.github/workflows/release.yml`** (on `push` de tags `v*`):
   - `actions/checkout` con **`fetch-depth: 0`**.
   - Gate de tests completo (ruff/pytest del borde y ota; lo que ya exista en CI de PRs).
   - Build del artefacto (`scripts/build_edge_artifact.sh` → `make-release.sh`), cálculo de `sha256`.
   - **Firma minisign** con clave **traída en runtime desde Secrets Manager vía OIDC** (rol de deploy). Jamás imprimir la clave.
   - Upload a `cam-counter-fleet-releases-950639281773` bajo `releases/<version>/`.
   - `scripts/publish_manifest.py` auto-apunta **canary** con sequence + If-Match y espeja `desired_version` en el registry.
   - El manifiesto resultante valida contra `contracts/channel_manifest.schema.json`.
   - **NO ejecuta `terraform apply`** de infra: el bucket ya lo aplicó el runner. CI/workflows solo publican OBJETOS S3 con el rol de deploy OIDC (publicar objetos ≠ aplicar infra).

9. **`.github/workflows/promote.yml`** (`workflow_dispatch`):
   - **prod-Environment-gated** (requiere aprobación de Environment).
   - **REPOINT-only**: promueve una versión ya publicada y soakeada de canary→stable, o rollback a `previous_version`. **Sin rebuild.**
   - **Rechaza** promover si el **gate de salud de canary no se cumple** (p.ej. consulta el registry/fleet-status y exige que los devices canary estén `healthy` en la versión candidata).
   - **NO ejecuta `terraform apply`** de infra (solo publica/repunta objetos S3 con el rol de deploy OIDC).

10. **APPLY AUTÓNOMO DEL BUCKET DE RELEASES (lo hace el RUNNER, en este PR).**
   - Si falta, crea `terraform/modules/fleet-releases/` (bucket EXACTO `cam-counter-fleet-releases-950639281773`: privado, BlockPublicAccess all-true, SSE-S3, bucket-owner-enforced, política deny TLS-only) + wiring en `terraform/environments/prod` (output del nombre/ARN). Etiqueta con los tags lógicos en MINÚSCULA `project=cam-counter` y `managed_by=mad-runner` además de las `default_tags` capitalizadas (`{Project=cam-counter, ManagedBy=terraform, Env=prod}`). NUNCA `ManagedBy=\"mad-runner\"` (F3).
   - **CONFIRMA QUE APLICAS DESDE LA RAMA ACUMULADA MÁS ALTA (F1):** esta rama (`feat/11-...`, sobre PR10) contiene todo el HCL de PR02–PR04 + el bucket de releases. NUNCA apliques desde una rama inferior. NUNCA reaplicas una rama inferior tras una superior.
   - Desde `terraform/environments/prod`, con el state remoto S3 compartido y lock DynamoDB de PR02, aplica el **ROOT COMPLETO** (NO uses `-target` como camino primario — es frágil y desaconsejado por HashiCorp, puede dejar el state parcialmente convergido y enmascarar drift; en esta rama el root ya contiene todos los módulos, así que el apply del root completo es lo natural):
     ```bash
     terraform fmt -check -recursive
     terraform init            # reusa el backend S3 remoto compartido (environments/prod)
     terraform validate
     terraform plan  -out=tf-prod.plan     # ROOT COMPLETO
     ```
   - **INSPECCIONA EL PLAN ANTES DE APLICAR (F1, gate duro):** el plan debe ser ESTRICTAMENTE ADITIVO — SOLO el bucket de releases (y sus sub-recursos: public-access-block, encryption, policy) \"to add\", con **0 to change y 0 to destroy** sobre los recursos de PR02–PR04. Si aparece CUALQUIER `destroy`, `replace`, o `change` inesperado de un recurso ya existente de un PR superior, **ABORTA el apply** y reporta (no apliques). Solo si el plan es aditivo, aplica:
     ```bash
     terraform apply -auto-approve tf-prod.plan
     ```
     (FALLBACK documentado SOLO si el wiring impide un plan limpio del root: `terraform plan -target=module.fleet_releases -out=tf-fleet-releases.plan` y `apply` de ese plan, dejando constancia del motivo. `-target` NO es el camino primario; úsalo solo como último recurso documentado.)
   - **Verifica que el recurso existe DE VERDAD** contra AWS real:
     ```bash
     aws s3api head-bucket --bucket cam-counter-fleet-releases-950639281773
     aws s3api get-public-access-block --bucket cam-counter-fleet-releases-950639281773
     aws s3api get-bucket-encryption  --bucket cam-counter-fleet-releases-950639281773
     aws s3api get-bucket-policy       --bucket cam-counter-fleet-releases-950639281773
     aws s3api get-bucket-tagging      --bucket cam-counter-fleet-releases-950639281773   # debe incluir la clave managed_by=mad-runner en MINÚSCULA (F3)
     ```
   - **Demuestra idempotencia:** un segundo `terraform plan` debe reportar **\"0 to add, 0 to change, 0 to destroy\"**. Deja constancia de esa salida.
   - Si NO hay credenciales AWS en el entorno (entorno de prueba sin runner), **NO falla**: documenta que el apply autónomo lo ejecuta el runner con credenciales del entorno y deja el `plan` (aditivo) como evidencia. El código Terraform debe quedar `fmt/validate`-limpio igualmente.

11. **VALIDACIÓN CONTRA EL BUCKET REAL (artefacto de prueba + manifiesto del canal).** Implementa `ota/tools/selftest_release_bucket.py` (o equivalente) y EJECÚTALO contra el bucket REAL ya aplicado:
   - (a) **Publica un artefacto de prueba** al bucket real bajo un prefijo claramente marcado, p.ej. `releases/_selftest/<runid>/cam-counter-edge-selftest-arm64.tar.gz` (PutObject), con su `.sha256` y un `.minisig` de prueba (puedes firmar con una clave efímera de prueba; NO uses la clave productiva de Secrets Manager para el self-test, y NO la imprimas).
   - (b) **Escribe un channel manifest de prueba** `channels/_selftest/manifest.json` (canal de prueba, NO `canary`/`stable` productivos) que referencie el artefacto de prueba por key+sha256.
   - (c) **Verifica que el manifiesto del canal valida** contra `contracts/channel_manifest.schema.json` (usa `validate_manifest.py`).
   - (d) **Léelo de vuelta** desde S3 real (HeadObject + GetObject), confirma sha256 del artefacto descargado == el publicado, y confirma que el manifiesto leído coincide con el escrito.
   - (e) **LIMPIA todos los objetos de prueba al final** (DeleteObject de artefacto, `.sha256`, `.minisig` y manifiesto de prueba) para no contaminar el bucket. Verifica con HeadObject que ya no existen.
   - Esta prueba es **gated por credenciales**: si NO hay credenciales en el entorno → **skip con aviso** (exit 0, mensaje claro), nunca rojo en CI sin OIDC. Las pruebas unitarias con fakes (S3 mock, etc.) son ADICIONALES y siguen existiendo.
   - **Anti-skip-indebido (gate runner-strict):** cuando SÍ hay credenciales en el entorno del runner, un SKIP de esta validación NO es aceptable: el self-test DEBE haber publicado/leído/limpiado de verdad contra AWS real (al menos un PASS efectivo). Documenta el comando exacto que el runner ejecuta en modo strict (que falla si, habiendo credenciales, no se produjo un PASS real). El skip legítimo es SOLO la ausencia de credenciales.
   - Idealmente, si el rol per-Pi de mínimo privilegio (de PR04, vía el output `edge_role_arn`) o el rol de deploy aplica, asume ese rol para validar también que el IAM acotado funciona; léelo de la fuente canónica (`terraform -chdir=terraform/environments/prod output -raw edge_role_arn` o variable inyectada por el orquestador). Si no se resuelve el ARN y la integración real está habilitada, trátalo como FALLO de configuración (no fallback silencioso a creds del entorno); fuera de ese caso, usa las credenciales del runner y documéntalo.

12. **Tests pytest** en `ota/tests/` que cubran (todos x86, sin Pi/Hailo/cámara salvo la integración real gated): minisign verify-pass y verify-fail; swap atómico; **health 200 pero frames=0 → rollback**; **crash-loop dentro del soak → rollback**; **health-fail → rollback a last_good**; versión failed-marked NO se reintenta; `last_good` nunca se borra (se conservan ≥2 releases); manifiesto valida contra el schema; **ciclo completo fake en qemu-arm64** (o un fake-release equivalente determinista si qemu no está disponible en el runner — documenta el fallback); `promote` rechaza cuando el gate de salud de canary no se cumple; **el artefacto NO contiene el secreto de cámara**; **igualdad de la version string** entre bundle-manifest, channel manifest, registry desired/reported y `/api/device app_version`. **MÁS** la prueba de integración real `selftest_release_bucket` (gated por credenciales: skip si no hay; PASS real obligatorio si hay).

13. **Tag `v0.1.0`.** Crea el tag anotado: `git tag -a v0.1.0 -m \"v0.1.0: primera release del borde cam-counter-edge\"`. Verifica que `scripts/version.py` ahora produce `0.1.0` (o `v0.1.0` según el contrato de salida del script; respeta lo que el script ya hace). El **push del tag** lo documentas en el cuerpo del PR (no lo empujes automáticamente a menos que sea inocuo; ver sección 9).

14. Documenta el **checklist de smoke on-Pi** (curl `/api/health`, cruce manual, Hailo presente, frames>0) como distinto de CI: CI verde nunca por sí solo promueve a la flota. Documenta también el **apply autónomo del bucket por el runner** (qué verifica contra AWS real, idempotencia, la justificación F2 y la regla de state aditivo/monótono F1) y el **teardown** (`terraform destroy` del módulo del bucket de releases o script de teardown; bajo costo S3 + DynamoDB on-demand; tags `project=cam-counter`/`managed_by=mad-runner` en MINÚSCULA para limpieza).

---

## 7) Definition of Done (objetivo y verificable)

1. `make-release.sh` construye un tarball arm64 **determinista** SIN secretos de cámara y SIN rutas absolutas `/home/pi`, con un bundle-manifest que fija el `native_blob` por sha256.
2. El **update-agent** lee su canal desde **config LOCAL** y la versión deseada **SOLO del channel manifest en S3** (nunca del registry); verifica **sha256 LUEGO minisign**; instala vía temp-extract + rename atómico + symlink swap (mismo FS); y ante fallo de la ventana de **SOAK** (frames no crecientes, crash-loop, o non-200) **revierte a last_good**, **nunca borra last_good**, y escribe un **failed-marker por versión**.
3. `release.yml` firma con una clave **traída en runtime desde Secrets Manager vía OIDC** (sin clave en git), publica a `cam-counter-fleet-releases-950639281773`, auto-apunta canary con **sequence monótono + If-Match**, espeja `desired_version` en el registry, y el manifiesto **valida contra `contracts/channel_manifest.schema.json`**. `release.yml`/`promote.yml` **NO ejecutan `terraform apply`** de infra (publican objetos S3 con el rol de deploy OIDC, distinto de apply de infra).
4. `promote.yml` está **prod-Environment-gated**, repunta una versión ya publicada (sin rebuild), soporta **rollback a `previous_version`**, y **rechaza** promover cuando el gate de salud de canary no se cumple.
5. **El RUNNER aplicó AUTÓNOMAMENTE el bucket de releases** `cam-counter-fleet-releases-950639281773` contra AWS real (compartiendo el state de `environments/prod` + lock DynamoDB), aplicando SOLO desde esta rama acumulada más alta (F1) y tras INSPECCIONAR el plan para confirmar que es **ESTRICTAMENTE ADITIVO** (solo el bucket de releases to add; 0 change, 0 destroy sobre PR02–PR04; se aborta ante cualquier destroy/replace — F1). Se **verificó que existe de verdad** (`aws s3api head-bucket` + BlockPublicAccess + SSE + política TLS + tagging con `managed_by=mad-runner` en MINÚSCULA), y el apply es **idempotente** (segundo `terraform plan` = \"0 to add, 0 to change, 0 to destroy\"). El apply autónomo pre-merge está justificado y acotado al módulo del bucket de releases (F2); **GitHub Actions CI permanece plan-only.** (Si el entorno carece de credenciales, queda el `plan` aditivo como evidencia y se documenta que el runner ejecuta el apply.)
6. **La validación contra AWS REAL pasó:** se publicó un **artefacto de prueba** al bucket real bajo prefijo `_selftest`, su **channel manifest de prueba valida** contra el schema, se **leyó de vuelta** (HeadObject/GetObject, sha256 coincide) y se **LIMPIARON** todos los objetos de prueba al final (verificado con HeadObject que ya no existen). La prueba es gated por credenciales (skip con aviso SOLO si no hay; con credenciales en el runner, un skip NO cuenta como DoD cumplido: exige PASS real), nunca contamina los canales productivos.
7. La version string de `scripts/version.py` (tras crear `v0.1.0`) es **idéntica** en bundle-manifest, channel manifest `version`, registry `desired/reported_version` y `/api/device app_version` (hay un **test que asegura igualdad**); y hay un **test que asegura** que una release \"health 200 pero frames=0\" se hace **rollback**.

Reglas adicionales que también deben cumplirse (de la auditoría): el agente NUNCA requiere DynamoDB para actualizarse; manifiesto = única fuente de versión deseada; `releases/`+`current` mismo FS, `shared/` puede ser otro mount; `.part` descartados al arranque del agente; health-check de PRODUCTO (no liveness); `hailo-personas` sigue ejecutable y el cutover es decisión humana; el apply autónomo NO introduce secretos en el repo (usa credenciales del ENTORNO del runner); state environments/prod ADITIVO Y MONÓTONO — solo aplicar desde la rama acumulada más alta, plan estrictamente aditivo, abortar ante destroy/replace (F1); apply autónomo acotado y justificado, CI plan-only (F2); todos los recursos del runner llevan tags `project=cam-counter`/`managed_by=mad-runner` en MINÚSCULA, con `ManagedBy=terraform` capitalizado en default_tags (F3); existe camino de teardown documentado.

---

## 8) Verificación (ejecuta estos comandos y deja constancia de su salida)

Ejecuta exactamente lo siguiente desde la raíz del repo (ajusta rutas solo si la estructura real difiere, documentándolo):

```bash
# 1) Sintaxis bash + shellcheck del empaquetador y scripts del agente
bash -n ota/packaging/make-release.sh ota/agent/*.sh 2>/dev/null; shellcheck ota/packaging/make-release.sh

# 2) Suite OTA completa (verify pass/fail, atomic swap, soak frames=0 rollback,
#    crash-loop rollback, failed-marker, last_good-retained, promote-gate)
cd ota && python3 -m pytest -q ; cd ..

# 3) Schema del manifiesto carga como JSON válido y el validador corre
python3 -c \"import json; json.load(open('contracts/channel_manifest.schema.json'))\" && python3 ota/tools/validate_manifest.py --schema contracts/channel_manifest.schema.json

# 4) La versión derivada aparece en el dry-run del empaquetador
VER=$(python3 scripts/version.py) && bash ota/packaging/make-release.sh --dry-run | grep -q \"$VER\"

# 5) El artefacto NO contiene el secreto quemado de cámara
bash ota/packaging/make-release.sh --list-bundle-files 2>/dev/null | xargs grep -rli 'RWCHBY' 2>/dev/null | grep . && echo 'SECRET LEAKED INTO ARTIFACT' && exit 1 || true
```

Además (Terraform + validación contra AWS real; el apply lo hace el RUNNER con credenciales del entorno; CI sigue plan-only):
```bash
# 6) Terraform fmt/validate del módulo del bucket de releases y su wiring
terraform -chdir=terraform/environments/prod fmt -check -recursive
terraform -chdir=terraform/environments/prod init
terraform -chdir=terraform/environments/prod validate

# 7) APPLY AUTÓNOMO del bucket de releases por el RUNNER (solo con credenciales del entorno).
#    F1: plan del ROOT COMPLETO (NO -target como camino primario) y verificación de que es
#    ESTRICTAMENTE ADITIVO (solo el bucket de releases; 0 change, 0 destroy). Si hay cualquier
#    destroy/replace/change inesperado de PR02-PR04 -> ABORTAR (no aplicar).
#    Si NO hay credenciales -> deja el plan como evidencia y documenta; NO falla CI.
if aws sts get-caller-identity >/dev/null 2>&1; then
  terraform -chdir=terraform/environments/prod plan -out=tf-prod.plan
  # Gate F1: el plan NO debe destruir/reemplazar nada existente. Inspección legible:
  terraform -chdir=terraform/environments/prod show tf-prod.plan | grep -E 'will be destroyed|must be replaced' && echo 'PLAN NO ADITIVO: ABORTAR (F1)' && exit 1 || true
  terraform -chdir=terraform/environments/prod apply -auto-approve tf-prod.plan
  # Verificación de recurso REAL
  aws s3api head-bucket --bucket cam-counter-fleet-releases-950639281773
  aws s3api get-public-access-block --bucket cam-counter-fleet-releases-950639281773
  aws s3api get-bucket-encryption  --bucket cam-counter-fleet-releases-950639281773
  # F3: el tagging debe incluir la clave managed_by en MINÚSCULA con valor mad-runner
  aws s3api get-bucket-tagging --bucket cam-counter-fleet-releases-950639281773 | grep -q '\"Key\": \"managed_by\"' && echo 'tag managed_by OK'
  # Idempotencia: segundo plan = sin cambios
  terraform -chdir=terraform/environments/prod plan -detailed-exitcode  # exit 0 == sin cambios
else
  echo \"Sin credenciales: el RUNNER ejecuta el apply autónomo; CI queda plan-only\"
fi

# 8) VALIDACIÓN contra AWS real: publicar artefacto de prueba al bucket real,
#    verificar manifiesto del canal, leerlo de vuelta y LIMPIAR (gated por credenciales).
#    Con credenciales en el runner: un skip NO es aceptable -> exige PASS real (modo strict).
python3 ota/tools/selftest_release_bucket.py --bucket cam-counter-fleet-releases-950639281773 --cleanup
```

Y finalmente:
```bash
# Lint Python del nuevo código OTA si ruff está disponible en el repo
ruff check ota scripts || true
# El tag existe y version.py lo refleja
git tag -l | grep -q '^v0.1.0$' && python3 scripts/version.py
# Los workflows son YAML válido
python3 -c \"import sys,yaml; [yaml.safe_load(open(f)) for f in ['.github/workflows/release.yml','.github/workflows/promote.yml']]; print('workflows OK')\"
```

Todos los comandos de la sección 8 deben pasar (exit 0 donde aplica; los `|| true` son tolerados por diseño pero ni la rama de \"SECRET LEAKED\" ni la de \"PLAN NO ADITIVO\" deben dispararse NUNCA). El paso 7 (apply autónomo) lo ejecuta el RUNNER con credenciales del entorno y debe: aplicar SOLO desde esta rama acumulada más alta (F1), inspeccionar el plan para confirmar que es estrictamente aditivo y abortar ante cualquier destroy/replace (F1), verificar el recurso real e idempotencia, y confirmar el tag `managed_by=mad-runner` en minúscula (F3); el paso 8 publica/lee/limpia el artefacto de prueba contra el bucket real (skip con aviso SOLO si no hay credenciales; con credenciales exige PASS real, nunca rojo en CI sin OIDC). Si algún comando falla por una diferencia legítima de estructura del repo real, corrige el código/los comandos y documenta el ajuste en el cuerpo del PR.

---

## 9) Entrega: abrir el Pull Request y devolver su URL

1. Asegura el working tree limpio salvo tus cambios; haz commits atómicos y descriptivos. Termina CADA mensaje de commit con:
   ```
   Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
   ```
2. Push de la rama: `git push -u origin feat/11-ota-release-promote`.
3. Sobre el **tag `v0.1.0`**: por defecto **NO lo empujes** automáticamente (empujar un tag a `jlsaco/cam-counter` dispararía `release.yml` que intenta firmar/publicar al bucket de releases real; eso es una publicación deliberada, no parte de este PR). Crea el tag **localmente** y **documenta** en el cuerpo del PR el comando exacto para que el orquestador humano lo empuje deliberadamente tras el merge:
   ```
   git tag -a v0.1.0 -m \"v0.1.0\" && git push origin v0.1.0
   ```
   (Si tu entorno tiene una política explícita de empujar tags y el orquestador lo aprobó, hazlo y dilo en el PR; en ausencia de esa señal, NO lo empujes.)
4. Abre el PR **apilado** con `gh`, base = la rama del PR anterior:
   ```bash
   gh pr create \
     --base feat/10-supervisor-sync-e2e \
     --head feat/11-ota-release-promote \
     --title \"PR11 — OTA flota: packaging + update-agent (soak/product-health rollback) + release/promote firmados (manifiesto = única fuente) + apply autónomo del bucket de releases (runner) validado contra AWS real\" \
     --body \"<CUERPO>\"
   ```
   El `<CUERPO>` debe incluir, en secciones claras:
   - **Qué**: empaquetado de releases, update-agent en la Pi con soak + rollback por salud de producto, workflows release/promote firmados, apply autónomo del bucket de releases por el runner, validación contra AWS real (artefacto de prueba + manifiesto del canal), tag v0.1.0.
   - **Por qué**: cerrar el subsistema OTA/CICD; manifiesto S3 como única fuente de versión deseada (offline-tolerante, un solo salto); rollback seguro que detecta \"200 pero conteo roto\"; dejar el bloque \"publicar release → leer manifiesto del canal\" FUNCIONANDO de verdad contra AWS real.
   - **Cómo verificar**: pega los comandos de la sección 8 y su salida resumida, incluyendo la verificación del recurso real (`head-bucket`, BlockPublicAccess, SSE, política TLS, tagging `managed_by=mad-runner`), la inspección del plan como estrictamente aditivo (0 destroy/0 replace — F1), la prueba de idempotencia (segundo plan \"0 to add, 0 to change\") y el self-test del bucket (publicar/leer/limpiar).
   - **Modelo de despliegue**: el RUNNER aplicó autónomamente el bucket de releases contra la cuenta real 950639281773/us-east-1, compartiendo el state de environments/prod con lock DynamoDB, aplicando SOLO desde esta rama acumulada más alta y con plan inspeccionado como estrictamente aditivo (F1); el apply autónomo pre-merge está justificado y acotado al módulo del bucket de releases (F2); GitHub Actions CI permanece plan-only (fmt/validate/plan vía OIDC); los workflows release/promote publican objetos S3 con el rol de deploy OIDC, no hacen terraform apply (publicar objetos ≠ aplicar infra).
   - **Notas de seguridad**: cero secretos en git; clave de firma desde Secrets Manager vía OIDC; pubkey fijada en el agente; artefacto sin `RWCHBY` ni rutas `/home/pi`; el apply autónomo usa credenciales del ENTORNO del runner (jamás committeadas); el self-test usa prefijos `_selftest`, no toca canales productivos y limpia todo.
   - **Trazabilidad/teardown/costo**: recursos etiquetados `project=cam-counter` y `managed_by=mad-runner` (clave en MINÚSCULA; `ManagedBy=terraform` capitalizado en default_tags — F3); existe `terraform destroy` (o script de teardown) para desmontar el bucket de releases; costo bajo (S3 + DynamoDB on-demand).
   - **Acción manual pendiente**: empujar el tag `v0.1.0` tras el merge (dispara `release.yml`); cutover `hailo-personas`→`cam-counter-edge` lo decide el orquestador.
   - Termina el cuerpo con:
     ```
     🤖 Generated with [Claude Code](https://claude.com/claude-code)
     ```
5. **DEVUELVE la URL del PR** como salida final de tu ejecución (imprímela claramente, p.ej. la que devuelve `gh pr create`).

---

## Recordatorios finales (prohibiciones explícitas)
- PROHIBIDO cualquier secreto en el repo o en el artefacto (claves AWS de larga vida, clave privada minisign, password de cámara `RWCHBY`). Solo OIDC + Secrets Manager + env/SSM + pubkey pública + credenciales del ENTORNO del runner para el apply (jamás committeadas).
- PROHIBIDO que el agente lea `desired_version` del registry para decidir: el **manifiesto S3 es la única fuente**.
- PROHIBIDO presigned URLs en el agente; usa SigV4/IAM.
- PROHIBIDO borrar `last_good`, declarar salud sin soak, o aceptar liveness en vez de salud de producto.
- PROHIBIDO tocar `rtsp-enable/`, el bucket `cam-counter-rpi-artifacts-...`, romper `hailo-personas`, o aplicar/destruir recursos AWS distintos del bucket de releases (los demás ya los aplicó el runner en PRs anteriores).
- PROHIBIDO aplicar desde una rama inferior de la pila o reaplicar una rama inferior tras una superior (destruiría recursos): aplica SOLO desde esta rama acumulada más alta, con plan estrictamente aditivo, y ABORTA ante cualquier destroy/replace (F1).
- PROHIBIDO usar `-target` como camino primario del apply (frágil/desaconsejado): aplica el ROOT COMPLETO con plan inspeccionado como aditivo; `-target` solo como fallback documentado (F5).
- PROHIBIDO usar `ManagedBy=\"mad-runner\"` capitalizado: `ManagedBy` permanece en `terraform`; el atributo del runner es la clave en MINÚSCULA `managed_by=mad-runner` (F3).
- PROHIBIDO que GitHub Actions CI ejecute `terraform apply`: CI es **plan-only**. El apply autónomo del bucket lo hace el RUNNER en este PR (idempotente, verificado contra AWS real). Los workflows release/promote SÍ publican objetos S3 con el rol de deploy OIDC; eso NO es apply de infra (F9).
- PROHIBIDO que el self-test contamine los canales productivos (`canary`/`stable`): usa prefijos `_selftest` y LIMPIA todo al final. Con credenciales en el runner, un skip del self-test NO cuenta como cumplido: exige PASS real.
- PROHIBIDO abrir el PR contra `main`: base = `feat/10-supervisor-sync-e2e` (apilado).
- Si algo del entorno real contradice este prompt, NO inventes: inspecciona los archivos, ajústate a la realidad del repo y documenta la desviación en el PR.
